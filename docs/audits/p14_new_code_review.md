# P14 - adversarial review of the P13 session's own code

Run `wf_1b621b24-7ef`, 2026-08-01. Scope: the ~2,900 lines written during the P13
adjudication session - four new files (hooks/capped_report.py, the two *_selftest.py
siblings, tools/check_readme_fresh.py) and sixteen changed ones. Eight lenses, every
finding refuted individually, NO cap on the refuter fan-out.

## Denominator

| | count |
|---|---|
| findings produced | 47 |
| adjudicated | 46 |
| **dropped - never refuted** | **1** |
| confirmed | 42 |
| refuted | 4 |

The dropped finding is **OPEN, not absent**. The refuter for
`verify:ast-guard-completeness:1` died on a session usage limit, so that candidate has
never been confirmed or refuted; P14 cannot be called closed until it is. The coverage
block REPORTED the gap instead of printing the survivor count as the total - which is the
P13 Step 0 fix working on a real run, against a real interruption.

Confirmation rate is 42/46, an order of magnitude above the ~7% of the P13 pass. That is
what fresh, never-reviewed code looks like, and it is the argument against shipping it on
the strength of a green suite: every one of these landed with 22/22, 30/30 and CI green.

Produced per lens: capped-report-correctness 4, ast-guard-completeness 8, freshness-gate-soundness 8, selftest-split-fidelity 3, mutation-harness-integrity 8, run-selftests-denominator 5, meta-audit-semantics 6, dispatcher-and-payload 5

## Confirmed findings

### HIGH

- **has_decision_tag treats any bracketed prose containing an allow-word as a decision tag (markdown links, URLs, code spans, ordinary parentheticals)**
  - where: C:/Users/ammar/Downloads/unbluff/hooks/meta_audit_on_stop.py
  - why: _ALLOW_BRACKET_RE imposes no tag shape or position, so any parenthetical, markdown link, code span or URL query containing a \b-delimited allow-word silently suppresses a genuine hiding line and removes it from the total, and no test in the repo pins or detects this.
  - fix: Constrain the bracket to TAG SHAPE: require the allow-word at the start of the bracket content with a length bound, e.g. rf"[(\[]\s*(?:{_TAG_ALT})[^)\]]{{0,30}}[)\]]" (verified: passes all 22 selftests + integration unchanged). Add the bracketed twins of the four [P13 B3] should-FIRE cases to _selftest_line_cases, and a "B3-bracket" entry to tools/mutation_check.py that widens the bracket body back to [^)\]]*. Consider dropping bare "done"/"closed" from _ALLOW_TAGS, or requiring them tag-initial, since \b matches inside "closed-loop"/"done-list".
- **The P12 split silently disarmed the finding-10 regression test: `_child()` spawns the SELFTEST module, not the hook**
  - where: C:/Users/ammar/Downloads/unbluff/hooks/pre_push_gate_selftest.py
  - why: `_child()` really does spawn `pre_push_gate_selftest.py` (no `__main__` block, exits 0 instantly), so check 14 and the process half of check 13 are assertions that cannot fail - but the claimed consequence is wrong: reverting `run_tests` turns `fast_test_on_stop.py --selftest` red with four failures, so `run_selftests.py`, CI and mutation #10 all still catch finding 10.
  - fix: hooks/pre_push_gate_selftest.py:185 - spawn the hook, not the suite: `os.path.abspath(_m.__file__)`. Then add a guard the snapshot cannot lie about, e.g. assert `_child` targets a path whose basename is `pre_push_gate.py`, or have `_child` assert the spawned rc/stderr is non-empty for a known-blocking repo. Same audit for any other dunder the line-22 snapshot drops (`__file__`, `__name__`, `__spec__`) in both *_selftest.py siblings.
- **pre_push_gate.main()'s fail-open handler is pinned by NO test, and the assertion that claims to cover it cannot fire**
  - where: C:/Users/ammar/Downloads/unbluff/hooks/pre_push_gate_selftest.py
  - why: Check 13's `rc_c not in (0, 1)` provably cannot distinguish a crashing hook from a healthy one (CPython exits 1 on an uncaught exception and 1 is accepted), and removing `main()`'s fail-open handler leaves every gate in the repo green.
  - fix: In `_child` (line 185) launch `_m.__file__`, not `__file__` - it currently runs the selftest module, which has no `__main__` guard and always exits 0 (this also revives check 14). Then assert on stderr, not just rc: check 13's command is `python -c "pass"`, so require `rc_c == 0` and `"Traceback" not in err_c` - rc 1 alone can never separate a BLOCK from a crash. Add a mutation_check.py entry that deletes `main()`'s try/except, so the fail-open handler README:248 promises is actually pinned.
- **missing_gates() is dead code: main()'s own missing-gate branch has no test, and mutation A3 certifies the copy nobody calls**
  - where: C:/Users/ammar/Downloads/unbluff/run_selftests.py
  - why: Reproduced with the repo's own harness: reverting main()'s inline missing-gate branch (lines 133-140) SURVIVES because mutation A3 only mutates missing_gates(), which main() never calls, and the suite then prints "all 21 selftests passed" rc 0 while readme-fresh reports "22 matches 22" - but the reviewer's "every green signal agreed" is wrong, since selftest()'s live missing_gates(HERE) check turns the CI mutations job red (rc=1, "baseline already RED").
  - fix: Delete the inline branch: have main() call missing_gates(HERE) once, fail on any label, and keep ran = len(AUX_GATES) so the denominator cannot shrink. Re-anchor mutation ('./run_selftests','A3') at main()'s production branch (or add an A3-PROD entry) so the harness reverts the code that actually runs. Make check_readme_fresh compare the README count against the last gate_runs.json "ran" as well as expected_count(), so a declared-but-unexecuted gate is visible.
- **`ran` counts hook files whose --selftest never executed: has_selftest matches the dispatch inside a comment and rc==0 is accepted as proof**
  - where: C:/Users/ammar/Downloads/unbluff/run_selftests.py
  - why: Reproduced end to end: `_DISPATCH_RE` matches a commented-out dispatch so the hook stays in the swept set, and `run_selftests.py` accepts rc==0 with zero bytes of output as proof, so a hook whose 10 selftest assertions never execute is counted and printed as `usage_snip_prompt: OK` inside `all 22 selftests passed`.
  - fix: run_selftests.py:112 - capture stdout/stderr and require an execution marker, not just rc: FAIL unless rc==0 AND re.search(r"SELFTEST (OK|PASS)", out); 15/16 hooks already print "SELFTEST OK", the disarmed one printed 0 bytes. Normalize duplicate_registration_check.py's "SELFTEST PASS:" line (the only non-conforming marker) so the check is a floor, not a filter. Add a MUTATIONS entry whose verify_unit IS usage_snip_prompt - it is the only detected hook that is never a verify target, so its selftest is currently unpinned by the mutation harness too.
- **Mutation #10 verifies against the mutated unit itself, so the twin it claims to cover was never checked**
  - where: C:/Users/ammar/Downloads/unbluff/tools/mutation_check.py
  - why: Confirmed mechanically: mutation #10 self-verifies, and re-wiring it to the twin its own comment names returns "SURVIVED - the test for finding 10 is DECORATIVE", because pre_push_gate_selftest's `_child()` executes `os.path.abspath(__file__)` — the selftest module itself, which has no `__main__` guard and exits rc=0 in 0.2s — making both of its call sites unfailable.
  - fix: hooks/pre_push_gate_selftest.py:185 - `os.path.abspath(__file__)` -> `os.path.abspath(_m.__file__)`; post-split __file__ is the guard-less selftest module, which runs nothing. Then add "pre_push_gate" as the 6th element of mutations #10 and #28/34 in tools/mutation_check.py so the documented twin is the actual verifier. Durable guard: before any other _child assertion, assert _child on a repo with a failing test command returns non-zero - a subprocess helper that can only return 0 must fail loudly, not silently pass.
- **The mutation harness is the only unit in the repo that nothing verifies: it can be gutted and every gate stays green**
  - where: C:/Users/ammar/Downloads/unbluff/tools/mutation_check.py
  - why: Gutting `run()` in a scratch copy makes the harness print "81 of 81 mutations executed ... all mutations caught" in 389 ms with exit 0 while run_selftests (22/22), test_integration (30/30) and hook_health_check all stay green, so the sole verifier of the entire 81-entry evidence base has no verifier of its own.
  - fix: Add a `--selftest` to tools/mutation_check.py holding a POSITIVE CONTROL: call `run()` with a no-op edit (e.g. adding a space in `_CLAIM_RE = re.compile` in tools/check_readme_fresh) and require the verdict to start with "SURVIVED" — it returns "CAUGHT" the moment run() is short-circuited, and takes ~2 s. Then move "mutation_check.py" out of run_selftests.NOT_A_GATE into AUX_GATES with `("--selftest",)` so the fast self-check is gated even though the full table stays its own CI job. Also add a MUTATIONS entry for unit "tools/mutation_check" reverting that guard, so the harness is pinned by the same rule it enforces on the other 19 units.
- **A type annotation or tuple target on the cap makes the twin-guard blind to a byte-identical forbidden slice**
  - where: hooks/capped_report.py
  - why: Reproduced exactly: `_max_names` (hooks/capped_report.py:68-76) collects only `ast.Assign` with bare `ast.Name` targets, so annotating or tuple-assigning a cap constant removes its name from `caps` and `slicing_offenders` goes blind to a byte-identical raw-slice reintroduction of the P13 defect, with every gate reporting green.
  - fix: In `_max_names`, also collect `ast.AnnAssign` targets and `ast.Name` elements of tuple/list `ast.Assign` targets (one shared "bound names" walk). Then add planted fixtures to `selftest()` for BOTH new forms (`MAX_B: int = 3` and `MAX_B, _P = 3, 0`) alongside the existing plain-assign `planted.py` - the current canary only exercises the one form the code already handles, so it cannot notice the guard going blind. Guard against the twin: the collection-cap branch (line 117) shares the same `caps` set and is disarmed identically, so one fix covers both only if it lands in `_max_names`.
- **Display-cap detection only matches a bare Name upper bound - alias, arithmetic, del-tail and islice all pass**
  - where: hooks/capped_report.py
  - why: The "assume a fifth" sweep matches exactly one syntactic shape (`xs[:MAX_NAME]`) and, worse, its `if not caps: continue` gate skips 13 of the 18 hook files outright, so it only ever inspects the 4 modules already routed through capped_report and prints "clean" over a denominator it never names.
  - fix: Delete the `if not caps: continue` whole-file skip and the MAX_ prefix keying; scan every hook and report the file denominator it swept. Flag any Slice whose upper OR lower subtree contains any Name/Constant, any ast.Delete of a Subscript-with-Slice, and islice/pop-while loops; allowlist survivors by (module, name) via BOUND_EXEMPTIONS. Add one planted fixture per shape (alias, BinOp, del-tail, islice, bare literal) to selftest() so a shape that silently stops matching fails the suite.
- **slicing_offenders (the 'assume a fifth' guard) is blind to 9 of 11 cap spellings, and a fifth unrouted cap already exists in hooks/ that it reports as clean**
  - where: hooks/capped_report.py:79-123
  - why: Demonstrated: slicing_offenders matches only 2 of 11 cap spellings, misses reversed-compare/return/nested-break caps that are inside its own documented MAX_* scope, and already reports the live unrouted cap at hooks/hook_health_check.py:470 as clean, so its stated invariant ("a new cap not routed here is a selftest failure") cannot be enforced.
  - fix: Line 116: also inspect cmp_node.left (not just .comparators), and use ast.walk(node) for Break/Return instead of direct-body children. Drop the `if not caps: continue` early-skip and additionally flag Slice uppers that are numeric literals or non-MAX_* module constants, so hook_health_check.py:470 `problems[:12]` is caught. Replace the tautological canary: plant all ~11 spellings in selftest() and assert each is either flagged or on an explicit documented-blind-spot list.

### MEDIUM

- **_is_superseded still accepts a MENTION rather than a declaration, silently skipping an entire ACTIVE plan file**
  - where: C:/Users/ammar/Downloads/unbluff/hooks/meta_audit_on_stop.py
  - why: Confirmed on the pristine module: the `[#>*\-\s]*` prefix lets a bullet, blockquote or plain sentence that merely opens with "superseded" satisfy `_is_superseded`, so `run()` returns (0,'') and silently skips an entire ACTIVE plan file, and no selftest case exercises the word at line start.
  - fix: Drop `>` and `-` from the prefix class (bullets/blockquotes are prose, not declaration context) and require a declaration tail: `^[#*\s]*(?:status\s*[:\-]\s*)?\**\s*superseded\b(?=\**\s*(?:$|[:\-.,)]|by\b|as\s+of\b|on\b))`. Bias must stay toward NOT-superseded: over-declaring costs silence, under-declaring only costs noise. Add selftest cases with 'superseded' as the FIRST token of a bullet, a blockquote and a bare sentence, plus a mutation that re-adds `>`/`-` to the prefix class.
- **has_decision_tag accepts an ALL-CAPS allow-word anywhere on the line, contradicting its own "IN TAG POSITION" contract**
  - where: C:/Users/ammar/Downloads/unbluff/hooks/meta_audit_on_stop.py
  - why: The all-caps half of has_decision_tag searches the whole line, so an ALL-CAPS allow-word appearing anywhere in ordinary prose (CLOSED, DONE) suppresses a genuine hiding line from both the bullet list and the reported total, making an incomplete list read as complete.
  - fix: Search _ALLOW_CAPS_RE only in the line's tag region (text before the first ':', else the first ~3 tokens after stripping bullet punctuation) instead of the whole line; keep _ALLOW_BRACKET_RE whole-line. Verified: all 21 existing line fixtures still pass unchanged, including line 283 - so no fixture needs revisiting, contrary to the finding's claim. Add the four demo lines as expect-True fixtures so the position anchor cannot be regressed back to case-only.
- **count_unpushed reports 0 unpushed commits when @{u} resolves to a LOCAL branch, reproducing the exact B1 failure it was written to fix**
  - where: C:/Users/ammar/Downloads/unbluff/hooks/meta_audit_on_stop.py
  - why: `count_unpushed` branches on whether `git rev-list --count @{u}..HEAD` succeeds rather than on whether the upstream is a remote-tracking ref, so when `@{u}` resolves to a local branch the command succeeds, the remote-aware fallback on line 157 is skipped, and commits that exist only on this machine are silently under-reported - the exact P13 B1 silence the function's own docstring says it fixed.
  - fix: In count_unpushed, resolve the upstream first: `up = _git(cwd, "rev-parse", "--symbolic-full-name", "@{u}")` and only run the `@{u}..HEAD` count when `up` starts with `refs/remotes/`; otherwise fall through to the existing remotes-based branch (verified: fixes both rows, selftest still OK). Add a fifth shape to _selftest_unpushed: clone, `git config branch.autoSetupMerge always`, commit N times without pushing, `checkout -b spike`, assert count_unpushed >= N. Extend the B1 mutant (tools/mutation_check.py:340) with a second mutation that drops the remote-upstream check, so the new case is proven to be load-bearing.
- **An auxiliary gate that exists and exits 0 is recorded as passed with no evidence it checked anything; 4 of the 6 gate scripts are re-run by no mutation**
  - where: C:/Users/ammar/Downloads/unbluff/run_selftests.py
  - why: The blind spot is real and I reproduced it - gutting three gate scripts to `raise SystemExit(0)` leaves run_selftests printing "all 22 selftests passed" at RC=0 with the integration suite bit-identical to pristine - but the reviewer's headline proof is wrong, because the deleted skills/completeness-audit/SKILL.md is caught hard by tests/test_integration.py, a gate CI runs.
  - fix: Add one mutation per unmutated aux gate (gut its assertion branch, require the gate to go red against a deliberately-broken fixture tree) so "does this check bite?" has an answer for all 6, not 2. Give mutation_check.main() a coverage line that names checks with zero mutations, so the gap is printed rather than inferred - a denominator it currently omits. Cheaper interim: require each AUX_GATES entry to expose --selftest and run gate plus selftest, turning a no-op script into a missing-selftest FAIL.
- **check_readme_fresh gates one number in a README that pastes two transcripts as evidence; the ungated one is already stale by 4 scenarios**
  - where: C:/Users/ammar/Downloads/unbluff/tools/check_readme_fresh.py
  - why: check_readme_fresh.py's only regex is `all (\d+) selftests passed`, so the second pasted transcript in the same "this is exactly what CI runs" block is ungated and is already stale by 4 scenarios (README says 26/26, the suite CI runs prints 30) while the gate prints "readme-fresh: OK".
  - fix: Widen the gate past one regex: also match `==== (\d+)/(\d+) scenarios passed ====` and compare its denominator to a DERIVED integration total (static `record(...)` count + `len(install.SKILL_NAMES)`, mirroring how expected_count() derives the selftest total), reusing the existing absent-claim FAIL branch so a removed paste cannot go quiet. Then assert the pasted `name: OK` set equals `selftestable_hooks(...) | AUX_GATES` labels, not just its cardinality. Pin both with a mutation: bump a scenario / rename a hook and require readme-fresh to go red.
- **--release never runs the scope/denominator check, so a tracked .py outside the one-level globs is never asked about and the gate exits 0**
  - where: C:/Users/ammar/Downloads/unbluff/tools/check_review_freshness.py
  - why: `main()` never calls `_scope_failures()`, so `--release` genuinely exits 0 over tracked `.py` files that `UNIT_GLOBS` cannot reach — but the scope check runs as a mandatory AUX gate in `run_selftests.py`, which CI runs on every push, so the sabotage cannot actually ship green.
  - fix: In `main()`, when `args.release`, also run `_scope_failures(REPO, load_ledger())` and return 1 on any result. Optionally widen `UNIT_GLOBS` to a recursive `**/*.py` so depth is not the thing that decides scope. Pin it with a mutation that deletes the new `main()` scope call (today only `--selftest` bites).
- **units() interpolates the repo path into a glob pattern unescaped; one `[` in the checkout path makes the unit list empty and --release prints a full all-clear**
  - where: C:/Users/ammar/Downloads/unbluff/tools/check_review_freshness.py
  - why: Reproduced exactly as claimed - a `[` in the checkout path makes every UNIT_GLOB match nothing, `units()` returns `[]` with no third state, and `--release` prints the full all-clear at exit 0 where the identical copy elsewhere exits 1 on 32 stale/unreviewed units - but it is not silent end-to-end, since `--selftest` (which `run_selftests.py` and CI run) goes loudly red on the same tree, which is why this is MEDIUM rather than HIGH.
  - fix: Line 82: `glob.glob(os.path.join(glob.escape(repo), *pattern.split("/")))`. That alone leaves the zip+`git init` trigger, so also give `units()` its third state: an empty result must land in the `unknown` bucket (verified: `--release` then exits 1 on both shapes, suite stays 22/22). Pin with a mutation that reverts the escape and one that accepts `[]`, and sweep the sibling `glob.glob(os.path.join(<dir>, ...))` calls listed in the evidence.
- **Submodule content is dropped by the git intersection with no report: the glob finds the file, git ls-files does not list it, and BOTH --release and --selftest report all-clear**
  - where: C:/Users/ammar/Downloads/unbluff/tools/check_review_freshness.py
  - why: Confirmed end-to-end: `found &= tracked` in units() is an unprinted filter, so a committed, checked-out submodule .py under `skills/*/scripts/` is dropped with both --release and --selftest exiting 0 — but no submodule exists in the repo today, so it is latent rather than HIGH.
  - fix: In units(), compute `dropped = found - tracked` and PRINT it — never drop in silence (untracked scratch stays allowed, just reported). In _scope_failures(), FAIL when any dropped path is visible to `git ls-files --recurse-submodules` — that is committed, shipped content, not scratch. Do not just add --recurse-submodules to _tracked(): last_change() must then read `git -C <submodule> log`, or the unit is UNKNOWN forever.
- **The hermetic fixture does not fail for UNIT_GLOBS narrowings other than the one mutation A1 tests, so mutation_check certifies UNIT_GLOBS as pinned when only one narrowing shape is**
  - where: C:/Users/ammar/Downloads/unbluff/tools/check_review_freshness.py
  - why: The fixture plants one file per glob, so it detects glob DELETION but never glob NARROWING, making the lines 284-285 claim "Narrowing UNIT_GLOBS therefore fails here on any machine, in any directory" demonstrably false - though the ambient denominator check still catches every narrowing that reduces coverage in a real checkout, so the escaping residue is latent-only rather than an active false all-clear.
  - fix: In `_selftest_fixture()` plant a SECOND adversarially-named file per multi-file glob (`hooks/_zz.py`, `tools/zz.py`, `tests/conftest.py`, `scripts/zz.py`, `skills/demo/scripts/zz.py`) and assert those too - verified to turn all five narrowings CAUGHT with the baseline still green. Then correct the docstring to what the fixture actually asserts (every glob still matches something), since "any narrowing fails here" is the overclaim. Optionally add a `tools/check_review_freshness` mutation for a within-glob narrowing so the harness samples that shape at all.
- **The EXEMPT floor's stated guard does not exist: it names a function that was never written, is never printed, and a rotting entry is never reported**
  - where: C:/Users/ammar/Downloads/unbluff/tools/check_review_freshness.py
  - why: The comment at line 35 promises that `_selftest_scope` prints EXEMPT "so it cannot grow in silence", but no such function exists, EXEMPT is never printed by any code path, and no check reports an EXEMPT entry naming a file that is no longer tracked — while the analogous list in the same suite has exactly that guard.
  - fix: In `_scope_failures`, after `tracked` is known, add: `fails += ["EXEMPT names %s, which is not tracked (the exemption is rotting)" % p for p in sorted(EXEMPT - tracked)]`. Print the EXEMPT list (and its length) in `selftest()` and in `main()`'s denominator line so it cannot grow in silence. Fix the line 35 comment to name whatever function actually does this, and mutation-test the new check.
- **A tracked file deleted from the worktree but not yet committed turns --selftest RED with a message naming a file that is not in the repo**
  - where: C:/Users/ammar/Downloads/unbluff/tools/check_review_freshness.py
  - why: Confirmed and broader than claimed: deleting a reviewed unit turns `--selftest` (and the whole suite, and the push gate) red not just between `rm` and commit but permanently after a clean `git rm` + commit, because the orphan check at line 255 builds `recorded` from the entire append-only ledger history, so the only remediation is scrubbing historical audit entries.
  - fix: In `_scope_failures`, judge both checks against worktree reality: drop index-only paths from `tracked` (`os.path.isfile(os.path.join(repo, p))`) before computing `missing`. For orphans, flag a recorded unit only when its file still exists but falls outside `asked` (a real scope gap); a recorded unit whose file is gone is history, not a silent no-op. Keep the fixture honest by planting a deleted-but-indexed file and a committed-deletion case, else a mutation reverting either filter survives.
- **Mutation anchors are checked for presence, not uniqueness, so a mutation can silently retarget a different function and the verdict becomes a statement about code the finding never touched**
  - where: C:/Users/ammar/Downloads/unbluff/tools/mutation_check.py
  - why: Confirmed: mutation_check.py validates anchors with `if find not in text` then `replace(find, replace, 1)`, and transcript_util #13's anchor already matches twice, so any reordering of that file silently retargets the edit to a function the finding never touched - but no current verdict is wrong, which is why this is a latent hazard rather than HIGH.
  - fix: In run(), replace the presence test with uniqueness: `n = text.count(find)` then return "HARNESS ERROR: anchor matches %d times (ambiguous)" % n unless n == 1. That check fails #13 as written, so widen its anchor to include first_text's unique preceding lines: prefix with ` if isinstance(content, str):\n return content\n` (verified: 1 match). Consider making the same uniqueness assertion a startup audit over all MUTATIONS so a future reorder fails loudly at the table, not at edit time.
- **The `skipped` bucket is unreachable, so the CI-must-not-skip failure path is dead code and every skip is auto-relabelled as another platform's problem**
  - where: C:/Users/ammar/Downloads/unbluff/tools/mutation_check.py
  - why: Both SKIPPED return sites in run() (lines 402, 404) contain the word "only", so line 521's prose-substring test can never select the `skipped` bucket, making the "A skip is NOT a pass" warning and the `CI must not skip: failing.` hard fail at lines 551-556 unreachable dead code.
  - fix: Classify by the marker, not the prose: have run() return the platform flag alongside the verdict (e.g. "SKIPPED-PLATFORM" when posix_only is True/"nt", plain "SKIPPED" otherwise) and branch on that at line 521. Add a selftest asserting run() emits at least one non-platform SKIPPED shape, so the `skipped` bucket and its CI hard fail stay reachable. Add a gate (tools/, like check_readme_fresh) asserting .github/workflows/selftest.yml still defines the mutations-windows job the message defers to.
- **`executed` counts HARNESS ERRORs as executed, so the one headline number the evidence base rests on overstates what actually ran**
  - where: C:/Users/ammar/Downloads/unbluff/tools/mutation_check.py
  - why: Line 530 omits `len(errors)` from the `executed` subtraction, so mutations that were never applied are still counted as executed, and the summary line directly contradicts the HARNESS ERRORS block printed two lines below it.
  - fix: Line 530: subtract the bucket too - `executed = considered - len(skipped) - len(other_platform) - len(unproven) - len(errors)`. Line 533: add `%d harness-error` to the summary format so the count appears in the line that gets pasted, not only in the block below it. Worth a `--selftest` on main()'s bucket arithmetic plus a `("tools/mutation_check", ...)` entry, since this is the only gate with no mutation covering itself.
- **Collection-cap detection only matches `if ... >= MAX_NAME: break` in the If body - return, reversed comparison, else-branch, while and comprehension all pass**
  - where: hooks/capped_report.py
  - why: The collection-cap half of `slicing_offenders` only matches an `ast.Break` sitting directly in `node.body` with the `MAX_*` name on the right of the compare, so five other ways of stopping a scan early — all of which destroy the total exactly as P13 B7 did — are structurally invisible to it, though no hook in the repo evades it today.
  - fix: In `slicing_offenders` (hooks/capped_report.py:111-122): test `[cmp_node.left] + cmp_node.comparators`, not comparators alone; treat `ast.Return`/`ast.Continue` alongside `ast.Break` and scan `node.orelse` as well as `node.body`. Add `ast.While` (walk `node.test`) and comprehension `generator.ifs` to the node kinds inspected — a `while len(hits) < MAX_X:` guard is the more idiomatic spelling and is currently unseen. Plant one file per shape in the selftest's tempdir block (lines 158-171) so the meta-check's denominator is the shape list, not two hand-picked cases; a naive "any MAX_* load not passed to keep()/render()" rule is NOT a safe substitute — I ran it and it false-positives 6 times on the clean tree (memory_hygiene_guard:147,318; meta_audit_on_stop:298,312,531; plan_defer_guard:371).
- **BOUND_EXEMPTIONS is an unverified roster: 2 of its 3 entries are already dead, and a bogus entry raises nothing**
  - where: hooks/capped_report.py
  - why: Confirmed and worse than claimed: BOUND_EXEMPTIONS has no liveness check (bogus entries raise nothing, 2 of 3 real entries are inert), and because it is keyed by (module, name) rather than by shape it silently exempts a genuine display slice `xs[:MAX_FILE_BYTES]` that the guard would otherwise flag.
  - fix: In selftest(), for each (module, const) in BOUND_EXEMPTIONS fail unless hooks/<module>.py parses and const is in _max_names(tree) - the same stale_exempt shape run_selftests.py already uses for NOT_A_GATE. Then apply the exemption only in the collection-cap (`break`) branch, never in the `[:MAX_*]` slice branch, so an exempted name can never hide a display cap. Add a mutation_check unit that deletes a still-live exemption AND one that plants `xs[:MAX_FILE_BYTES]`, so both halves stay load-bearing.
- **An exemption whitelists the constant name across the whole module, not the one bound it was written for**
  - where: hooks/capped_report.py
  - why: `BOUND_EXEMPTIONS` is keyed by `(module, name)` rather than by call site, so exempting one anti-balloon bound silently pre-authorises every future use of that identifier anywhere in the same file — and two of the three entries whitelist nothing at all today, so they are pure pre-authorisation.
  - fix: Key the exemption by call site, not by name: drop `BOUND_EXEMPTIONS` and instead exempt a node only when its own source line (`lines[node.lineno-1]`) contains a literal marker like `# capped-report: bound`; annotate numbers_match_on_write.py:432 and delete the two dead entries. Add the inverse selftest the current one lacks — plant a cap using a *bound* name on an unmarked line and assert it IS flagged, so the exemption can never again widen unnoticed. Also note for later (out of scope here): the collection-cap shape only matches `break`, so an early `return` inside a scan (numbers_match_on_write.py:191) is invisible to the guard.
- **numbers_match_on_write still has the exact collection-cap lie capped_report exists to abolish, and BOUND_EXEMPTIONS certifies it as safe with a false rationale**
  - where: hooks/numbers_match_on_write.py:432 (and hooks/capped_report.py:35)
  - why: Confirmed by running the real hook on a 700-finding fixture: the header and the truncation notice both print 500, the BOUND_EXEMPTIONS entry does hide this cap from slicing_offenders, but the gate still exits 2 with an explicit truncation notice rather than a false clean pass, so the harm is an under-stated count in an extreme regime, not a missed detection.
  - fix: In run(), bound STORAGE not the count: `total += 1` for every unmatched number, append only `if len(findings) < MAX_FINDINGS_TRACKED` (drop the break). Thread `total` into build_message() for the header and into capped_report.render(..., total=total). Then delete the ("numbers_match_on_write","MAX_FINDINGS_TRACKED") line from BOUND_EXEMPTIONS, update the M2 mutation in tools/mutation_check.py:231 (its anchor text disappears), and add a >MAX_FINDINGS_TRACKED case to the M2 selftest so the 500-vs-real-total distinction is pinned.
- **plan_defer_guard's claims/promises render() call sites have no regression test at all - reverting them to the pre-fix silent slice keeps the entire suite green**
  - where: hooks/plan_defer_guard.py:154-155
  - why: Confirmed: reverting hooks/plan_defer_guard.py:154-155 to bare `claims[:2]`/`promises[:5]` slices leaves every gate green (22/22 selftests, 30/30 integration, 5/5 plan_defer_guard mutations caught, both targeted --selftests OK) while silently dropping 4 of 9 promises with no truncation notice, because the repo's only promise fixture has exactly one promise and slicing_offenders only flags slices bounded by a MAX_* name.
  - fix: In _selftest_dangling add a fixture with 3 claims and 9 promises; assert the message contains "not shown", "9 total", and "3 total". Add a mutation_check entry for plan_defer_guard that reverts lines 154-155 to bare `claims[:2]`/`promises[:5]` and requires the suite to go red. Optionally teach capped_report.slicing_offenders to flag integer-literal slice bounds inside build_message-style renderers, not just MAX_* names.
- **Both dispatchers' exit-code contract is asserted nowhere; CRASH_RC=-1 is a truthy sentinel feeding the dict that decides the exit code**
  - where: hooks/stop_dispatcher.py
  - why: Confirmed: rewriting the exit-code expression at stop_dispatcher.py:108 to `any(results.values())` survives 22/22 selftests and 30/30 integration with a baseline-identical result set, yet flips main() from 0 to 2 when every sub-hook crashes, because CRASH_RC=-1 is truthy and no test drives main() through a crash.
  - fix: In BOTH dispatchers, extend _selftest_crash_is_not_clean to drive main() end to end: seed sys.modules[name]=Boom for every name in HOOKS (run_hooks binds hooks=HOOKS at def time), point sys.stdin at a JSON payload, and assert main() == 0 with an isolated UNBLUFF_LEDGER_PATH. Then register the pinning mutation in tools/mutation_check.py: ("stop_dispatcher", "C1c", ...) and ("post_tooluse_dispatcher", "C1d", ...) replacing "return 2 if any(rc == 2 for rc in results.values()) else 0" with "return 2 if any(results.values()) else 0", and confirm both report CAUGHT. Cheaper belt-and-braces: have main() compute `any(rc == 2 ...)` via a named helper (e.g. `fired_keys(results)`) already used by write_ledger, so the exit code and the ledger's "fired" list cannot drift apart.
- **The transcript_util twin-guard watches 3 of the classifier's names, so a complete divergent second implementation is invisible to it**
  - where: hooks/transcript_util.py
  - why: The twin-guard's three hardcoded regexes watch only `SYNTHETIC_PREFIXES`/`first_text`/`is_synthetic`, so a complete divergent second `is_genuine_user` classifier ships all-green while the guard's own docstring claims it asserts "exactly ONE implementation exists".
  - fix: Derive the watched names instead of listing them: at selftest time collect transcript_util's own module-level `def` names + UPPER_CASE constants (via `ast` or `globals()`), then scan hooks/*.py for `^\s*def _?<name>\b` and `^\s*_?<name>\s*=` for each. Keep the current three as a FLOOR (a name that stops being watched is an error), and print the denominator so a shrinking roster is visible. Then resolve the one real hit: delete show_your_proof.get_content and use transcript_util.get_content, or allowlist it with a written reason.
- **The exemption logic is pinned by no mutation - broadening it leaves the entire evidence base green**
  - where: tools/mutation_check.py
  - why: Broadening BOUND_EXEMPTIONS is a real behaviour change that every gate reports green, because all three capped_report mutations pin what the guard REPORTS and none pins what it IGNORES.
  - fix: In capped_report.selftest(), plant a fixture module named something other than numbers_match_on_write that caps against each exempted constant, and assert it IS still flagged (pins module-scoping). Also assert every BOUND_EXEMPTIONS entry is load-bearing: dropping it must yield >=1 offender - this fails today for MAX_FILE_BYTES and MAX_SOURCE_VALUES. Add a mutation_check entry that broadens the predicate (module-blind), verified via capped_report.

### LOW

- **The "unpushed bullet survives the display cap" assertion in _selftest_unpushed cannot fail**
  - where: C:/Users/ammar/Downloads/unbluff/hooks/meta_audit_on_stop.py
  - why: The line 537 check `if "unpushed" not in msg` can never fire because build_message's hardcoded header "[meta-audit] parked/unpushed state at stop:" contains the substring "unpushed", so the assertion is satisfied by every possible message including build_message([], 0) — the cap arithmetic the reviewer cites is real but not the load-bearing cause.
  - fix: Line 537: compare against the full bullet text, not the bare substring — e.g. bind `unp = next((f for f in findings if "commit(s) unpushed" in f), None)` and assert `unp and unp in msg`, mirroring the live pattern at line 315. Do NOT "fix" this by adding a second plan file: I built that fixture (17 findings, real truncation, bullet genuinely discarded) and the check still passed on the header. Same header-substring trap applies to line 337's `"[meta-audit]" not in msg`; line 540's "not shown" check is live but passes via the per-file cap, so correct its comment.
- **_SUPERSEDED_DECL_RE rejects several ordinary markdown ways of declaring a file superseded**
  - where: C:/Users/ammar/Downloads/unbluff/hooks/meta_audit_on_stop.py
  - why: Reproduced exactly — underscore-emphasis, table-row and HTML-comment declarations are rejected, and any YAML front matter with 3+ keys pushes the heading past the hard 5-line window so such a file can never be recognised — but the consequence is only once-per-session noise on a root-level archived plan, so LOW stands.
  - fix: Strip a leading `---`…`---` YAML block before taking the first 5 lines (kills the hard boundary); a `status: superseded` key inside front matter already works at line <=5. Do NOT widen the `[#>*\-\s]*` prefix class — it already OVER-matches the other way: `- superseded: PLAN_V2.md (see archive/)` or `## Superseded documents` in an ACTIVE plan skips the whole file at exit 0 (demonstrated). Add a regression case for that bullet/heading form; the shipped P13 B4 fixture only pins the mid-sentence prose shape, so it cannot detect it.
- **--record writes any string as a unit and reports success, so a review recorded under a non-canonical path is a silent no-op that --release still calls UNREVIEWED**
  - where: C:/Users/ammar/Downloads/unbluff/tools/check_review_freshness.py
  - why: The missing record-time validation is real and reproducible, but it is not silent and not MEDIUM: every non-canonical spelling the reviewer names is caught by the orphan check, which turns the whole suite red, and the failure direction is fail-closed (a mis-spelled record can never turn a unit green).
  - fix: In record(), intersect each normalised unit with units(REPO); on a miss print "not a unit the gate asks about" plus the closest match and return 1 instead of 0. Also reject a --unit that yields zero components: `--unit ','` prints success and writes nothing, the only variant no orphan check can ever see. Pin both with a mutation so the guard cannot be removed silently.
- **--record accepts a future timestamp with no sanity check, permanently marking a unit FRESH; the same skew is reachable by accident from a wrong clock**
  - where: C:/Users/ammar/Downloads/unbluff/tools/check_review_freshness.py
  - why: The missing future-stamp guard is real and reproduces exactly as described, but it is worth LOW rather than MEDIUM because the identical all-green bypass is already reachable through the plain documented `--record` path with no flag at all, so `--utc` grants no one any power they did not already have.
  - fix: In record(): `if args.utc and (_parse(args.utc) is None or _parse(args.utc) > now)` -> sys.exit an error, so a bad or future stamp never reaches the ledger. In evaluate(): after `reviewed_at, entry = newest[unit]`, add a branch putting `reviewed_at > now` into `unknown` ("ledger stamp is in the future") - the ledger is a file anyone can edit, so the write-side check alone is not enough. Make _parse() attach timezone.utc to naive results, which also kills the TypeError at line 201; pin all three with a case in _selftest_third_state().
- **A mutation unit that lives outside COPY_TREES aborts the whole run with an unhandled FileNotFoundError, and COPY_TREES disagrees with the repo's own definition of a reviewable unit**
  - where: C:/Users/ammar/Downloads/unbluff/tools/mutation_check.py
  - why: Demonstrated: an entry for a unit outside COPY_TREES (e.g. scripts/make_demos, which UNIT_GLOBS counts as reviewable) passes the guarded live-file read and the baseline, then dies on the unguarded twin read at line 437, aborting the loop with no summary - but all 19 units currently referenced resolve inside the copy set, and the crash is a loud exit-1 traceback that can never certify a false green, so it is latent and LOW, not MEDIUM.
  - fix: Add "scripts" to COPY_TREES so it matches check_review_freshness.UNIT_GLOBS (skills/*/scripts is already covered wholesale). Wrap the line-437 read in the same try/except OSError as lines 388-392, returning "HARNESS ERROR: target missing in scratch tree (unit not under COPY_TREES)". Durable version: alongside duplicate_ids(), pre-check in main() that every MUTATIONS unit resolves inside COPY_TREES/COPY_FILES and fail fast with a named bucket rather than mid-loop.
- **An unknown or renamed filter argument runs zero mutations and exits 0**
  - where: C:/Users/ammar/Downloads/unbluff/tools/mutation_check.py
  - why: A filter argument matching none of the 22 valid unit tokens runs zero mutations and still exits 0, so the exit status of a filtered run cannot distinguish "this unit is pinned" from "this name no longer exists".
  - fix: Build `_ALL_NAMES` from MUTATIONS (hook, verify, and their basenames) once in main(); if `args.only and args.only not in _ALL_NAMES`, print the valid tokens and `return 1` before the loop. Optionally also `return 1` when `considered == 0`, so a future match-narrowing change cannot reintroduce the vacuous pass. A regression test should assert exit 1 for a bogus name, since exit 0 for a *valid* filter (verified: `show_your_proof` -> 1 of 81, exit 0) must stay 0.
- **CAUGHT is any non-zero exit code and the mutated selftest's output is thrown away, so a test that bit and a file that merely crashed are indistinguishable**
  - where: C:/Users/ammar/Downloads/unbluff/tools/mutation_check.py
  - why: `run()` derives CAUGHT from `p.returncode` alone and discards the captured output, so a selftest that crashed before evaluating any assertion is reported identically to one whose assertion bit - and I demonstrated that a deliberately disarmed assertion is still certified CAUGHT.
  - fix: Keep `p.stdout`/`p.stderr`: if the red run shows no explicit failure marker (SELFTEST FAIL / SELFTEST FAILED / FAIL:) or shows a `Traceback (most recent call last)`, return a distinct verdict (e.g. `CRASHED (rc=%s, no assertion fired)`) and bucket it with UNPROVEN rather than CAUGHT. Print the output tail for that bucket so the author can see which path blew up, and fail under CI as the SKIP/UNPROVEN buckets already do. Same shape as the earlier rc==77 carve-out (V131_REVIEW_PLAN MEDIUM-2) - that fixed one value; this generalises it.
- **Guard scope is hardcoded to hooks/*.py - a cap in tools/ or a hooks/ subpackage is invisible**
  - where: hooks/capped_report.py
  - why: The selftest's positive-control offender is invisible when planted in `tools/report_tool.py` or `hooks/lib/shared.py` (SELFTEST OK, exit 0) yet fails loudly as a byte-identical file in `hooks/` (exit 1), so the module's unqualified invariant holds in only one of the five directories the repo's own `UNIT_GLOBS` calls units.
  - fix: Have selftest() union slicing_offenders() over the repo's canonical UNIT_GLOBS (hooks, tools, tests, scripts, skills/*/scripts) instead of one flat dirname, keeping the existing tempdir positive control. Widening needs one BOUND_EXEMPTIONS entry first: ("sources", "MAX_VALUES") is a flagged bound (sets idx.truncated), not a silent cap. Also plant the tempdir control in a SUBDIRECTORY so the guard proves it recurses, otherwise the same blind spot is re-provable.
- **_as_text welds adjacent text blocks with no separator, silently disabling the verbatim escape hatch**
  - where: hooks/rate_prompt.py
  - why: The empty-string join and the total absence of multi-block coverage are both real and reproduced exactly, but the branch is defensive-only (every prompt payload in the repo and in the live UserPromptSubmit contract is a plain string) and the escape hatch only breaks when the sole trigger phrase begins a non-first block, so the demonstrable harm is a latent, narrow, non-silent-gate defect rather than a MEDIUM one.
  - fix: hooks/rate_prompt.py:67 - change `"".join(...)` to `"\n".join(...)`. Pin it in both directions in selftest(): a 2-block list whose ONLY trigger starts block 2 must return LITERAL_INSTRUCTION, and [{'text':'o'},{'text':'k'}] must NOT be swallowed as trivial. Register the '\n'.join -> ''.join reversal as a mutation in tools/mutation_check.py so the new fixtures cannot rot.
- **The fire ledger's `crashed` field can be deleted from both dispatchers with the entire suite green, and nothing in the repo ever reads it**
  - where: hooks/stop_dispatcher.py
  - why: Confirmed by demonstration: the entire ledger-side of the C1 fix is unpinned — deleting the `crashed` key from either dispatcher (and even `results` alongside it) leaves all three CI gates green, because every existing assertion inspects the in-memory dict returned by `run_hooks` or only the ledger's `fired` list, never the crash data that actually lands in the file.
  - fix: In each dispatcher's selftest, drive the crashing `_Boom` hook through run_hooks + write_ledger, then re-read the file and assert the parsed record's `crashed["boom"]` and `results["boom"] == CRASH_RC` — assert the LEDGER LINE, not just run_hooks' return value. Add mutation entries deleting the `"crashed"` and `"results"` ledger keys so the new assertions are themselves pinned. Optional: keep `crashes.clear()` honest by asserting a clean run after a crashing run reports `crashed == {}`.

## Refuted, with reasons

- hooks/capped_report.py:51-65: render() has no boundary contract: a generator input raises TypeError that every hook swallows into a silent exit 0, and a caller total below len(findings) suppresses the notice while printing more bullets than the claimed total -- All six edge behaviors reproduce in isolation, but none is reachable from any live call site — every caller passes a materialised list, every `total` is a sum of per-file totals provably >= len(findings), and every `limit` is a hardcoded positive constant with no env override — so this is an optional hardening of a helper's contract, not a defect in shipped code.
- C:/Users/ammar/Downloads/unbluff/tools/mutation_check.py: The finding id "D8" names two different plan findings, and the duplicate-id guard is keyed so it cannot see cross-unit label reuse -- The label collision is real, but no claimed failure is reachable: the report prints `[unit #finding]` so both D8 lines are self-distinguishing, the CLI filter keys on units (both entries reachable, both CAUGHT), no bucket or denominator collapses, and the plan's D8 (line 1023, pre_push_gate) resolves to exactly one table entry - so the guard's own stated failure modes ("report ambiguous", "CLI filter unable to name one of them") only occur for the same-unit case it does catch.
- C:/Users/ammar/Downloads/unbluff/tools/check_readme_fresh.py: check_readme_fresh's zero-denominator assertion can never fire -- The branch is indeed unreachable, but the harm it is accused of hiding cannot occur — a collapsed denominator makes this two-sided equality gate fail LOUDLY (verified exit 1), and the hook-half floor assertion the reviewer says is missing already exists verbatim in hook_health_check.py:527-529.
- hooks/transcript_util.py: Both containments that stop has_user_media admitting non-user entries are unpinned; together they gate 92 real harness injections -- The two containments are individually unpinned but their harmful COMPOSITION is caught hard — applying X5+X6 together takes the full suite to FAILED (2/22) on the exact `<system-reminder>`-as-block-list fixtures the finding claims would leak, so the "two green edits compose into a silent 92-injection regression" scenario is false.


---

## The FIRST run's findings - nearly lost, recovered by the source-coverage audit

This review ran twice. The first attempt was aborted when an agent stalled, and it was
resumed; the resume re-ran the lenses rather than replaying them, so it produced a SECOND,
independently-worded finding set. The workflow's return carries only the resume's 46.

**The first run's 42 confirmed findings appear in the journal and in NOTHING else.** They
were not in the returned result, not in this document, and not in the plan. A source-
coverage pass over the journal is the only reason they exist here.

That is this repo's own thesis landing on this repo's own review: the run reported a
complete-looking denominator (47/46/1) that was complete *for the resume* and silently
excluded an entire independent pass. The coverage block cannot catch this - it reconciles
within one run. **A resumed workflow needs its runs reconciled across the resume boundary,
and nothing in the harness does that yet.**

### Status: TRIAGE REQUIRED, not merged

Overlap with the 42 findings above is UNQUANTIFIED. Two independent passes over the same
code will agree on much and not all. Before the fix pass, deduplicate these against the
main list; treat anything without a clear twin as an additional finding. Do NOT assume the
sets are identical because the counts happen to match - that assumption is what this whole
session exists to disprove.

Severity spread of this set: MEDIUM 23, LOW 10, HIGH 9

Examples that appear to have NO twin in the main list (spot-checked, not exhaustive):

- `units()` returns 0 of 35 tracked files when the repo path contains glob metacharacters
  (e.g. a directory named `proj[v1]`), so `--release` prints a full all-clear.
- a submodule's `.py` under `skills/*/scripts/` is found by the glob but absent from
  `git ls-files` (no `--recurse-submodules`), so the intersection silently drops it.
- mutation `#10` self-verifies: its entry has no sixth element, so it verifies through the
  unit it mutates rather than the twin it was written to protect.
- `_child()` spawns the selftest module with no `__main__` guard, so it always exits 0 in
  ~0.3s and two checks assert nothing.

### Full first-run set


#### HIGH

- Confirmed on the real module: the `*`-quantified prefix class `[#>*\-\s]*` matches bullets, blockquotes and the empty string, so any of the first 5 lines merely OPENING with "superseded" freezes the whole file, and run() returns (0, '') on an ACTIVE plan holding live PARKED/TODO lines.
  - fix: Accept only MARKED declaration forms: heading `#`, bold `**`, `Status:`/`State:`, or case-sensitive all-caps SUPERSEDED - drop the `-`/`*`/`>`/bare-prose prefixes. Verified pattern (0/7 mismatches, suites + mutations still green): r"^\s*(?:#+\s*\**\s*|\*\*\s*|(?:status|state)\s*[:\-]\s*\**\s*)superseded\b|(?-i:^\s*\**\s*SUPERSEDED\b)" with re.IGNORECASE. Add selftest cases for the word OPENING a line as bullet/blockquote/Title-case sentence, plus a mutation entry that loosens the prefix class back.
- Confirmed and stronger than claimed: `_ALLOW_BRACKET_RE` imposes no tag shape or position, so any bracket pair containing one of the 13 allow-words in ordinary prose suppresses a genuine hiding line, and nothing in the 22-selftest suite or the mutation harness pins the bracket branch at all.
  - fix: Anchor the allow-word to tag position and bound the bracket (verified 22/22 + 30/30): _ALLOW_BRACKET_RE = re.compile(rf"[(\[]\s*(?:{_TAG_ALT})[^)\]]{{0,32}}[)\]]", re.IGNORECASE) Then ARM it: add ("- PARKED: port the (closed-loop) controller", True) to _selftest_line_cases, and a mutant that drops the allow-word from the bracket regex - today that mutant survives the whole suite.
- Confirmed and worse than reported: mutation #10 self-verifies because the entry at mutation_check.py:75 has no sixth element, and the twin layer its comment claims covers it is inert because pre_push_gate_selftest.py:185 launches os.path.abspath(__file__) - an import-only module with no __main__ that exits 0 in 0.3s - so section 14's grandchild-on-pipe assertions can never fail.
  - fix: hooks/pre_push_gate_selftest.py:185 - _child launches os.path.abspath(__file__), an import-only module with no __main__ (exits 0 in 0.3s); use _m.__file__ so sections 13 and 14 actually run the gate. tools/mutation_check.py:75 - add the sixth element "pre_push_gate" so the twin is really verified, and fix the comment at 72-74 which asserts coverage that does not exist. Order matters: fix _child FIRST, or adding verify_unit alone just flips #10 to SURVIVED (verified: with _child fixed, baseline stays OK and #10 goes red).
- Confirmed: `_child()` spawns the selftest module (no `__main__` guard, always rc=0 in ~0.3s), so check 14 and the process half of check 13 assert nothing — mutation #10 SURVIVED `pre_push_gate --selftest` and FAILED it after the one-token `_m.__file__` fix — but it is HIGH not CRITICAL because the twin guard in `fast_test_on_stop --selftest` is live and turns that same mutation red with four failures, so CI does not stay green in the stated scenario.
  - fix: Line 185: `os.path.abspath(__file__)` -> `os.path.abspath(_m.__file__)` (verified: restores detection, control run still green). Add a note under the REBINDING RULE docstring that dunders are the snapshot's blind spot, and pin it with a mutation entry ("_child spawns the selftest module") verified against pre_push_gate. Cheap structural guard: assert `os.path.basename(_m.__file__) == "pre_push_gate.py"` once inside `_child`.
- Reproduced end-to-end: a commented-out dispatch still matches `_DISPATCH_RE` (as does the repo's own verbatim docstring quote of it) and `run_selftests.py` accepts the resulting bare `rc==0` as a pass, printing "usage_snip_prompt: OK ... all 22 selftests passed" with SUITE_RC=0 while that hook's assertions never executed.
  - fix: In run_selftests.py and hook_health_check.py's weekly sweep, capture stdout and require a positive `SELFTEST OK`/`SELFTEST FAILED` marker; rc==0 with no marker must be FAIL, not OK. Replace the text-regex `_DISPATCH_RE` with an `ast` check for a live (non-comment, non-dead-branch) `"--selftest" in argv` compare, and assert the new detector rejects a commented-out dispatch. Add usage_snip_prompt as a mutation verify target so its selftest has at least one mutation that bites it directly.
- slicing_offenders recognises only 2 of 11 genuinely-truncating cap spellings and skips any file with no MAX_* constant outright, so the invariant its own comment states ("a new cap that is neither routed through this module nor listed here is a selftest failure") is already violated by the live, unrouted `problems[:12]` cap in hooks/hook_health_check.py that the sweep reports as clean.
  - fix: Delete `if not caps: continue` and widen matching: int-literal/attribute/arith/islice slice bounds, cmp_node.left as well as .comparators, and `return`/`while`/nested-Break collection caps. Route hooks/hook_health_check.py:470 through capped_report.render(), or add it to BOUND_EXEMPTIONS so the cap is a recorded decision. Replace the two self-template canaries with one fixture per spelling, so the positive control cannot pass by matching only what the detector implements.
- Confirmed: `_max_names` collects only `ast.Assign` -> bare `ast.Name`, so declaring a cap as `MAX_X: int = 12` or `MAX_X, _PAD = 12, 0` makes `slicing_offenders` blind to a byte-identical raw-slice regression in every hook it protects, with the whole suite still green - but the guard is not blind today (all 10 live `MAX_*` caps are plain assigns), so this is a latent bypass, not a present-tense failure.
  - fix: In `_max_names` (capped_report.py:68-76) add `ast.AnnAssign`/`ast.AugAssign`/`ast.NamedExpr` targets and recurse into `ast.Tuple`/`ast.List` targets of `ast.Assign`. Then pin the SHAPES: extend the planted-offender fixture (line 160) with `MAX_B: int = 3` and `MAX_B, _P = 3, 0` files, so narrowing `_max_names` again fails the selftest. Consider widening the name rule to `MAX_*` or `*_MAX` - `memory_hygiene_guard.py:85` already declares `PROJECT_DIR_MAX` and slices with it at line 103, invisible to the guard.
- Both halves reproduce: check 13's `rc_c not in (0, 1)` evaluates False on exactly the traceback its message names (an uncaught exception exits 1, which the set accepts), and deleting only `main()`'s fail-open `except Exception` leaves the hook selftest, all 22 selftests, the integration suite and the mutation harness green.
  - fix: In check 13 fix `_child` to spawn `_m.__file__`, then assert on stderr not just rc: require `rc_c == 0` and `"Traceback" not in err_c` (the corrupt state must be ignored and the tests actually run). Add a direct main() test: monkeypatch `_m.gate` to raise RuntimeError, assert `_m.main([]) == 0` and that stderr contains "the gate crashed". Add a mutation to tools/mutation_check.py deleting main()'s `except Exception` block so the pin is itself mutation-verified.
- Confirmed: main() re-implements the missing-gate rule inline instead of calling missing_gates(), so the A3 regression test and mutation certify a function nothing calls - reverting main()'s own branch leaves --selftest printing SELFTEST OK and a whole gate can vanish while the suite, the gate ledger and readme-fresh all report green - but the shipped code still behaves correctly today, so this is a decorative-test/duplicate-rule defect (HIGH), not a live failure (CRITICAL).
  - fix: In main(), replace the inline check with the shared function: `missing = set(missing_gates(HERE))` before the loop, then `if label in missing: print FAIL; failed.append(label); continue` - one implementation, and the A3 mutation then bites production. Re-point/keep mutation ('./run_selftests','A3') and add a case asserting main() returns 1 when an AUX_GATES file is absent (subprocess a temp tree with one gate removed). Optional durable guard: extend hook_health_check.py's twin-detector idea to flag a second `os.path.exists(os.path.join(...parts))` over AUX_GATES.

#### MEDIUM

- Confirmed end-to-end — an identical repo copy at `proj[v1]/unbluff` makes `units()` return 0 of 35 tracked .py files so `--release` prints a full all-clear and exits 0 — but downgraded from HIGH because Windows forbids `?`/`*` in path names (only `[`/`]` can trigger it) and `run_selftests.py` still exits 1 in that same tree via the git-derived `_scope_failures` check.
  - fix: Line 82 -> `glob.glob(os.path.join(glob.escape(repo), *pattern.split("/")))`; verified to restore 3/35 and exit 1 in the `proj[v1]` copy. Then give units() the module's third state: if `_tracked()` is non-empty but the glob found nothing, that is "could not answer", not "this repo has no Python" — route it to the `unknown` bucket so --release blocks. Pin with a mutation whose fixture repo is built under a directory literally named `br[k]t`; check run_selftests.py's hook sweep and classify_tools for the same unescaped-path pattern.
- The fixture only fails for narrowings that drop one of its seven hand-picked planted basenames, so its docstring claim ("Narrowing UNIT_GLOBS therefore fails here on any machine, in any directory") and the skip message's "the hermetic fixture above is what asserts the logic" are both false — but the ambient denominator check catches any narrowing that actually exempts a tracked file, so no unit can go silently unreviewed in a checkout, which caps this below HIGH.
  - fix: In _selftest_fixture, plant decoys the plausible narrowings would drop (tests/conftest.py, hooks/Zz_helper.py, tools/9x.py) alongside the current seven. Assert the strict _scope_failures() result contains NO "tracked .py file(s) ... never asks" entry at all, instead of only grepping for ghost.py / tools/t.py. Then add the tests/test_*.py and hooks/[a-z]*.py narrowings to MUTATIONS, and reword the docstring + the "ambient scope check SKIPPED" message to claim only what the fixture proves.
- Reproduced verbatim in a scratch repo — a submodule's `.py` under `skills/*/scripts/` is found by the glob, absent from `git ls-files` (no `--recurse-submodules`), silently dropped by `found &= tracked`, and invisible to the `tracked - asked` denominator check, so `--release` printed "4/4 units reviewed" rc 0 and `--selftest` printed "SELFTEST OK" rc 0 over an unreviewed file containing `BACKDOOR = 42` — but this repo has no `.gitmodules`, no gitlink, and no vendoring planned anywhere, so the hole is dormant rather than live.
  - fix: 1. `_tracked()` line 49: add `--recurse-submodules` (verified — still excludes `untracked_scratch.py`, and the submodule file then lands in `unreviewed`, which blocks correctly). 2. Durable class fix: have `units()` REPORT `found - tracked` instead of dropping it silently; a glob hit git will not confirm is an unanswered question, not a pass. 3. Pin it: plant a real gitlink submodule in `_selftest_fixture()` and assert its `.py` is either asked about or named in that drop report.
- Confirmed and demonstrated end-to-end against a sha-verified pristine copy: when `@{u}` resolves to a local branch, `git rev-list --count @{u}..HEAD` succeeds, the remote-aware fallback on line 157 is never reached, and `count_unpushed` measures distance from a local ref that is itself unpushed, restoring the exact P13 B1 silence the docstring claims to have fixed.
  - fix: Trust `@{u}` only when `git rev-parse --symbolic-full-name @{u}` starts with `refs/remotes/`; otherwise fall through to the existing remote branch (line 154+). Simplest uniformly correct form: once any remote exists, always use `rev-list --count HEAD --not --remotes` (it matched truth in every shape tested, including the two-remote and detached-HEAD cases). Then add a fifth shape to `_selftest_unpushed`: `git config branch.autoSetupMerge always` (or `checkout -b s --track main`) with N unpushed commits on the parent, asserting `count_unpushed >= N`, and assert on an exact value rather than `got < 1`.
- Confirmed by reproduction: `main()` calls only `evaluate()`, so `--release` asks the roster question but never the scope/denominator question, and a tracked `.py` outside the one-level `UNIT_GLOBS` is silently excluded while the gate prints "all units have an adversarial review newer than their last change" and exits 0 — downgraded to MEDIUM because `run_selftests.py` AUX_GATES runs `--selftest` and CI (`.github/workflows/selftest.yml:34`) runs that suite, and the live tree has zero escapes (35 tracked `.py`, `units()` = 35/35).
  - fix: In `main()` (line 402), before `evaluate()`: `scope = _scope_failures(REPO, load_ledger())`, print each entry as `SCOPE:`, and add `or scope` to the `--release` guard at line 414. Verified in a scratch copy: escape case flips 0 -> 1 and names all three files; the no-escape control stays 0, so it does not false-block. Third state is already safe (git-unanswerable -> non-strict SKIP here, and `evaluate()` independently blocks via `unknown`). Add a mutation to `tools/mutation_check.py` that deletes the new `main()` call — otherwise the fix is pinned only by `--selftest`, which is the command that already worked.
- The ALL-CAPS half of has_decision_tag searches the whole line, so an incidental all-caps allow-word anywhere in an item's prose suppresses genuine hiding work, contradicting the "IN TAG POSITION" contract on line 84.
  - fix: Replace _ALLOW_CAPS_RE.search(line) at line 85 with an adjacency-anchored regex: the ALL-CAPS tag must abut the marker on either side, separated by [\s:,;()\[\]/|-]* only. Add a fixture pinning the position half, e.g. ("- TODO: handle the CLOSED state in the websocket client", True) - nothing in the suite currently distinguishes the two rules. Update the B3 anchor at tools/mutation_check.py:351 (it is a literal copy of line 85 and will HARNESS ERROR), and add a second mutation that kills position anchoring alone.
- Confirmed: above MAX_FINDINGS_TRACKED the `break` at hooks/numbers_match_on_write.py:432 destroys the real count, so both the header and capped_report.render's "(N total)" print the cap (500) rather than the truth (700), and the BOUND_EXEMPTIONS comment "total reported via M2" is demonstrably false - but this is a MEDIUM, not a HIGH, because the hook still fires exit 2 with a truncation notice and directs the identical remedy, so the failure is an understated magnitude on an extreme input (>500 unmatched cited numbers in one file), not a lost detection or a clean pass.
  - fix: In run(), count every unmatched number (`n_unmatched += 1`) and only `findings.append(...)` while `len(findings) < MAX_FINDINGS_TRACKED` - this keeps the anti-balloon bound and recovers the real total without materialising 700 strings. Thread it through: `build_message(..., findings, n_unmatched)`, header uses that total, and `capped_report.render(findings, MAX_BULLETS, total=real)`. Then correct the BOUND_EXEMPTIONS comment in hooks/capped_report.py:35, and extend the M2 selftest with a >MAX_FINDINGS_TRACKED fixture (the current one uses 40, so it can never reach this branch).
- `_max_names` collects only `ast.Assign` targets, so a cap constant reached any other way — `from constants import MAX_BULLETS`, `MAX_BULLETS: int = 12`, or `constants.MAX_BULLETS` — is invisible to `slicing_offenders`, which then reports the file clean; but the reviewer's stated cause (the `if not caps: continue` early-out) is wrong and the impact is latent, not live.
  - fix: In `_max_names` also collect `MAX_*` names from `ast.ImportFrom` aliases (use `asname or name`) and `ast.AnnAssign` targets; separately match slice/comparator nodes that are `ast.Attribute` with `attr.startswith("MAX_")`, since a qualified `constants.MAX_X` has no Name to look up. Then extend the planted fixtures at capped_report.py:160-165 to cover all four shapes (import+slice, import+break, AnnAssign, attribute) so the guard's liveness proof stops being satisfied by the single shape it already handles.
- Reverting the two DANGLING HOME render() calls at hooks/plan_defer_guard.py:154-155 to bare `claims[:2]`/`promises[:5]` slices silently drops promises with no truncation notice, yet every gate stays green — mutation_check reports SURVIVED/DECORATIVE — because _selftest_dangling's fixtures never exceed the caps and slicing_offenders cannot see integer-literal slices; severity is MEDIUM not HIGH since the shipped code is correct today and this is a missing regression test rather than a live bug.
  - fix: In _selftest_dangling, add a case with 3+ claim lines and 6+ promise lines and assert both notices appear ("not shown", the real totals) plus the correct bullet counts. Register the bare-slice revert of lines 154-155 as a MUTATIONS entry in tools/mutation_check.py so the new assertion is proven to bite. Optionally extend capped_report.slicing_offenders to also flag ast.Constant integer slice bounds on list-typed locals, so literal caps stop being structurally invisible.
- Confirmed: `_CLAIM_RE` matches only "all N selftests passed", so the second pasted transcript in the same "this is exactly what CI runs" block ships a stale `==== 26/26 scenarios passed ====` against a suite that actually runs 30, and readme-fresh prints OK.
  - fix: Add `_SCEN_RE = re.compile(r"==== (\d+)/(\d+) scenarios passed ====")` and gate both numbers, treating an absent match as FAIL like the existing branch; derive the expected total from tests/test_integration.py itself (e.g. a `--count` flag that builds the roster and prints len(results)) so it stays DERIVED, never listed. Also diff the pasted `<name>: OK` tokens against `selftestable_hooks() | AUX_GATES` so a rename/swap fails even when the count matches. Instance fix alongside the general one: update README.md:171 to 30/30.
- Every claim reproduced exactly (1 of 3 entries load-bearing, bogus entries silent), and the dead entries are worse than inert: the MAX_FILE_BYTES exemption pre-authorizes a real collection cap at that exact site, which the selftest then swallows while staying green.
  - fix: In selftest(), verify each (mod, const): fail if hooks/<mod>.py is absent or const not in _max_names(ast.parse(...)) - the direct analogue of classify_tools()'s stale_exempt. Also fail any entry whose removal yields no new offender (dead/over-broad), and print "N exemptions, M load-bearing" so the denominator is visible. Add a mutation_check unit that widens BOUND_EXEMPTIONS, so the new check cannot itself rot.
- The collection-cap arm of `slicing_offenders` demonstrably sees 1 of 7 semantically identical early-exit shapes — a pure operand swap (`MAX_X <= len(hits)`) in a registered hook flips the gate from FAIL to SELFTEST OK with the full 22/22 suite green — but the exposure is narrower than claimed because behavioural twin selftests and the hook-registration gate catch the reviewer's two headline scenarios.
  - fix: In slicing_offenders, check `cmp_node.left` as well as `.comparators`, scan `node.orelse` as well as `node.body`, and add ast.Return to the ast.Break test plus ast.While.test and comprehension `.ifs` as cap sites. Stronger: invert to an allowlist — flag ANY load of a MAX_* name that is not an argument to capped_report.keep/render, minus BOUND_EXEMPTIONS, so unenumerated shapes fail closed. Replace the tautological canary: plant fixtures for shapes the guard does NOT yet match, so the selftest can actually fail.
- Reproduced in an isolated scratch tree: a two-line widening of the `BOUND_EXEMPTIONS` membership test blinds `slicing_offenders` to real offenders while capped_report --selftest, run_selftests (22/22) and all three capped_report mutations stay green, because nothing anywhere pins the (module, constant) pair.
  - fix: In hooks/capped_report.py selftest, plant a third fixture `planted3.py` with `MAX_FINDINGS_TRACKED = 3` + `xs[:MAX_FINDINGS_TRACKED]` and assert it IS flagged - an exempted name under a non-exempt module must not be exempt (verified: clean rc=0, widened rc=1). Then add the widening as mutation B9 in tools/mutation_check.py so the ignore-branch is pinned like the report-branch. Same lens applies to any other exemption/allowlist keyed on a tuple: test that every component of the key is load-bearing, not just that the guard fires at all.
- BOUND_EXEMPTIONS is keyed only by (module, name), so exempting one bound blinds the guard to every slice/break use of that identifier anywhere in the file - confirmed by planting sources[:MAX_FINDINGS_TRACKED] in build_message (22/22 selftests green, integration unchanged from baseline) while the identical truncation via non-exempt MAX_BULLETS is caught instantly.
  - fix: Key exemptions by (module, name, lineno) - or (module, name, shape) - so one bound cannot license the identifier file-wide; update the two call sites at capped_report.py:108/118. Delete the inert MAX_FILE_BYTES and MAX_SOURCE_VALUES entries: they shield nothing and only widen the blind spot. Add a rotting-exemption assert to selftest() - reuse the existing NOT_A_GATE pattern - failing if any BOUND_EXEMPTIONS entry matches no offender when removed.
- Both SKIPPED strings contain "only", so the `skipped` bucket and its `CI must not skip: failing.` hard fail are provably dead, and the CI verdict is decided by comment prose rather than the `posix_only` value already in scope - deleting the word "only" from line 402 alone flipped a scratch run from `0 skipped`/exit 0 to `1 skipped`/exit 1 with the identical marker, mutation and platform.
  - fix: Line 521: `(other_platform if posix_only else skipped).append((hook, finding))` - classify on the marker in hand, not on verdict prose. Add a MUTATIONS entry for tools/mutation_check that reverts that line (verify unit = itself or a wrapper), so this dispatch is no longer the one uncovered line in the harness. Add a gate asserting .github/workflows/selftest.yml still defines both `mutations:` and `mutations-windows:`, since the "proven by the OTHER platform's job" claim is currently unverified.
- Confirmed: the harness validates anchor presence but never uniqueness, and a static sweep of all 81 entries finds transcript_util #13's anchor already matching twice, so a pure reordering of hooks/transcript_util.py (all selftests still green) retargets the edit to has_tool_result and the harness falsely reports finding 13's test as DECORATIVE - but the demonstrated failure is a loud red exit 1, not the silent false-CAUGHT the reviewer also claims, so HIGH over-rates it.
  - fix: In run() require uniqueness, not presence: `n = live_text.count(find)` (and the same on the scratch text after prior edits) -> return "HARNESS ERROR: anchor matches %d times" unless n == 1. Disambiguate transcript_util #13 by extending its anchor one line to `...block.get("type") == "text":` - verified count==1 and still CAUGHT (rc=1). The other 80 anchors already count==1, so the new check passes the existing table unchanged.
- Confirmed by execution: `executed` at line 530 never subtracts `errors`, and every HARNESS ERROR return in `run()` fires before the mutated selftest is ever launched, so the headline number counts mutations that provably did not run.
  - fix: Line 530: `executed = considered - len(skipped) - len(other_platform) - len(unproven) - len(errors)`. Also add `, %d harness-errors` as its own field in the line 533 summary so the count is visible in the number, not only in the block below. Verified on a scratch copy: the one-line change turns the false "3 of 3" into the correct "2 of 3" with the error block unchanged.
- Reproduced exactly: swapping line 108 to `any(results.values())` leaves 22/22 selftests and the full integration fail-set unchanged while flipping `main()` from 0 to 2 when all hooks crash, so the truthy CRASH_RC=-1 sentinel's interaction with the exit-code decision is pinned by nothing in either dispatcher — but since today's behaviour is verifiably correct, this is an unpinned contract, not a live bug.
  - fix: Add to each dispatcher's selftest: stub every HOOKS module to raise, feed a JSON payload via sys.stdin, assert main() == 0 — the one assert that fails under the truthiness swap. Better, kill the class: derive one `fired(results) = [k for k, rc in results.items() if rc == 2]` helper used by BOTH main() and write_ledger, so the exit code and the ledger's `fired` list can never disagree. Register the line-108/107 swap as a mutation entry for stop_dispatcher AND post_tooluse_dispatcher (both are unpinned).
- Confirmed and slightly under-rated: `run()` treats every non-zero, non-77 rc as CAUGHT while discarding the output, so for 2 of the 79 executed mutations the verifying selftest dies on an uncaught traceback at the call line before any assertion about the mutated behaviour is evaluated, and deleting those assertions outright still yields CAUGHT - the harness cannot detect a disarmed test in exactly the case it exists to detect.
  - fix: Keep `p.stdout + p.stderr` and split the red: require an explicit failure marker (`SELFTEST FAIL:` / `SELFTEST FAILED` / `FAIL:`) for CAUGHT. A non-zero rc whose output shows only `Traceback (most recent call last):` is a new bucket - `CRASHED (rc=%s) - died before asserting` - counted like UNPROVEN, never as caught, with the output tail printed. Then fix the two it exposes: guard `verdict()`'s success message (or assert via a return-value check before the crash point) and move rate_prompt's non-str block call above line 125's bare call.
- The twin-guard's own stated contract is "exactly ONE implementation exists," but it enforces only three hardcoded name patterns, so a complete divergent second classifier that avoids those three names passes every gate green.
  - fix: Derive the watch set instead of listing it: build the roster from transcript_util's own public names at runtime (dir()/AST of this module) so anything added later auto-enrols. Add a structural check as the real backstop: flag any hooks/*.py that references entry keys ('isMeta','sourceToolUseID','tool_result','origin') or block types without importing transcript_util. Then fix the live instance: delete show_your_proof.get_content (:145-153) and call transcript_util.get_content.
- The mutation-coverage gap is real and reproducible - gutting all four never-mutated aux gates to `raise SystemExit(0)` leaves run_selftests at "all 22 selftests passed" and test_integration at 30/30 - but the reviewer's headline consequence is wrong, so this is a coverage/denominator gap rather than a HIGH-severity blind gate.
  - fix: Add one mutation per uncovered check to MUTATIONS (e.g. mutate check_skill_deps `if dep not in shipped` -> `if False`, verify_unit=itself) - which first requires giving audit.py/check_python_floor/check_skill_deps/regen_example_settings a `--selftest`. Then make the gap self-reporting: have mutation_check print "N of 22 run_selftests checks have >=1 mutation" and name the uncovered ones, deriving the 22 by importing run_selftests.AUX_GATES + selftestable_hooks rather than a second roster.
- Confirmed by mutation: the `crashed` ledger key and `crashes.clear()` can each be deleted from both dispatchers with 22/22 selftests, 30/30 integration and the mutation harness all green, so the half of the C1 fix that its own comment calls the point (the LEDGER) is unpinned — though the reviewer overstates, since `results=-1` still answers "which hook raised" and IS pinned.
  - fix: In `_selftest_crash_is_not_clean` (both dispatchers): set UNBLUFF_LEDGER_PATH to a temp file, call `write_ledger` after the boom run and assert `rec["crashed"] == {"boom": "RuntimeError: hook exploded"}`; then run a CLEAN hook, write again, assert `rec["crashed"] == {}` (this pins `crashes.clear()`). Add three mutants to tools/mutation_check.py alongside C1/C1b: delete the `"crashed": dict(sorted(...))` line, delete `crashes.clear()`, and drop the `isinstance(rc, int)` guard. Optional: have the dispatcher also write the crash to stderr, so a raising hook is not visible only in an unread file.
- Confirmed: tools/mutation_check.py has no --selftest, is exempted by name in run_selftests.NOT_A_GATE, and appears in none of its own 81 MUTATIONS entries, so a disarmed run() still prints "all mutations caught" while every gate stays green - but this is a latent verification gap, not a live failure, so MEDIUM rather than HIGH.
  - fix: Give mutation_check.py a `--selftest` that builds a fixture tree with one biting and one decorative selftest and asserts run() returns CAUGHT and SURVIVED respectively. Then move "mutation_check.py" out of run_selftests.NOT_A_GATE into AUX_GATES as ("mutation-harness-bites", ("tools","mutation_check.py"), ("--selftest",)). Add a ("tools/mutation_check", ...) MUTATIONS entry that guts the `if rc == 0: return "SURVIVED"` branch, verified by that new selftest.

#### LOW

- Reproduced exactly - `record()` prints unqualified success for a non-canonical unit that `--release` then calls UNREVIEWED - but it fails CLOSED (no false all-clear is reachable, since `units()` emits only canonical keys) and the `_scope_failures` orphan check is a mandatory `AUX_GATES` entry that fails the whole suite and names the bad path, so the harm is a misleading message costing one cycle, not a silent gate bypass.
  - fix: In `record()` (after the strip/backslash normalisation, ~line 214), intersect each unit with `units(REPO)`; on a miss print the offending path plus the nearest canonical match and return non-zero rather than "recorded". Reuse the `_scope_failures` orphan wording verbatim so the record-time and selftest-time messages cannot drift apart. Pin it with a fixture case recording `./hooks/h.py` and asserting a non-zero exit, or the guard is itself unfalsifiable.
- Confirmed and stronger than claimed: `"unpushed" not in msg` (line 537) is false for EVERY possible input because build_message's static header is `[meta-audit] parked/unpushed state at stop:`, so the assertion is unconditionally dead rather than merely fixture-dead, and the reviewer's proposed fix (more plan files) does not repair it.
  - fix: Line 537: match the bullet, not the bare word - `if "commit(s) unpushed" not in msg:` (verified: fires on a dropped bullet, stays green on clean code). Also spread the overflow across >=2 plan files so len(findings) > MAX_BULLET_LINES, and assert that precondition in-block. Line 540's "not shown" check is powered by the per-file cap, not the bullet cap - correct its comment or drop it.
- A future ledger stamp does make the gate report a committed backdoor as FRESH with exit 0, since nothing compares the recorded stamp against now, but no caller anywhere passes --utc, ordinary clock skew only blinds the gate for the skew's duration before self-healing, and the dirty-tree branch still catches uncommitted work.
  - fix: In evaluate(), before the `changed > reviewed_at` compare at line 201, add a branch bucketing `reviewed_at > datetime.datetime.now(datetime.timezone.utc)` as `unknown` ("ledger stamp is in the future - clock skew or a bad --utc"). In record(), reject a parsed args.utc later than now with a nonzero exit rather than writing it. Pin both in _selftest_third_state() so a future-stamped entry can never land in `fresh`.
- Confirmed by scratch repro: a tracked .py absent from the worktree stays in `git ls-files` but drops out of `units()`, so `--selftest` exits 1 naming a file the reader cannot open, and the orphan half is worse than reported - it survives `git rm` and the commit, reddening the suite permanently until the audit ledger is hand-pruned.
  - fix: In `_scope_failures`, split `missing` into worktree-present vs index-only and report the latter as "tracked but missing from the worktree - commit the deletion" (still a failure, but naming the real state). Scope the orphan check (line 256) to units git has never tracked, so deleting a reviewed file does not redden the suite forever. Do NOT filter worktree-absent paths out of `_tracked()` - that would make a shrinking denominator silent, the failure class this gate exists for.
- The line 34-38 comment credits a `_selftest_scope` function that exists nowhere in the repo, and no code path prints EXEMPT or reports an EXEMPT entry naming a file that does not exist, so the "cannot grow in silence" promise is unbacked - though EXEMPT is empty and currently has zero suppression surface, making this a latent claim defect rather than a live gate failure.
  - fix: In `_scope_failures`, after `tracked = _tracked(repo)` resolves non-None, add the twin of `classify_tools`'s `stale_exempt`: `rot = sorted(e for e in EXEMPT if e not in tracked)` and append a "the exemption is rotting" failure when non-empty. Also flag no-op entries (`e in asked`), and print `sorted(EXEMPT)` from `selftest()` so the list is visible per the comment's own promise. If the guard is not wanted, delete the false sentence naming `_selftest_scope` instead of leaving it as cover.
- A filter name matching zero of the 81 entries runs no mutations and still exits 0, the identical exit status a real all-caught run produces, so the machine-readable contract cannot distinguish "pinned" from "asserted nothing".
  - fix: In main(), right after `considered = len(MUTATIONS) - filtered` (line ~528), add: `if args.only and considered == 0: print("HARNESS ERROR: filter %r matched none of the %d entries - nothing was asserted" % (args.only, len(MUTATIONS))); return 1` Verified in a scratch copy: bogus/hyphenated names exit 1, `show_your_proof` still exits 0.
- Confirmed and reproduced verbatim - the unguarded scratch read at line 437 does abort the whole run with FileNotFoundError before any summary when a mutation unit lives outside COPY_TREES, and COPY_TREES genuinely omits scripts/ while check_review_freshness.units() counts scripts/make_demos.py among its 35 reviewable units - but it is latent (zero current entries resolve outside COPY_TREES/COPY_FILES), it fails loudly with the scratch tempdir path in the traceback rather than certifying a false all-green, and it needs the twin-verify form since a scripts/ entry with no verify= instead returns a clean non-fatal HARNESS ERROR.
  - fix: Add "scripts" to COPY_TREES and wrap line 437's read in the same try/except OSError -> "HARNESS ERROR: cannot read" used at 388-392. Durably: derive COPY_TREES from check_review_freshness.UNIT_GLOBS so the two definitions of a unit cannot drift again. Add a pre-flight check in main() (beside duplicate_ids) that fails fast naming any MUTATIONS unit/verify that will not exist in the scratch tree.
- The two mutations do individually survive the full suite, but the HIGH rating rests on a composition that is loudly caught — X5+X6 together fails 2/22 selftests (rc=1), so the 92-injection state is unreachable with a green suite, leaving only X6 as a real surviving mutation worth 1 misclassified entry in 2033.
  - fix: Add two fixtures to transcript_util.selftest(), which I verified kill each surviving mutation: check(user([{"type":"image","source":{}},{"type":"text","text":"<system-reminder>x</system-reminder>"}]), False, "media must not outrank the synthetic filter") # kills the hoist check(user([{"type":"thinking","thinking":"..."}]), False, "a non-media block list is not a user prompt") # kills the block-type deletion; optionally register both in tools/mutation_check.py
- Both halves reproduce against the live module — `""` welding does flip a multi-block verbatim prompt from LITERAL to RATE, and nothing in the repo pins the separator — but the harmful path needs an upstream payload shape change that has not happened, so this is an unpinned defensive branch rather than a live escape-hatch failure.
  - fix: hooks/rate_prompt.py:67 — join with a separator: `return "\n".join(...)` (verified: fixes both directions, leaves all existing fixtures unchanged). Add a TWO-block selftest fixture in both directions next to :125 — one where the verbatim phrase starts the 2nd block (must be LITERAL), one where fusing two blocks would forge a trivial ack (must not be SILENT). Add a mutation_check entry pinning the separator (`"\n".join` -> `"".join`) so the join can never silently revert.
- Confirmed and reproduced: `slicing_offenders` is called only with `hooks/` and globs non-recursively, so a `MAX_*` cap in `tools/` or a `hooks/` subpackage passes the whole 22-gate suite green, but the defect is latent (zero live offenders anywhere in the repo today) and the fix is a one-line scope change.
  - fix: Pass the repo root, not hooks/: walk every dir the repo already treats as a unit (reuse check_review_freshness.UNIT_GLOBS - hooks/, tools/, tests/, scripts/, root) recursively via os.walk, skipping __pycache__. Key BOUND_EXEMPTIONS and the capped_report self-skip on the repo-relative PATH, not the bare basename, so tools/capped_report.py is not auto-skipped and same-named modules in different dirs cannot share an exemption. Extend the planted-offender positive control to put one file in a SUBDIRECTORY of the tempdir, so the recursion itself is pinned and cannot silently regress to a flat glob.
