{
  "_note": "The 800-line offenders, DERIVED by walking the tree - not the hand-maintained list the plan carried, which was missing tools/no_regression.py at 805 lines. RATCHET: a new offender or a recorded one GROWING fails; shrinking is reported so this file can be re-recorded tighter. Every entry is DEBT and can only go down.",
  "_accepted_growth": "2026-08-14: mutation_check.py re-recorded 1377 -> 1399. The growth is new mutation ENTRIES (SB-IO, FS-1, FS-2 - the pins for the day's new gates), i.e. coverage, not bloat. RE-RECORDING IS THE LOOPHOLE IN THIS DESIGN, so it is stated here rather than done quietly: this file is now the TOP split candidate, and the next growth should be preceded by the split, not by another re-record.",
  "_split_2026_08_16": "The 08-14 instruction above was HONOURED rather than repeated. Adding the MODE-1/MODE-2 pins took mutation_check.py 1399 -> 1415, so the split came first: the MUTATIONS table moved to tools/mutation_entries_a.py and tools/mutation_entries_b.py. AT THE SPLIT the three files measured 377 / 541 / 538 and all 200 entries and 201 anchors were preserved - the cut is at an entry boundary and order is unchanged, so the sweep iterates an identical list. THOSE ARE AN INSTANT, NOT A CURRENT FACT: later work the same session added the sys.path re-export fix and eleven more entries, so the sizes are now 387 / 541 / 604 across 211 entries and 212 anchors. The session's own consistency audit caught the undated versions of these numbers going stale within hours, which is the drift class this repo exists to catch. tools/mutation_check.py is REMOVED from over_limit below: it is no longer over the limit, and the ratchet only turns one way.",
  "_reverse_walk_gap": "Observed 2026-08-16 while doing the above, and it is the 08-16 review's finding #19: when mutation_check.py fell off this list the gate printed '7 recorded offender(s)' and listed 6, with no SHRANK line, because verdict() only inspects files that are CURRENTLY over the limit and never walks the baseline in reverse. A stale baseline entry is therefore invisible. Scheduled in the plan as task #12; this note exists so the discrepancy is not mistaken for a miscount.",
  "_accepted_growth_2026_08_18": "pre_push_gate_selftest.py re-recorded 1109 -> 1131. The growth is scenario 5a, which asserts the new .claude/pre-push-always.cmd hook point from INSIDE the fast-path state scenario 5 establishes - i.e. coverage of a real hole, not bloat: a check whose input is not the source (a ledger, a staleness comparison) was previously cached away by the 'no source touched since' short-circuit, and `git commit` touches no mtime at all. Measured in ghg-copilot on that exact incident path. STATED, NOT DONE QUIETLY, per the 08-14 note above. THE 08-14 INSTRUCTION NOW APPLIES TO THIS FILE: it is the top split candidate at 1131 lines, and the next growth should be preceded by the split (the natural seam is the four _selftest_* helpers, which are already module-level functions, versus the numbered scenario walk in selftest()).",
  "recorded_utc": "2026-08-25",
  "limit": 800,
  "over_limit": {
    "hooks/duplicate_registration_check.py": 858,
    "hooks/fast_test_on_stop.py": 851,
    "hooks/fast_test_on_stop_selftest.py": 1026,
    "hooks/pre_push_gate_selftest.py": 1213
  },
  "_split_2026_08_26": "run_selftests.py REMOVED from over_limit: item 7 landed and it is 655 lines, under the 800 limit outright, so the ratchet's own rule removes the entry rather than re-recording it lower. The 08-14 / 08-18 / 08-23 / 08-25 instruction - that the next growth be preceded by a SPLIT, not by a fifth deliberate re-record - was honoured, and it was honoured because it BIT exactly where the 08-25 note predicted: item 15 split tools/hook_divergence_report.py (925 -> 667 plus a 321-line sibling), the new sibling had to be named in NOT_A_GATE, and there were not enough lines left to write it. selftest() moved to run_selftests_selftest.py following the install / install_selftest precedent, with the same REBINDING RULE docstring and a scripted check for parent-global rebinds in the moved body (none found). THE SEAM WAS MEASURED, NOT CHOSEN: ZERO pinned mutation anchors fall inside selftest(), checked against both entry tables BEFORE cutting; AUX_GATES does NOT move, because tools/mutation_check.py does not import it but ast.literal_evals it out of this file's SOURCE TEXT, so moving that table would break the harness without breaking an import; and main() does not move, because tools/check_selftest_isolation.py asserts a scrub in its DIRECT body. The import list the moved body needs was DERIVED from its own AST rather than hand-listed. THOSE ARE AN INSTANT, NOT A CURRENT FACT. hooks/pre_push_gate_selftest.py remains the largest file in the repo and the TOP split candidate, with the _SH_SITES rebinding trap still unresolved.",
  "_split_2026_08_24": "install.py REMOVED from over_limit: the #46-adjacent _import_closure fix and its pins took it 927 -> 1006, and the 08-14 / 08-18 / 08-23 instruction above - that the next growth be preceded by a SPLIT, not by another re-record - was honoured rather than repeated for a fourth time. selftest() moved to install_selftest.py following the pre_push_gate / pre_push_gate_selftest precedent, with the same REBINDING RULE docstring; a scripted check for parent-global rebinds inside the moved body returned none. AT THE SPLIT the two files measured 682 and 358, and install.py is under the 800 limit outright, so the ratchet's own rule removes it rather than re-recording it lower. THAT IS AN INSTANT, NOT A CURRENT FACT. The gate's reverse walk - task #12, the _reverse_walk_gap noted above - is what surfaced the now-stale entry, so that gap is closed in practice. hooks/pre_push_gate_selftest.py did NOT grow: the same session's CI-blocker fix to scenario 15b was written to land at exactly 1192, by replacing an 18-line _pytest_importable stub with a 3-line fixture change that removes the environment dependence instead of masking it. It remains the largest file in the repo and the TOP split candidate; the natural seam is still the module-level _selftest_* helpers versus the numbered scenario walk, and it was NOT attempted here because _SH_SITES is mutable module state shared between _sh_site() and selftest() - moving one without the other is precisely the rebinding trap this repo's own split docstring warns about, and doing it under release pressure in the file that carries #46 is a bad trade.",
  "_merge_2026_08_23": "Conflict resolution when main (b6cc6cc) merged into feat/enforcing-verify. The two sides disagreed on TWO entries and the resolution was MEASURED, not negotiated: pre_push_gate_selftest.py takes main's 1131 because main is the side that grew it (the branch never touched the file, so its 1109 was simply stale); tools/no_regression.py LEAVES the baseline because the branch shrank it 805 -> 719, which is under the 800 limit, and the ratchet's own rule is that an entry dropping below the limit vanishes. Both numbers were re-derived by walking the merged tree, so this file is not the union of two claims - it is a fresh measurement that happens to agree with the union.",
  "_accepted_growth_2026_08_23": "fast_test_on_stop.py 832 -> 851 and pre_push_gate_selftest.py 1131 -> 1192, both for gate 2 (#25, the execution model). RE-RECORDING IS THE LOOPHOLE IN THIS DESIGN, so the reason is stated rather than taken quietly. The 19 lines in the hook are ONE import and one guarded call plus the comment explaining why the import must NOT be wrapped in try/except ImportError - install.py's _import_closure treats such an import as optional and drops the module from the install roster, MEASURED: roster 26 unguarded vs 25 guarded, so the guarded form would have disabled the disclosure on every installed machine in silence. The 61 lines in the selftest are three new scenarios (15b, 15c, and the widened 15) - coverage, not bloat, and 15c exists only because a surviving mutation proved 15b did not cover it. THE ALTERNATIVE WAS TRIED AND REJECTED: moving detect() into the new sibling module would have shrunk the hook instead, but it breaks pinned mutation FTB-2, whose anchor is a line of detect()'s body. Trading a pinned mutation for 19 lines is a bad trade. pre_push_gate_selftest.py is now the largest file in the repo and is the TOP split candidate; the next growth should be preceded by that split, not by a third re-record.",
  "_accepted_growth_2026_08_25": "Three entries move, all for #46 item 3/4 - keeping a selftest's git fixtures off the real repository. (1) hooks/fast_test_on_stop_selftest.py 1003 -> 1026 and (2) hooks/pre_push_gate_selftest.py 1192 -> 1213: each gains an import-time scrub. It goes in these modules rather than their parents because fast_test_on_stop.py sits at 851 against an 851 baseline with ZERO headroom and pre_push_gate.py had 8 lines before becoming a new offender - so the growth was placed where it could be recorded deliberately instead of silently breaking a tighter constraint. (3) run_selftests.py 794 -> 803 becomes a NEW offender: registering tools/check_selftest_isolation.py needs two AUX_GATES rows, a SELFTEST_IS_THE_GATE adjudication and the comment saying why. Comments were trimmed twice trying to stay under; the third trim would have deleted the reasoning, which is the wrong trade. Recorded rather than shaved, and the split is scheduled as item 7 - the orchestrator having only 6 lines of headroom is the actual finding here, and it will bite the next person who adds a gate."
}
