# Task #17 - adversarial sweep of the never-examined files (2026-08-19)

**GENERATED from the run's own result object** (workflow `wf_7b752d72-92e`), not transcribed -
every count and every quoted claim below came out of the JSON that run returned, because
hand-copying findings into prose is how a canonical file drifts from its data. The generator is
deliberately NOT committed: it reads a session-scoped path under `%TEMP%` that will not exist
later, and a pointer that cannot be followed is worse than no pointer. The per-agent record is
`subagents/workflows/wf_7b752d72-92e/journal.jsonl` under the session directory.

## Scope and denominator

| | |
|---|---|
| Files swept | **33** - every `.py` unit the review ledger records as NEVER examined |
| Batches | 11, size-balanced, all files read in full |
| Agents | 72 (11 sweepers + 61 refuters), 0 errored, 0 empty |
| Findings produced | 61 |
| Adjudicated | 61 - **0 unadjudicated** |
| Refuted | 46 |
| **Confirmed** | **15** (3 HIGH, 11 MEDIUM, 1 LOW) |

The population was derived with `python tools/check_review_freshness.py`, not copied from the
plan: 55 tracked units, 32 never examined, plus `tools/gate_modes.py` created the same day by
the same author and examined by nobody either - 33. The plan's figure of 51 was a different
population (see the erratum in `source_coverage_2026-08-16.md`).

## Why this surface

Per tooling-discipline 7.1, and now measured on this repo rather than quoted. A fan-out over a
commit written an hour earlier returned 4 confirmed for 4.6M tokens (0.87/M). This fan-out, over
state nobody had ever examined, returned 15 for 8.5M (1.76/M) - twice the yield, and the findings
are structurally deeper: gates that CANNOT FAIL rather than edge cases. Most of these files are
the instruments themselves - the gate tooling, the corpora that grade the guards, the audit
skill's own extractor - while the product they measure has been reviewed repeatedly.

## Confirmed findings

Ordered by severity. Each survived a refuter instructed to kill it and to default to *refuted*
when uncertain; 46 of 61 did not survive that.

### 1. [HIGH] The no-regression gate's only covered unit is a delegate, so its A/B compares the working tree against itself

- **Where:** `tests/noregress_registry.py:36`
- **Class:** check-cannot-fail / C6 declared-vs-derived
- **Claim:** REGISTRY registers hooks/capped_report.py as the one covered unit, but capped_report.py holds no detector: its only cap_detector-family entrypoint, slicing_offenders (capped_report.py:71-91), is `return cap_shapes.slicing_offenders(hooks_dir)`. capped_report.py:30-33 bootstraps `sys.path.insert(0, dirname(__file__))` then `import cap_shapes`. tools/no_regression.py:331 loads the working-tree copy first (populating sys.modules['cap_shapes'] from <repo>/hooks and leaving <repo>/hooks on sys.path), and :332 loads the predecessor blob from a temp file - whose `import cap_shapes` therefore resolves to the SAME working-tree module. I verified with `git cat-file -p 2656f045...` that the predecessor blob (the first differing committed blob, commit f77eefd) is itself a delegate carrying `import cap_shapes` at line 33 and `def slicing_offenders` at line 71. Both probes therefore execute byte-identical detection code, so prev_hits == cur_hits, `lost` is provably always [], and `total_loss` is provably always False. The gate registered as the enforcing AUX_GATE 'no-regression' (run_selftests.py:110) cannot report a regression for the only unit it covers.
- **Failure scenario:** Rewrite hooks/cap_shapes.py so slicing_offenders misses 40 of the 103 positives in tests/cap_spelling_corpus.py - exactly the incident tools/no_regression.py's docstring says the gate exists to block ('a rewrite of hooks/capped_report.py was blind to 10 of 14 cap spellings its own predecessor caught'). Run `python tools/no_regression.py`: it prints `-- hooks/capped_report.py: predecessor 2656f045 saw 63 of 103, working tree sees 63` and `no-regression: OK`, exit 0. Nothing else catches it: hooks/cap_shapes.py IS a derived unit (it contains '--selftest' and defines selftest() at cap_shapes.py:639) but is absent from REGISTRY, so it is only printed in the non-blocking UNCOVERED list (no_regression.py:521-528). Effective covered-unit count is 0 while the coverage line reports 1. A total-blindness rewrite would trip `prev_score == 0 -> Broken`, but a PARTIAL regression - the measured, real case - is silent.
- **Suggested fix (a hypothesis, not a prescription - verify before applying):** Register hooks/cap_shapes.py (the file that actually holds the detector) as the unit, and/or make load_version() isolate sibling imports for the predecessor: snapshot and restore sys.path and sys.modules around each load, and resolve a delegate's siblings from the predecessor's own commit rather than the working tree. Independently, add an assertion in compare() that the prev and cur probe targets are not the same code object / not sourced from the same file, so a self-comparison raises Broken instead of returning a green verdict. The ENTRY SHAPE docstring should also gain an `entrypoint` field so the probed function is declared rather than max-scored (hooks/capped_report.py:73-75 already claims the registry declares an entrypoint - it declares only the family 'cap_detector').

### 2. [HIGH] STALE waiver detection is a tautology: _detected_now() returns only `gained`, so a capability BOTH versions detect is filed as SETTLED and never blocks

- **Where:** `tools/no_regression.py:428`
- **Class:** check that cannot fail / two spellings of one rule that are the same statement twice
- **Claim:** Line 420 reads `elif cap in res.get("gained", []) or cap in _detected_now(res):` and line 431 is `return set(res.get("gained", []))`. The second disjunct is byte-for-byte the same test as the first, so the STALE branch is edge-triggered on `gained` alone. Its own docstring (lines 429-430) describes a different, level-triggered set - "everything the predecessor saw minus what was lost, plus anything gained" - which is exactly the both-sides-detect case the body drops. That set cannot even be computed from `res`: compare() returns `prev_detected`/`cur_detected` as COUNTS (lines 371) and only the `lost`/`gained` differences as sets, so `prev_hits` never leaves compare(). Consequence: the only waiver state the ledger declares BLOCKING for rot (tests/noregress_waivers.py:20-23, "a ledger nobody prunes is how an exemption roster rots into pre-authorisation") fires on a single transition and is silent forever after. A capability that is live in the working tree AND live in the predecessor falls through to `settled` (line 424) - non-blocking, and printed only as a standing reason.
- **Failure scenario:** Two commits are pushed together. Commit A restores detection of `scalar_slice_without_a_roster_entry` in hooks/capped_report.py (the capability the one live waiver excuses); commit B then refactors the same file. At push time the gate compares the working tree (B, detects it) against `predecessor()` = the newest DIFFERING committed blob = A (also detects it). The capability is therefore in neither `lost` nor `gained`: `active` is empty, the STALE branch is unreachable, and classify_waivers files it in `settled`. run() prints `no-regression: OK` and exits 0 while a waiver on record says "detecting it was WRONG" for a shape the guard is once again flagging. The same happens to any single-commit restoration whose gate run was skipped once (a `--no-verify` push, a shallow checkout where the unit is UNDETERMINED, or a run where compare() raised Broken): after that one missed run the predecessor also detects it and the alarm can never re-arm. Nothing catches it - selftest() covers A-F (derive, identical-blob walk, narrowing caught, unusable yardstick, total loss, shallow history) with no STALE assertion, and the four no_regression mutations in tools/mutation_entries_b.py (D2a, D2b, CS1, CS2) pin the predecessor walk, the zero-yardstick guard and the shallow-history split - none touches classify_waivers' STALE arm.
- **Suggested fix (a hypothesis, not a prescription - verify before applying):** Have compare() return the actual `prev_hits`/`cur_hits` sets (it already computes them at lines 362-363), then implement _detected_now(res) as `set(res["cur_hits"])` and drop the duplicated first disjunct at line 420. Add a selftest assertion G: a fixture where prev and cur BOTH detect a waived capability must be reported STALE and BLOCKING, plus a mutation entry that deletes the STALE arm.

### 3. [HIGH] set() and frozenset() are listed as clause-2 aggregators, so `return set(rows[:MAX])` - a truncated collection handed straight to the caller - is reported CLEAN

- **Where:** `hooks/cap_types.py:45`
- **Class:** declared vocabulary roster contradicts the rule it implements; shipped false negative with zero corpus and zero selftest coverage
- **Claim:** _AGGREGATORS (cap_types.py:45) contains "set" and "frozenset". Its own docstring at cap_types.py:41-44 states the rule as: "A value consumed by one of these never reaches the user as a list - it becomes a bool, a number or a branch. Shortening it hides nothing, because nothing was going to be reported." set() and frozenset() violate that premise - they return a COLLECTION that is reported. _aggregated_nodes() marks every argument of such a call, and _module_sites (cap_shapes.py:469-471) then drops the site under clause 2. Verified with one-token controls: `def r(rows): return set(rows[:MAX])` -> CLEAN; `frozenset(rows[:MAX])` -> CLEAN; the byte-identical `sorted(rows[:MAX])` -> FLAGGED; `list(rows[:MAX])` -> FLAGGED. Neither tests/cap_spelling_corpus.py nor tests/false_alarm_corpus.py contains any set(/frozenset( entry, and cap_types' own clause-2 assertion (line 488) uses any(), so nothing pins these two names in either direction. This is NOT the 2026-08-06 review's #10, which mutates the vocabularies (adds get/pop/copy to _SCALAR_CALLS, marks BoolOp) - `set`/`frozenset` have zero hits in that document; this is the shipped roster.
- **Failure scenario:** A hook that de-duplicates its findings before returning them - `return set(findings[:MAX_FINDINGS])`, or any `set(...)`/`frozenset(...)` wrapper over a capped list - silently drops everything past MAX and the guard whose entire purpose is catching that says nothing. Nothing goes red: cap_shapes --selftest, run_selftests and both corpora are all unaffected because no fixture exercises the shape.
- **Suggested fix (a hypothesis, not a prescription - verify before applying):** Remove "set" and "frozenset" from _AGGREGATORS (they belong in _COLLECTION_CALLS, where "set" already is). Add both directions to cap_types.selftest(): assert a slice inside set(...) is NOT in _aggregated_nodes(), and keep the any() case asserting it IS. Then re-derive the roster from the stated rule rather than by name: an aggregator is a call whose result is a bool/number/branch, so "next" also deserves a second look.

### 4. [MEDIUM] The mutation table's module roster is DECLARED in two hand-typed import lines, so a third split file - or one half losing its list - removes entries from the sweep with every gate green

- **Where:** `tools/mutation_entries_b.py:14`
- **Class:** C6 declared roster standing in for a derived one / C8 denominator that can silently shrink
- **Claim:** MUTATIONS is assembled from exactly two hand-written imports (tools/mutation_check.py:310-311, `from mutation_entries_a import ENTRIES as _ENTRIES_A` / `_B`, concatenated at :313). Nothing derives that roster from `tools/mutation_entries_*.py`, and no floor anywhere compares len(MUTATIONS) against a recorded or derived expectation - I grepped: the only denominator floors are `if not entries or anchors <= 0` (tools/check_mutation_anchors.py:88-91) and `if not MUTATIONS` in that gate's selftest (:226-227), both of which pass on any non-empty subset. The current table is 218 entries (107 in _a, 111 in _b); file_size_baseline.json's own note records _b growing 538 -> 604 lines in the days after the split and it is now 682, so the 800-line ratchet that forced this split at 1415 lines will force the next one. run_selftests' `classify_tools` (run_selftests.py:312-322) WILL notice a new tools/*.py file, but it demands only that the file be declared a gate or added to NOT_A_GATE - where mutation_entries_a.py and _b.py already sit (run_selftests.py:148-149) - so the guard that fires points the author at the exemption list, not at the import roster.
- **Failure scenario:** Split _b again into tools/mutation_entries_c.py (or resolve a merge that truncates either file's ENTRIES list). Add mutation_entries_c.py to NOT_A_GATE, as the classify_tools failure instructs, and forget the import line. MUTATIONS silently becomes 218-minus-c entries. check_mutation_anchors prints `mutation-anchors: OK - N anchor(s) across M mutation entr(ies)` because M > 0; the sweep prints `M of M mutations executed, 0 skipped ... all mutations caught` because its denominator is len(MUTATIONS) itself; mutation_check records gate_ledger `mutation_sweep PASS`; the ship bar reads a green mutation tier. Every entry in _c is unrun and unreported, and no signal in the repo distinguishes that from a complete sweep. This is the same shape as INSTALL-TAUTOLOGY and ROSTER-DERIVE, reintroduced by the 2026-08-16 split: before the split, emptying the table gave 0 entries and the zero-denominator floor fired; after it, losing one half leaves a healthy-looking positive denominator.
- **Suggested fix (a hypothesis, not a prescription - verify before applying):** Derive the roster instead of declaring it: in mutation_check.py, glob `tools/mutation_entries_*.py`, import each with importlib, and concatenate their ENTRIES in sorted filename order - then a new part is picked up by existing, and a part that fails to import raises instead of silently contributing zero. Add a floor beside it that fails if any discovered module contributes 0 entries, and extend check_mutation_anchors.verdict()'s denominator check from `anchors <= 0` to also assert that every discovered entries module is represented, so 'one half evaporated' cannot read like 'the table is fine'.

### 5. [MEDIUM] The cap corpus declares itself APPEND-ONLY but nothing enforces it: deleting an entry is invisible to its own selftest, to the corpus-scorer gate and to no_regression, and raises the printed score

- **Where:** `tests/cap_spelling_corpus.py:628`
- **Class:** C8 denominator that can silently shrink / REMEMBER-not-ENFORCE
- **Claim:** Lines 21-22 state the load-bearing invariant: "APPEND-ONLY. Removing an entry, or flipping a must_flag, silently narrows every guard graded against it - the same class of regression that caused the revert." `selftest()` (line 628), whose docstring says "The corpus checks ITSELF", checks unique names, non-empty sources, .py paths, and that each class has at least ONE member (lines 642-645). It does not check the invariant the file says is the one that matters. No other gate does either: tools/score_corpus.py:selftest only asserts its own split arithmetic (`len(pos)+len(neg) != total`), which holds for any corpus size including 2; tools/no_regression.py:319-321 loads the CURRENT corpus for BOTH the predecessor and the working tree, so a deleted entry is absent from `prev_hits` and `cur_hits` alike and can never appear in `lost` - its only floor is `if not entries: raise Broken` (i.e. zero). The one entry with any protection is `scalar_slice_without_a_roster_entry`, guarded incidentally by the GHOST-waiver check at tools/no_regression.py:405 because tests/noregress_waivers.py:51 names it. The other 147 entries have none. No recorded baseline count exists anywhere - the three denominators stamped in docstrings (132 / 103 / 135) are prose, and docs/audits/adversarial_review_2026-08-06_guards.md:238 records that no gate compares them.
- **Failure scenario:** A maintainer or agent tidies the corpus by deleting the contradictory pair `size_skip_by_getsize` (line 238, must_flag=True) and `neg_size_skip_rostered` (line 250, must_flag=False) - an attractive edit, because score_corpus prints that pair every run as a CEILING problem and the entries are byte-identical. Afterwards: `python tests/cap_spelling_corpus.py --selftest` prints "146 entries" and SELFTEST OK; the AUX gate `corpus-scorer` (run_selftests.py:137, invoked with --selftest only) is green; `tools/no_regression.py` sees both sides lose the same entry, reports no `lost` and stays green; the GHOST-waiver check passes because the surviving waiver names a different capability. score_corpus's report changes from "CAUGHT 102/105 ... MISSED 3" to "CAUGHT 102/104 ... MISSED 2" - the ratio goes UP from 97.1% to 98.1%, the CEILING block drops from three contradictory groups to two, and a real recorded blind spot (the guard does not detect an os.path.getsize size-skip) leaves the record entirely. Every gate is green, the grader got weaker, and the number it prints got better.
- **Suggested fix (a hypothesis, not a prescription - verify before applying):** Enforce the invariant instead of asserting it in prose. Add to `selftest()` a check against a recorded manifest - the simplest crash-safe form is a committed sorted list of entry NAMES (or a hash of it) plus a floor count, with the selftest failing on any name that disappears and printing the removed names. Growth updates the manifest in the same commit; removal cannot pass silently. A count-only floor is not enough (it permits swap-one-for-one), and the check must live in the corpus's own selftest, not only in score_corpus, since no_regression reads the corpus directly.

### 6. [MEDIUM] HARNESS ERROR returns before `gate_ledger.record()`, so the run that proves the instrument is broken writes nothing and `last_run()` serves a stale PASS

- **Where:** `tools/score_false_alarms.py:349`
- **Class:** C1 - result recorded on some exit paths only
- **Claim:** `main()` computes `dead` (controls that never fired) at l.345 and does `return 1` at l.349, which is ABOVE the `gate_ledger.record("false_alarm_scorer", ...)` call at l.383-388. The two sibling gates that shared this exact shape were both fixed with an explicit `RECORD ON EVERY EXIT PATH` comment - tools/ship_bar_gate.py:237-247 and tools/check_file_size.py:209-222 - and this third instance was left. The source-coverage audit rated class 1 'CLEAN - all 6 now compute a real verdict' (docs/audits/source_coverage_2026-08-16.md:29) because it checked whether the verdict was computed, not whether every exit path reaches the call.
- **Failure scenario:** A payload-shape change (or a hook that starts ignoring UNBLUFF_STATE_DIR) makes a control stop firing - the recorded 2026-08-13 incident, where `false_alarm_scorer` sat 45.2h stale while every gate was green. `main()` prints HARNESS ERROR and returns 1 having written NO ledger row. The previous PASS row remains the newest row for the tier, so `gate_ledger.last_run('false_alarm_scorer')` still returns PASS with its old timestamp, and the ledger cannot distinguish 'never re-run' from 'ran and the harness was broken'. `run_selftests.unrecorded_tiers()` (run_selftests.py:213-236) is purely TEXTUAL - it only greps for `gate_ledger.record(` and the string `"false_alarm_scorer"`, both of which are still present - so it stays green. Aggravating: the record is also wrapped in `try: import gate_ledger / except Exception: pass` (l.382-390) with `tools/` never inserted into sys.path (l.44-48), already recorded as finding #35 / task #17 and still unfixed - so under `-m` or an in-process import BOTH paths record nothing.
- **Suggested fix (a hypothesis, not a prescription - verify before applying):** Move the `gate_ledger.record(...)` call above the `if dead:` block and record the real verdict on every exit path, mirroring tools/check_file_size.py:214-218: compute `failing = bool(dead) or blocks(nagging)`, record `"FAIL" if failing else "PASS"` with a `harness_error=len(dead)` field, then return. Apply ship_bar_gate.py's module-scope `sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))` plus a narrow `except ImportError` that PRINTS the degradation, so an unavailable ledger is announced rather than swallowed.

### 7. [MEDIUM] The I/O-control assertion reads load_factor() = max(cpu, io) but diagnoses only the I/O constant, so an uncontrolled CPU term can fail the selftest with a false verdict

- **Where:** `hooks/selftest_budget.py:403`
- **Class:** duration assertion with no control for one of its two dimensions (the exact class this module exists to remove)
- **Claim:** The probe at lines 393-407 pins the I/O half of the control (`_calibrate_io` stubbed to 0.0592s) and then asserts on `load_factor()`, which returns `max(1.0, min(_LOAD_FACTOR_CAP, max(cpu, io)))`. The CPU term is left live and unmeasured. The upper assertion `if _f > 4.6` therefore fires on `cpu > 4.6` just as readily as on a shrunken `_CALIB_IO_REF_S`, but reports only the latter - stating as fact that "the calibration constant has been edited" when nothing was edited.
- **Failure scenario:** On a box where the CPU probe measures more than 4.6x the recorded reference - i.e. `_calibrate()` exceeds 4.6 x 0.0123 = 0.0566s for the 300k-iteration loop - `_f` becomes the CPU ratio (up to the 6.0 cap), exceeds 4.6, and `selftest_budget.py --selftest` returns SELFTEST FAILED with: "a 0.0592s I/O probe produced 5.20x, but 0.0592s is 4x the RECORDED reference 0.0148s - the calibration constant has been edited, which saturates the control at its cap and disables the budget check" No constant was edited; the box was slow. run_selftests.py then marks the module FAIL, and in CI that is a red build with a diagnosis pointing at the wrong file. Reachability, measured: on this machine (Python 3.12) the CPU probe is 0.01427s = 1.16x, so ~4x further slowdown trips it. The module's own comments record 2.5x measured under a local mutation sweep (lines 111-113), i.e. more than half the distance, from load alone. CI (.github/workflows/selftest.yml) runs this on Python 3.8, 3.9, 3.11 and 3.12 across ubuntu/windows/macos runners; CPython 3.8/3.9 lack the specializing interpreter and are roughly 1.5-2x slower on this exact loop shape, and shared runners add contention on top - so the py3.8/3.9 windows legs sit in range. Nothing catches it because the assertion is the check; a false RED here is indistinguishable from a true one. The irony is load-bearing: this is the identical defect shape the surrounding comment block (lines 89-124) was written to eliminate - "asserts a DURATION with no control for how fast the machine is running right now" - reintroduced inside the probe that verifies the control. The file already knows the fix technique, having applied it to the I/O half two lines earlier.
- **Suggested fix (a hypothesis, not a prescription - verify before applying):** Pin BOTH probes for this assertion, the same way the I/O one is pinned: stub `_calibrate` to a fixed value (e.g. `_CALIB_REF_S`, giving cpu = 1.0) inside the same try/finally, so `_f` is a deterministic function of the I/O constants only and the 3.5/4.6 bounds mean what their messages say. Keep the live, unpinned `load_factor()` bound check at line 430-433, which is correctly a range assertion rather than a value assertion.

### 8. [MEDIUM] The broken-parse FAIL branch is unreachable: `surfaces` is derived from parsed commands, so a settings layer that cannot be parsed vanishes from both the count and the surface list

- **Where:** `tools/hook_divergence_report.py:442`
- **Class:** C1/C8 - a gate whose failure path cannot execute; denominator shrinks silently
- **Claim:** main() at 432-447 documents the intent to distinguish two zero-denominator causes ("no surface at all" = inapplicable vs "surfaces exist but produced no command" = a broken parse wearing a clean result). The implementation makes that distinction impossible. `result["surfaces"]` is populated ONLY inside the `for surface, cmd in sources` loop (provenance, lines 185-188), so it can only ever contain surfaces that yielded at least one command. Therefore `r["examined"] == 0` implies `r["surfaces"] == []` unconditionally, the `elif r["examined"] == 0:` branch at line 442 is dead code, and its `return 1` at line 447 can never execute. Confirmed in-process: `provenance(settings_paths=['run_selftests.py'], hook_dirs=[])` (a file that exists and is not JSON) returns examined=0, surfaces=[], which routes to the INAPPLICABLE branch and exit 0. The same swallow is live even when examined > 0: `_json_commands` catches `(OSError, ValueError)` at line 73 and returns `[]`, so "this layer has no hooks" and "this layer could not be read" are indistinguishable downstream, and the printed line "wiring surfaces read: N" lists only the layers that parsed.
- **Failure scenario:** A trailing comma is left in `~/.claude/settings.json` (or it is read mid-write, or saved with a UTF-16 BOM). `json.load` raises ValueError, `_json_commands` returns [], and every hook wired at the user layer - including a foreign copy of `pre_push_gate.py` pointing at a stale checkout - disappears from the population. The file is never listed in "wiring surfaces read", the count of examined commands drops by exactly the number of hidden wirings, and the gate prints "OK: every wired reference to our hooks resolves to this repo" and exits 0. If that layer was the only one declaring hooks, the run instead prints "This gate is INAPPLICABLE here" and still exits 0. Nothing catches it: the selftest writes every fixture with `json.dump` (lines 324-329), so an unparseable settings file is never planted, and no assertion requires a file that EXISTS to appear in `result["surfaces"]`.
- **Suggested fix (a hypothesis, not a prescription - verify before applying):** Record surfaces in `wired_sources()` at the moment each path is opened, with an explicit parse-ok flag, instead of reconstructing them from the commands that survived. Give parse failures their own bucket and add it to `bad` in main() alongside `foreign` and `unparsed`. Then plant an unparseable settings layer in selftest() and assert the run returns 1.

### 9. [MEDIUM] `<root>/.git/hooks` is a hardcoded path, so in a linked worktree or submodule the repo's own git hook surface is silently dropped - and the selftest assertion that would catch it is skipped in exactly that condition

- **Where:** `tools/hook_divergence_report.py:124`
- **Class:** C6 - declared path standing in for a derived one; two implementations of one rule drifted apart
- **Claim:** `_git_hook_dirs` assembles `os.path.join(root, ".git", "hooks")` at line 124 and then filters with `os.path.isdir(d)` at line 125. In a linked worktree and in a submodule `.git` is a FILE, not a directory, so the join names a path that is never a directory and the surface is dropped without a word. This repo already established the correct primitive and wrote down why: `hooks/pre_push_gate.py:85` `_common_git_dir()` uses `git rev-parse --git-common-dir` and its docstring states it "resolves a linked worktree to the HOST repo's .git, which is where the hooks actually live"; `hooks/fast_test_on_stop.py:77-84` documents the identical trap in the identical words ("os.path.isdir(cwd/.git) is FALSE in a linked worktree"). The newer gate reverted to the primitive V131 finding 5 recorded as wrong. Confirmed live in THIS checkout: `os.path.isdir('C:/Users/ammar/Downloads/unbluff-enforcing/.git/hooks')` is False; `_git_hook_dirs()` returns only `C:/Users/ammar/.claude/githooks`; git's real repo-local hooks dir for this worktree is `C:/Users/ammar/Downloads/unbluff/.git/hooks`, which is a directory, contains a live non-sample `pre-push`, and is NOT read.
- **Failure scenario:** Right now, in this worktree, `C:/Users/ammar/Downloads/unbluff/.git/hooks/pre-push` contains `exec "...python.exe" "C:/Users/ammar/Downloads/unbluff/hooks/pre_push_gate.py"` - a wiring to a copy of one of our hooks that lives OUTSIDE this repo root, i.e. exactly the "FOREIGN COPY WIRED" case this file exists to report. Git consults that file on every push from the worktree (the global dispatcher resolves it via `--git-common-dir`). The provenance gate cannot see it, because it looked for `<worktree>/.git/hooks`. The docstring's stated reason for reading `.git/hooks` at all - "the other becomes live the moment [core.hooksPath] is unset" (lines 100-105) - is therefore unmet for every worktree and every submodule: unset the global `core.hooksPath` and the surface that goes live is the one the gate never reads. The selftest cannot catch this either: its floor at line 279 is `if os.path.isdir(own):`, so in a worktree the assertion is skipped and, unlike the git-unavailable branch at 309-311, no NOTE is printed - `--selftest` prints SELFTEST OK with that assertion never having run.
- **Suggested fix (a hypothesis, not a prescription - verify before applying):** Resolve the directory the same way the rest of the repo does: `git -C <root> rev-parse --git-common-dir`, joined with `hooks` (reuse `pre_push_gate._common_git_dir` rather than writing a third copy). In selftest(), replace the `if os.path.isdir(own)` guard with a real linked worktree fixture (`git worktree add`) asserting the HOST repo's hooks dir is reported, and print an explicit NOTE on any path where the assertion is skipped.

### 10. [MEDIUM] GATE_TOKENS is a hardcoded roster with no cross-check: 3 of the 16 gates in run_selftests' own AUX_GATES registry are invisible to the guard

- **Where:** `hooks/piped_gate_guard.py:36`
- **Class:** C6 - declared roster standing in for a derived one
- **Claim:** `GATE_TOKENS` (lines 36-40) is a literal 13-entry tuple. Nothing derives it from the repo's gates and nothing asserts it covers them - grepping the tree for `GATE_TOKENS` finds no reference outside this file. Cross-checking it against `AUX_GATES` in `run_selftests.py` (extracted by AST, 16 rows) shows 3 registered gates that no token matches: `examples-settings-fresh` -> `tools/regen_example_settings.py --check`, `file-size` -> `tools/check_file_size.py`, `ship-bar` -> `tools/ship_bar_gate.py`. All three are registered as MEASUREMENTS (empty args tuple), deliberately not as `--selftest`, per the comments at run_selftests.py:66-83 - which is precisely why the `--selftest` catch-all token does not cover them. Confirmed by calling the detector directly: `piped_gates('python tools/check_file_size.py | tail -3')`, `piped_gates('python tools/ship_bar_gate.py 2>&1 | tail -5')` and `piped_gates('python tools/regen_example_settings.py --check | grep OK')` all return `[]`, while the sibling `piped_gates('python tools/no_regression.py | tail -3')` fires.
- **Failure scenario:** An agent runs `python tools/ship_bar_gate.py 2>&1 | tail -5` to read the stopping-rule verdict. `ship_bar_gate.main()` returns 1 on a CRITICAL/HIGH finding that is still unbuilt (tools/ship_bar_gate.py:205,215,248), the pipeline returns tail's 0, and the agent reads the last five lines and calls it clean - the exact silent-green shape this hook was built to block, on the gate whose whole job is refusing to ship. The same holds for `check_file_size` (the 800-line ratchet) and `regen_example_settings --check`. Nothing catches it: SHOULD_FIRE (lines 265-278) contains only tokens already in GATE_TOKENS, so the corpus is a restatement of the roster rather than an independent population, and adding a 17th gate to run_selftests changes neither.
- **Suggested fix (a hypothesis, not a prescription - verify before applying):** Assert coverage against a derived denominator in selftest(): read `run_selftests.AUX_GATES` (and any sibling registry), synthesise `<invocation> | tail -1` for every row, and fail if `piped_gates()` is silent on any of them - printing the denominator. That turns "add a gate, forget the token" from a silent blind spot into a red suite.

### 11. [MEDIUM] PROTECTED is a whole-command substring test, so the word "pipefail", "PIPESTATUS" or "LASTEXITCODE" anywhere - including after the pipeline, in a comment, or in a filename - disarms a blocking guard

- **Where:** `hooks/piped_gate_guard.py:187`
- **Class:** one-token disarm / a check that can be made unable to fail by unrelated text
- **Claim:** `piped_gates` opens with `if any(p in command for p in PROTECTED): return []` (line 187), matched against the raw command string with no position, tokenisation or dialect check. Any occurrence of `pipefail`, `PIPESTATUS` or `LASTEXITCODE` - in a comment, a quoted string, a filename, a grep pattern, or AFTER the pipeline it is supposed to protect - turns the guard off for that command. Confirmed by direct calls: the baseline `python run_selftests.py | tail -2` fires, while all of these return `[]`: `python run_selftests.py | tail -2 # remember pipefail next time`; `grep pipefail notes.md; python run_selftests.py | tail -2`; `python run_selftests.py | tail -2 > pipefail.log`; `python run_selftests.py | tail -2; set -o pipefail`; `echo "PIPESTATUS is a thing"; python run_selftests.py | tail -2`; `python run_selftests.py | tail -2 && echo LASTEXITCODE`. The last is a cross-dialect disarm: line 55-56 notes `LASTEXITCODE` "is meaningless in sh" and keeps one tuple for both dialects, but a meaningless token in sh still silences the sh guard.
- **Failure scenario:** The block message this hook itself prints says "or `set -o pipefail` first" (line 220-221). An agent that takes the advice but writes it in the wrong position - `python run_selftests.py | tail -2; set -o pipefail`, a plain ordering mistake - gets no warning at all on the retry, the suite's exit code is still tail's, and the guard has taught the agent the shape that disables it. The same holds for the entirely innocent `python tools/no_regression.py 2>&1 | tail -20 > pipefail-debug.log`. Nothing catches it: SHOULD_STAY_QUIET (lines 286-288) contains only the two correct spellings, and no case in either corpus asserts that a protector appearing AFTER the pipe still fires.
- **Suggested fix (a hypothesis, not a prescription - verify before applying):** Require the protector to be a token that appears in the command text BEFORE the first pipe (or, better, in a segment preceding the offending pipeline), and split the vocabulary by dialect so `LASTEXITCODE` cannot silence bash and `pipefail`/`PIPESTATUS` cannot silence PowerShell. Add both mis-positioned shapes to SHOULD_FIRE.

### 12. [MEDIUM] ship_bar_gate main() skips the ledger on both failure exits, so last_run("ship_bar") serves a stale PASS

- **Where:** `tools/ship_bar_gate.py:205`
- **Class:** C1
- **Claim:** The comment at ship_bar_gate.py:237-239 asserts "RECORD ON EVERY EXIT PATH with the REAL verdict" and names the exact defect it closed ("last_run() kept serving a stale PASS after a failing run"). Two exit paths still return 1 without ever reaching gate_ledger.record() at line 242-245: the CANNOT-RUN handler at 203-205 and the empty-population floor at 211-215. These are the two states in which the gate examined NOTHING, i.e. the worst two to be silent about.
- **Failure scenario:** docs/audits/findings.json is renamed, truncated, or loses its 'scope' key (or the report's '## Confirmed' heading is edited away). load_findings()/confirmed_from_report() raise ValueError, main() prints 'ship-bar: CANNOT RUN - ...' and returns 1 at line 205. No row is written to docs/audits/gate_runs.json. gate_ledger.last_run("ship_bar") therefore still returns the previous {"result": "PASS"} row, and tiers() still counts ship_bar as recorded. Any consumer asking the ledger "what did the ship bar last say?" - which run_selftests.py:204-206 states is exactly what the verify-before-pushing half will read - gets PASS for a gate that read zero findings. Nothing catches it: run_selftests.unrecorded_tiers() (run_selftests.py:235) is TEXTUAL - it only asserts the strings 'gate_ledger.record(' and '"ship_bar"' appear somewhere in the file, which they do; ship_bar_gate.selftest() never calls main(); and the mutation entries SHIPBAR-1..4 verify through --selftest, which does not execute main() either. The identical hole exists in the file this one cites as its twin: check_file_size.py:174 and :183 both return 1 above the record call at :216.
- **Suggested fix (a hypothesis, not a prescription - verify before applying):** Record before returning on both paths, e.g. gate_ledger.record("ship_bar", "CANNOT_RUN", reason=str(exc)) in the except at 203-205 and gate_ledger.record("ship_bar", "CANNOT_RUN", findings=len(findings), report_rows=len(report_rows)) at 211-215; apply the same to check_file_size.py:174 and :183. Then strengthen run_selftests.unrecorded_tiers() from a substring scan to an AST check that every `return`/`raise` in main() is dominated by a record() call, since the textual check cannot distinguish 'records' from 'contains the word record'.

### 13. [MEDIUM] PyMuPDF branch of _pdf_to_text returns unvalidated text, so a PDF with no text layer audits CLEAN over a document that was never read

- **Where:** `skills/consistency-audit/scripts/extract.py:161`
- **Class:** C4
- **Claim:** _pdf_to_text has three extractors. Branch 1 (pdftotext, line 153) guards its return with `proc.stdout.strip()`. Branch 3 (pdfminer, line 169) guards its return with `if text and text.strip()`. Branch 2 (PyMuPDF, line 161) returns `"\n".join(page.get_text() for page in doc)` with no emptiness check at all, so it returns whitespace-only text as a successful extraction and short-circuits the remaining fallback and the ExtractError. Nothing downstream floors the result either: deliverable_to_text (36-56) never asserts it produced any text, and audit.py's analyse() (audit.py:53-100) has no floor on len(numbers).
- **Failure scenario:** A user runs the skill on a scanned or image-only PDF (or any PDF whose text layer PyMuPDF cannot decode) on a machine where fitz is installed. pdftotext either is absent or returns whitespace, so branch 1 falls through. Branch 2 opens the document successfully, every page.get_text() returns "", and deliverable_to_text returns "\n\n\n...". find_numbers() returns [], find_placeholders() returns [], find_figure_captions() returns {}. render_text() then prints 'Numbers: 0 found, 0 checked' and '(none)' under [A] through [F] - a fully clean audit of a deliverable the tool never read. This is the identical failure the file's own [DOCX-1] note (lines 68-75) says the skill exists to catch ("The audit certified a deliverable clean for a number it never read, and printed a shrunken denominator with no notice"), reproduced on the PDF path. audit.py's selftest() has no PDF fixture, so nothing exercises this branch.
- **Suggested fix (a hypothesis, not a prescription - verify before applying):** Guard branch 2 the way its siblings are guarded: capture the join into a local, `if text.strip(): return text`, else fall through to pdfminer and ultimately to ExtractError. Separately, add a floor in analyse() (or in deliverable_to_text) that refuses to report a verdict when the extracted text has no alphanumeric content - 'zero numbers found' must be distinguishable from 'the document was not read'.

### 14. [MEDIUM] Word tables are undetectable by table_structure_*, so check [F] false-positives on every captioned table in any .docx deliverable

- **Where:** `skills/consistency-audit/scripts/extract.py:297`
- **Class:** check-cannot-pass
- **Claim:** table_structure_lines/table_structure_ranges recognise only a markdown separator row, an HTML <table>, or a LaTeX tabular/table environment (lines 294-297, 350, 378). Neither docx reader produces any of those: _docx_to_text_stdlib emits one line per w:p, so each Word table CELL becomes its own bare line (line 140-142), and the python-docx fallback joins cells with '\t' (line 101). find_table_captions, by contrast, matches a Word caption paragraph 'Table 1: ...' perfectly.
- **Failure scenario:** Run the audit on a .docx report containing five real Word tables, each with a 'Table N: ...' caption paragraph - the ordinary shape of the engineering report this skill was built for (SKILL.md:40 lists .docx first among supported deliverables). Verified directly against the flattened text a Word table produces: find_table_captions returns {'1': (1, 'Summary of results')} while count_table_structures returns 0 and table_structure_ranges returns []. audit.py's _table_findings therefore puts every captioned table into `missing`, and render_text prints '[F] TABLES PROMISED BUT NOT RENDERED (0 table(s) found)' followed by 'Table 1 has no rendered table body', once per real table. Both the count and every row are wrong, and SKILL.md:73-75 directs Claude to 'build the table from the source data or drop the reference'. Nothing catches it: every table fixture in audit.py's selftest (cases 4-7) is markdown, and _selftest_docx_textbox asserts only that three NUMBER needles survive the docx path - it never asserts the two readers agree on table structure, despite its stated purpose that 'The readers must AGREE' (audit.py:384).
- **Suggested fix (a hypothesis, not a prescription - verify before applying):** Have the docx readers emit a recognisable row delimiter for w:tbl content (e.g. join a row's cells with ' | ' and emit a separator line after the header row) so table_structure_ranges can see them, or make _table_findings skip class [F] entirely for formats whose text extraction cannot represent a table and say so in the report rather than printing '(0 table(s) found)'. Add a docx fixture with a real w:tbl to the selftest asserting count_table_structures >= 1.

### 15. [LOW] PowerShell truncation detection requires the full spellings `-first`/`-index`; the unambiguous abbreviations PowerShell actually accepts are undetected

- **Where:** `hooks/piped_gate_guard.py:85`
- **Class:** vocabulary too narrow to cover the shell it models - a blind spot in the same failure the guard documents
- **Claim:** `PS_TRUNCATING = ("-first", "-index")` (line 85) is compared by exact equality against lowered tokens in `_eater` (line 162). PowerShell resolves any unambiguous parameter prefix, and among Select-Object's parameters only `-First` begins with `f`, so `-f` and `-Fir` are valid, complete spellings of the same parameter and produce the identical pipeline truncation this file measured on 2026-08-13. Confirmed by direct calls: `piped_gates(r'python run_selftests.py | Select-Object -First 1', 'powershell')` fires, while `piped_gates('python run_selftests.py | select -f 1', 'powershell')` and `piped_gates('python run_selftests.py | Select-Object -Fir 1', 'powershell')` both return `[]`.
- **Failure scenario:** On the PowerShell host this repo is developed on, an agent writes `python tools\check_mutation_anchors.py | select -f 1`. The guard stays silent, `Select-Object` closes the pipeline after one object and tears the gate down before it finishes - the measured `$LASTEXITCODE = -1` with the completion marker absent, described at lines 71-76 as "strictly worse than the sh case". The agent gets no block, no message, and a verdict that was never computed. Nothing catches it: PS_SHOULD_FIRE (lines 299-311) uses only `-First` and `-Index` in full, so the corpus and the vocabulary are the same list written twice.
- **Suggested fix (a hypothesis, not a prescription - verify before applying):** Match by prefix rather than equality: treat a token as truncating when it starts with `-` and is a case-insensitive prefix of `first` or `index` (guarding `-i`, which is ambiguous with `-InputObject`), and add `select -f 1` and `Select-Object -Fir 1` to PS_SHOULD_FIRE.

## Refuted (46)

Recorded so the denominator is visible and nothing reads as un-examined. A refuted finding is
adjudicated, not dropped.

- [HIGH] check_python_floor cannot detect the two constructs its own docstring names as the reason it exists
- [LOW] The STATE_DIR 'paranoia' guard is unreachable - the line above it makes its condition always false
- [LOW] The D10 grandchild-kill probe treats a 2-second heartbeat stall as proof of death, with no control separating a stalled process from a killed one
- [MEDIUM] no_regression's "derived" unit population is a hardcoded directory roster that structurally excludes install.py and run_selftests.py, and it disagrees with check_review_freshness's roster
- [HIGH] A git history that cannot be READ is reported as "file has never been committed", and run() has no floor requiring that any unit was actually compared
- [MEDIUM] check_skill_deps reports OK after checking nothing: the dependency population is a declared name-roster matched only against ast.Assign, with no floor on `checked`
- [HIGH] The review ledger stopped recording on 2026-08-01, so cap_shapes.py/cap_types.py read as "never reviewed" while a 42-confirmed-finding adversarial review of exactly these two files sits unrecorded on disk
- [HIGH] _structural_split_names suppresses by NAME, not by bound, so one matched head/tail split immunises every other slice of that name - and it never checks the tail is kept, making a single discarded `_ = xs[MAX:]` a one-line disarm
- [MEDIUM] Every negative slice upper is skipped unconditionally, so `xs[:-500]` is CLEAN while its mirror `xs[-500:]` is FLAGGED
- [HIGH] DOCX1a reports CAUGHT by crashing, not by the text-box assertion, on every runner that lacks python-docx - which is every CI runner
- [HIGH] `exercises` is a DECLARED roster of hooks the payload cannot reach; 5 of 6 entries for numbers_match_on_write and plan_defer_guard exit before reading anything, and score_false_alarms counts them all in the denominator
- [MEDIUM] selftest() prints `N entries = A must-flag + B negative controls` as an identity but never asserts A + B == N, while A and B are hand-recomputed snapshots after each of four append blocks
- [HIGH] The NAG probe never varies the file path, so `blocks()` cannot fail for the two guards keyed per-(session, PATH)
- [MEDIUM] `DRIVEN` is a hand-typed event roster that nothing reconciles against `install.desired_groups()`, so narrowing it silently shrinks the roster while the selftest stays green
- [HIGH] An audit that examined ZERO numbers prints a fully clean report and exits 0 - no floor on either side of the comparison
- [MEDIUM] `_orphan_embeds` disables the class-B embed check for any .docx that contains a single figure caption or 'Figure N' mention
- [MEDIUM] A source file over 8 MB is dropped from the index AND from the reported file count, so real values read as fabricated with no signal
- [MEDIUM] budget_coverage() is a second, substring-based implementation of selftest-detection; it disagrees with the canonical has_selftest() on 4 of 25 files and prints a coverage figure that is wrong in both directions
- [MEDIUM] The twin gate asserts two hardcoded identifier names instead of the property "one implementation of selftest-detection" - and a second implementation exists today, with the gate green
- [MEDIUM] governing_binding_ok() checks only the attribute NAME, not the module it is read from, so the cap can be repointed at a different source with both structural guards green
- [MEDIUM] The glob-metachar probe silently discards test cases via `except OSError: continue`, with no count and no floor - one of its four cases is already being dropped on Windows
- [LOW] The twin gate silently skips any file it cannot read and prints no denominator, so "no twin found" is indistinguishable from "the file was never opened"
- [MEDIUM] No floor on the derived hook roster: with `our_hook_names` empty the gate examines 55 real commands, classifies none, prints OK and exits 0
- [MEDIUM] `git config --local core.hooksPath` is read from the process's cwd while `.git/hooks` is read from REPO_ROOT, so the two scopes can resolve to different repositories
- [LOW] The selftest pins `Select-Object -First ... ; exit $LASTEXITCODE` as SAFE, contradicting the file's own measurement that -First leaves no verdict to read
- [HIGH] The source index is an undifferentiated bag of numeric tokens, so class [A] structurally cannot fail against the sweep outputs the skill names as its primary input
- [MEDIUM] reconcile() joins report to ledger on (severity, where) while discarding the finding text both sides carry; 4 of 24 live rows share a `where`
- [MEDIUM] The report parser drops unrecognised rows silently, and its only shrinkage detector disables itself without saying so
- [MEDIUM] Placeholder vocabulary flags ordinary bracketed cross-references, and SKILL.md tells Claude they are almost always real defects
- [MEDIUM] index_sources swallows every per-file failure silently while source_files still counts the file
- [LOW] The PASS line counts only SCHEDULED rows as the MEDIUM/LOW that ship as written rows, omitting FINALIZED-EXCLUSION
- [HIGH] An UNREADABLE ledger read silently destroys the entire gate history with a successful atomic write
- [HIGH] check_file_size's two CANNOT-RUN exits return 1 without writing a ledger row, so a failed run leaves the previous PASS standing
- [HIGH] timing_claim_guard emits its only output on stderr with exit 0, against this repo's own measured rule that advisory hooks must print()
- [MEDIUM] tiers() and last_run() discard read()'s status, so a corrupt or unreadable ledger still reads as 'no gate has ever run'
- [MEDIUM] The __pycache__ assertion in check_file_size's selftest cannot fail under either derivation, leaving SKIP_DIRS unverified
- [LOW] AGAINST_A_BUDGET's unit list omits 'hours' while DURATION includes it, so the second trigger is blind to hour-scale claims
- [HIGH] Every plugin-layer degradation is computed and then discarded; report_line() has zero callers
- [MEDIUM] The roster-gap half of the mutation-anchors gate has no population floor and prints no denominator
- [HIGH] The per-hook timing never checks the subprocess return code, so a hook that errors out instantly is measured as a fast hook
- [MEDIUM] Hooks that cannot be measured vanish from the denominator, and the 60 s timeout removes the slowest ones first
- [HIGH] Every registered PostToolUse command is really executed 5x with matchers ignored, so the measured path is not the real one and the fire ledger is polluted
- [MEDIUM] compare_delivery_gate has no verdict for "the hook did not run", so a crash or timeout scores WARN and inflates the agreement counters
- [MEDIUM] The cross-tool eval harness carries no expected verdicts, so its two known false-clean failure modes are output-identical to a healthy result
- [MEDIUM] make_demos declares a 12-asset output roster against 16 README image references, and nothing reads the README's references
- [LOW] "node -e" is tested against the full command in one place and a 60-character truncation in another

