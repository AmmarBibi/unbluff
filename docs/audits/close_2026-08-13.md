# Close ritual - 2026-08-13 (completeness / source-coverage / meta-review)

Consistency is in `consistency_2026-08-13.md`. Order was consistency -> completeness ->
source-coverage -> **meta-review LAST**, and the order earned itself again: meta-review's
durability check found a gap created by a fix that source-coverage had prompted 20 minutes
earlier.

---

## completeness-audit

**STEP 1 - soft-defer sweep: CLEAN.** Grepped the plan and ledger for `-> park`, `on demand`,
`wait for a concrete`, `someday`, `if time`, `DROPPED`, `optional-forever`. Every hit is the
OPPOSITE of a soft defer: *"0 dropped"* (a coverage statistic), *"NONE of these may be dropped"*
(a rule), *"recorded rather than quietly dropped"*. **Zero optional-forever items.**

**STEP 2/3 - does every item deferred today have a HOME? Two gaps, both fixed.**

| item | ledger row | plan order | action |
|---|---|---|---|
| `FA-MEMHYG` | yes | **NO** | ADDED to step 3 with the precedented fix named |
| `LEDGER-POLLUTION` | yes | **NO** | ADDED to step 3 |
| the 800-line rule (now SIX files) | yes | **stale (said five)** | ADDED, with the three 08-13 increases named |
| criterion-3 tail (4 UNMEASURED + 3 NO-CORPUS-ENTRY) | yes | **NO** | ADDED as step 4's scheduled tail |
| `LEDGER-8`, `DENOM-SETTLED` | yes | n/a | resolved, not deferred - ledger is the right home |

**The structural finding:** the ledger records STATE and the plan carries ORDER OF WORK, and
today's items reached the first but not the second. The standing rule is that a deferral is
scheduled in the SAME edit; that held for the ledger and failed for the plan.

---

## source-coverage

**Source (2): the 24 confirmed findings. They reconcile EXACTLY, by ROW:**

| status | n |
|---|---:|
| BUILT | 16 (L17-L22, L24, L26-L33, L36) |
| FINALIZED-EXCLUSION | 1 (L25 - pytest rc 3 still blocks, decided deliberately) |
| OPEN | 7 (L23, L34, L35, L37, L38, L39, L40) |
| **total** | **24** |

**So "8 remain" is off by one** - seven are open, the eighth is a decision already taken. The
seven are now enumerated with severities (ledger N4); before today they were enumerated NOWHERE,
and N3's apparent list of them named five items the same table marks BUILT. None is CRITICAL or
HIGH, so **step 3 stays closeable - the conclusion survived, the premise did not.**

**Source (1): the four DoD criteria.** 1 = SCHEDULED (steps 5/6, 243 rows). 2 = closeable per
above. 3 = now PARTIAL and measured. 4 = CLOSED.

**GAP FOUND, and it was in today's own work.** The scorer's derived roster holds **11** wired
hooks; its results table printed **8**. `post_tooluse_dispatcher`, `close_skills_guard` and
`stop_dispatcher` were absent rather than visibly uncovered, so the table was becoming its own
denominator - the exact failure the tool's fourth rule exists to prevent, committed by the tool
that states rule 4. **Fixed:** every roster hook now gets a row, `NO-CORPUS-ENTRY` where nothing
exercises it, and the summary asserts the three buckets PARTITION the roster.

---

## meta-review (last)

1. **Parked-but-unscheduled** - none beyond completeness's two, now scheduled.
2. **Instance-only fixes - ONE FOUND, in my own work.** The `timing_claim_guard`
   `UNBLUFF_STATE_DIR` fix had NO mutation. Worse, a naive pin would have been HOLLOW: the
   hook's selftest overrides `MARKER_DIR` globally, so a mutation deleting the env lookup would
   still pass (hollow-pin mode 1, green for an unrelated reason). **Fixed properly:** an
   assertion that runs BEFORE the override and checks the marker actually lands under
   `UNBLUFF_STATE_DIR`, pinned by new mutation `TC-ENV`, CAUGHT.
3. **Optimization - the 800-line rule is now broken by SIX files, and three grew today:**
   `mutation_check` 1309, `pre_push_gate_selftest` 1109, `fast_test_on_stop_selftest` 1003,
   `install` 927, `duplicate_registration_check` **858 (newly over)**, `fast_test_on_stop` 832.
   The ledger said five. Scheduled with the numbers, not a vibe.
4. **Missing / wrong - THE GATE LEDGER, read not reconstructed.** `gate_runs.json` holds **200
   entries, all `run_selftests`**. Today ran the mutation harness 5x, the integration suite 4x,
   the anchors gate 4x and the new scorer - **none left a trace**. "Which gates ran this session"
   is therefore UNVERIFIABLE from the durable record, and today ADDS evidence to that finding
   rather than resolving it. It remains the enabler for `SHIP-BAR-GATE`'s verify-before-push half.
5. **Improvements** - the highest-value one: make `score_false_alarms` write its per-hook result
   into `gate_runs.json`, which would begin closing (4) with the tier that has the most to say.
6. **Mechanism health** - suite 34/34, integration 30/30, anchors 188/187 across 34 files, hooks
   healthy. ONE canonical order list, refreshed. `close_skills_guard` fired correctly TWICE today
   and blocked a premature close both times.

**End-of-turn finalize:** the recommended-order list is refreshed - PGG-PS marked DONE, step 4
split into DONE (bounded) and SCHEDULED (tail), and every item deferred today is in the order.
