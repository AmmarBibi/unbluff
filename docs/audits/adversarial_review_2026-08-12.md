# Adversarial review - 2026-08-12, run `wf_a6b49ecf-667`

**Subject:** unbluff @ `597baea`. **7 lenses, refuter stage UNCAPPED.** 56 agents, 6.84M subagent tokens, 46 min. Tree guard: **zero repo writes**.

**Coverage: produced 49, adjudicated 49, dropped 0, `coverage_complete: True`.** Confirmed 24, refuted 25.

Per lens (produced): `external-authority` 6, `r1-reachability` 5, `governance-routing` 8, `correctness` 7, `r2-dispositions` 7, `step2-instrument` 9, `test-quality` 7


This pass exists because the author of FASTTEST-BLOCK also wrote its only probes. It found a defect that fix INTRODUCED - a false NEGATIVE, the more dangerous direction than the false alarm it removed.


## Confirmed (24)

| sev (corrected) | finder said | lens | finding | file |
|---|---|---|---|---|
| **CRITICAL** | CRITICAL | external-authority | C1 is FALSE: pytest rc 4 is what a BROKEN conftest.py returns, so the waiver silently disables the gate on the user's own broken code | `hooks/fast_test_on_stop.py:252` |
| **HIGH** | CRITICAL | correctness | Containment notice is once-per-project FOREVER, so a permanently-inconclusive repo ends every turn after the first with rc 0 and EMPTY stderr | `hooks/fast_test_on_stop.py:603` |
| **HIGH** | HIGH | test-quality | C1 is FALSE for rc 4: a broken conftest.py is the user's own code, and after one notice the gate is silent forever | `hooks/fast_test_on_stop.py:252` |
| **HIGH** | HIGH | governance-routing | The inconclusive notice is keyed by path hash alone, so a LATER, DIFFERENT nothing-verified state is permanently silent - exit 0, empty stderr, byte-identical to a green turn | `hooks/fast_test_on_stop.py:610` |
| **HIGH** | HIGH | governance-routing | _notice_no_gate has two different reasons sharing ONE state key, so the "add a gate" notice permanently masks the "pytest is not importable" notice - the exact masking _inconclusive_state_path's docstring forbids | `hooks/fast_test_on_stop.py:566` |
| **MEDIUM** | HIGH | r1-reachability | Section N's "the roster is now the AST import closure of the wired entry points" is false - 7 of 25 hooks reach it only via a hardcoded REQUIRED_HOOKS tuple, and dispatcher-roster drift is silent end to end | `install.py:351` |
| **MEDIUM** | MEDIUM | r2-dispositions | review_runs.json omits the entire 2026-08-06 guards run (42 confirmed, 10 HIGH), so the ledger's "the 42 is the complete population" closure is complete only over what was recorded | `docs/audits/coverage_ledger_2026-08-09.md:246` |
| **MEDIUM** | HIGH | external-authority | C4 is FALSE after the first turn: the inconclusive notice is once-per-PROJECT and keyed only by cwd, so later turns - and later, different reasons - are byte-identical to a green run | `hooks/fast_test_on_stop.py:610` |
| **MEDIUM** | MEDIUM | external-authority | The exit map is incomplete in the blocking direction: ExitCode.INTERNAL_ERROR (3) is by pytest's own definition not a verdict, and still hard-blocks the turn end and the push | `hooks/fast_test_on_stop.py:250` |
| **MEDIUM** | MEDIUM | external-authority | C3 false negative from a canonical-value gap: _PYTEST_CONFIG_MARKERS recognises 4 of pytest's 7 config filenames, so real pytest projects get no gate | `hooks/fast_test_on_stop.py:164` |
| **MEDIUM** | HIGH | correctness | _is_pytest_command misses `py.test` and `pytest-3`, so the FASTTEST-BLOCK hard block survives verbatim for those spellings in BOTH gates | `hooks/fast_test_on_stop.py:264` |
| **MEDIUM** | MEDIUM | correctness | C3 is false: looks_like_pytest_project rejects three of the most common genuine pytest layouts, and README claims 'auto-detects pytest' | `hooks/fast_test_on_stop.py:235` |
| **MEDIUM** | MEDIUM | correctness | _PYTEST_FILE_CAP counts every file, so a tests/ tree with >5000 non-Python fixtures and ZERO .py files is classified as a pytest project | `hooks/fast_test_on_stop.py:224` |
| **MEDIUM** | LOW | correctness | _has_pytest_config matches its markers inside comments and strings, and reads config without utf-8-sig while _read_override uses it | `hooks/fast_test_on_stop.py:192` |
| **MEDIUM** | MEDIUM | test-quality | `py.test` - pytest's own still-shipped console script - is not recognised, so FASTTEST-BLOCK survives verbatim for that spelling | `hooks/fast_test_on_stop.py:264` |
| **MEDIUM** | MEDIUM | test-quality | The C2 control loop is decorative: none of its three commands contains the substring 'pytest', so a substring match passes it | `hooks/fast_test_on_stop_selftest.py:287` |
| **MEDIUM** | HIGH | governance-routing | The two gates DISAGREE about a real pytest project whose pytest is not importable: the Stop gate names the reason, the push gate says "has no test command - nothing to verify" | `hooks/pre_push_gate.py:220` |
| **MEDIUM** | MEDIUM | governance-routing | Stale detection claims the fix did not update: fast_test_on_stop's own module docstring, pre_push_gate's docstring, the repo's .claude/fast-test.cmd comment, and a CHANGELOG with no entry for the behaviour change | `hooks/fast_test_on_stop.py:9` |
| **MEDIUM** | MEDIUM | governance-routing | Design claim C2 is false: _is_pytest_command misclassifies non-pytest commands, and the repo's own push-gate selftest is built on that misclassification | `hooks/fast_test_on_stop.py:264` |
| **MEDIUM** | HIGH | step2-instrument | A failing `git commit` is misreported as "git worktree unavailable", silently drops worktree-delegation (and scenarios 17+18) from the denominator, and the suite still exits 0 | `hooks/pre_push_gate_selftest.py:842` |
| **MEDIUM** | LOW | step2-instrument | `_sh_delegation_fails` can emit three mutually contradictory statements in one run, and the probe asserts only the verdict and the numerator - never the message | `hooks/pre_push_gate_selftest.py:98` |
| **LOW** | MEDIUM | r2-dispositions | _open_count() maps an ABSENT `open` field to 0, and 45 of 68 ledger entries have no such field - the ledger reads unknown as zero | `tools/check_review_freshness.py:154` |
| **LOW** | MEDIUM | test-quality | _has_pytest_config's marker check is unpinned - dropping it re-introduces FASTTEST-BLOCK on every Python repo and the suite stays green | `hooks/fast_test_on_stop_selftest.py:174` |
| **LOW** | MEDIUM | test-quality | The three-state cap in _has_collectible_tests is unpinned; collapsing None to False silently disables the gate on any large tests/ tree | `hooks/fast_test_on_stop.py:224` |

## Refuted (25) - kept, because a cluster of near-misses is itself a signal

| lens | claim | why refuted |
|---|---|---|
| r1-reachability | R1's four-clause entry-point derivation omits install.py itself - the file the README tells every user to run, | REFUTED as a HIGH. The finding's literal premise is true but its consequence is contradicted by the same document, on the exact function it names.  WHAT I CONFIRMED (the reviewer's parsing is correct): - `install.py:desired_groups |
| r1-reachability | CAP-FP-1's R2 verdict ("reaching it requires editing unbluff's own source, so it is developer-facing") is wron | REFUTED on three independent grounds, each demonstrated by running code or quoting the file.  1) THE REVIEWER'S OWN REPRO CONFIRMS R2 RATHER THAN CONTRADICTING IT. I reproduced it exactly in my own scratch tree (scratchpad/lens_ca |
| r1-reachability | Three R1 disposition locators are stale at HEAD 597baea, including the anchor for clause 4 - the load-bearing  | REFUTED AS NOT-NEW, and mis-rated. The three locator facts are true; the finding they support is already recorded, adjudicated at LOW, and SCHEDULED in the repo's own ledger.  WHAT I VERIFIED AS TRUE (all three locators are genuin |
| r1-reachability | R1 clause 3's coverage claim overstates the sweep (22 of 25, not "every hooks/*.py"), and KNOWN_NO_SELFTEST is | REFUTED on its load-bearing claim, on its mechanism, and on its stated reason-it-matters. Only a LOW prose imprecision survives, and even that is already recorded in the ledger.  1) "Nothing asserts that delegation" - FALSE, demon |
| r2-dispositions | 32 of the 42 confirmed findings from the 2026-08-06 guards review were never put through the R1/R2 rule at all | REFUTED on four independent grounds, three of them decisive.  (1) NOT NEW - and recorded in the CURRENT ledger, not just the HISTORY doc. docs/audits/coverage_ledger_2026-08-09.md:547 (section K2, "The five CORRECTIONS - now carri |
| r2-dispositions | R2 HOLDS for the numbers_match cap: a user's own report file makes a shipped hook announce 500 as the real tot | TECHNICAL CORE REPRODUCES, BUT THE CLAIM IS REFUTED ON BOTH LOAD-BEARING NON-TECHNICAL ASSERTIONS (not-new + mis-rated severity).  WHAT I CONFIRMED (scratch dir C:\Users\ammar\AppData\Local\Temp\claude_lens_numcap_refute\, repo by |
| r2-dispositions | CAP-FP-1's holding exemption asserts, in shipped source, a self-liveness property that is demonstrably false a | REFUTED as not-new and over-rated, though one narrow sub-claim survives at LOW.  WHAT I REPRODUCED (so nothing below is speculation).  1. The mechanism is real. hooks/cap_shapes.py:463-464 runs `keys.add(key)` before the roster ch |
| r2-dispositions | The AR6-COUNT arithmetic error is still live three lines from the number that corrected it - 10 + 23 + 6 = 39, | FACTUAL CORE CONFIRMED, DISPOSITION REFUTED. I verified the reviewer's observation independently: docs/audits/adversarial_review_2026-08-06_guards.md headings run #1-10 [HIGH], #11-36 [MEDIUM] (=26), #37-42 [LOW] (=6), total 42. L |
| r2-dispositions | review_runs.json's `confirmed` field is run-scoped in 5 runs and unit-scoped in 1, with no schema marker disti | REFUTED on four independent grounds, each verified by running code against the repo at HEAD 597baea (tree left clean).  1. THE FINDING'S OWN MEASURED NUMBER IS WRONG. It states "Summing `confirmed` over all 68 entries yields 1,193 |
| external-authority | C2 is false in one direction: _is_pytest_command matches `pytest` as a PATH COMPONENT, applying pytest's exit  | REFUTED. The measured *behaviour* reproduces exactly, but every consequence the finding draws from it is wrong, and the central accusation misquotes the file.  **1. The docstring does not say what the finding quotes it as saying.* |
| external-authority | The ecosystem rationale for excluding `tests/` is factually wrong about Go and Java, and omits the one ecosyst | REFUTED — the finding is majority-false against the file, and its stated harm is mechanically prevented.  (1) SCOPE CLAIM FALSE, one command. The finding asserts "the same wording is repeated in pre_push_gate.py:271 and docs/audit |
| correctness | _has_collectible_tests's `except OSError: return None` arm is dead - os.walk swallows the error, so 'could not | The MECHANISM is true and I reproduced it; all three load-bearing CONSEQUENCE claims are false, and the repair direction the finding implies is the FASTTEST-BLOCK regression this session removed.  CONFIRMED (the only true part). h |
| correctness | _is_pytest_command matches non-pytest commands, and the repo's own selftest fixture depends on that false posi | MECHANISM CONFIRMED, DEFECT NOT. I re-ran the reviewer's regex claim against the real module (scratch probe, repo untouched, `git status --porcelain` empty at 597baea) and all six matches reproduce exactly: `/opt/pytest/bin/run_su |
| test-quality | C4 is FALSE from turn 2 onward, and every probe of it calls main() exactly once so no assertion can tell the d | REFUTED as stated (HIGH). The finding's load-bearing argument is factually wrong, its behavioural premise restates a limitation the repo has already recorded, decided and contained, and the only part that survives is a narrow test |
| test-quality | The e2e denominator counts a case that bypasses detect(), so on a pytest-less box neither counted case runs a  | REFUTED. The finding's own DETAIL already concedes the mechanical half (denominator derived by append, shrinks on skip, every counted case carries a guarded assertion). What remains is a labelling/interpretation worry, and its fai |
| governance-routing | README's "auto-detects pytest" is now conditional on a precondition the README never states, and the test that | REFUTED on four independently sufficient grounds, three of them by running code.  1) NOT-NEW: the exact claims are already rows in the repo's own criterion-1 denominator, and their disposition is SCHEDULED. docs/audits/promise_inv |
| governance-routing | The fire ledger can no longer distinguish a green turn from a nothing-verified turn - the same defect class th | REFUTED by running the exact scenario. (1) The failure scenario's load-bearing premise - "the ledger is the only remaining trace" - is demonstrably false. I built a temp repo (pyproject [tool.pytest.ini_options], tests/test_ok.py, |
| governance-routing | Both notices go completely silent when the state directory cannot be written - the one condition where silence | REFUTED as a criterion-2 defect: half the finding is factually wrong about which code path executes, and the reachable half is triggered only by an undocumented internal env var or a broken ~/.claude.  CONFIRMED PARTS (by running  |
| step2-instrument | The `bash` fallback and `_works`'s exception handler in `_resolve_sh` execute on NO box at all - not on any CI | REFUTED as a HIGH ship-blocking defect. The coverage half of the claim reproduces exactly (I re-ran the trace independently: plain-Windows PATH hits 300,301,304,305,306 and misses 308-310; sh-on-PATH/CI shape hits 300,301,302 and  |
| step2-instrument | The `[sh-delegation] shell: X (N candidate(s) probed)` line cannot say which branch resolved the shell - and t | REFUTED, on four independent grounds, all demonstrated by running the shipped code.  REPRODUCED FIRST (the reviewer's raw observation is true): on this box the two branches print, respectively, `shell: C:\Program Files\Git\bin\sh. |
| step2-instrument | Two of this unit's printed-denominator guards are structurally dead: `if not cases:` and `if not layouts:` gua | FACTUAL CORE IS TRUE BUT THE DISPOSITION IS WRONG.  Verified true by running code: an AST pass over hooks/pre_push_gate_selftest.py shows `cases` has exactly ONE binding (ast.List, 8 elements, line 141) and `layouts` exactly ONE ( |
| step2-instrument | The sh-delegation roster is DECLARED, not derived: 4 shell-executing call sites in the file, 3 hand-written na | REFUTED on three independent grounds. The finding's arithmetic is accurate but it is (a) not new, (b) not live, and (c) fails R2.  **1. NOT NEW - it is the ledger's own SCHEDULED row, near-verbatim.** `docs/audits/coverage_ledger_ |
| step2-instrument | `record_gate_run` fails completely silently - a run whose gate-ledger row was never written is indistinguishab | The MECHANISM is real and I reproduced it: run_selftests.py:256 is `except Exception: pass`, and with HERE pointed at a tree where `docs` is a FILE, record_gate_run(33, [], []) returned None, emitted '' on stdout and '' on stderr, |
| step2-instrument | `_works`'s docstring claims the exit-7 probe refuses "a binary that ignores its arguments"; measured false - a | REFUTED on three independent grounds, all measured.  (1) THE TITLE IS BUILT ON A TRUNCATED QUOTE. The docstring at hooks/pre_push_gate_selftest.py:287-289 reads: "a binary that ignores its arguments AND EXITS CLEANLY would satisfy |
| step2-instrument | check_readme_fresh gates the selftest COUNT but not the 33 unit NAMES the README pastes beside it | REFUTED as not-new: the repo has already recorded this exact limitation, in its own criterion-1 instrument, and scheduled the fix.  WHAT I CONFIRMED (the reviewer's mechanics are correct): - `tools/check_readme_fresh.py:29` `_CLAI |
