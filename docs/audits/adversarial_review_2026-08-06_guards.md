# Adversarial review - six unbluff guard files, 2026-08-06

**Run id `wf_91a48c61-20d`** (HEAD `5f23b2b`). 5 lenses, refuters UNCAPPED.

## COVERAGE - this run is INCOMPLETE and must not be read as a clean bill

| | |
|---|---|
| lenses launched | 5 |
| lenses that RETURNED | **4** |
| findings produced | 44 |
| findings adjudicated | 44 (all 44 from the 4 completed lenses) |
| CONFIRMED | 30 |
| REFUTED | 14 |
| agents launched | 49 |
| repo writes by those 49 agents | **0** (tree diffed against a pre-run snapshot) |

**The `disarmability` lens never returned.** Its transcript ends at "Now the disarm probes on
the never-probed units" - it had finished the cap_shapes/cap_types portion and stalled
entering hook_layers / check_mutation_anchors / score_corpus. Those three have therefore had
NO disarm probing at all, which is the single most important lens for this repo (finding MR-a
was a gate fully disarmable with every test green). **Treat its findings as OPEN CANDIDATES,
not as absent.** Scheduled as `AR-GAP`.

## CONFIRMED findings (30), by refuter-corrected severity

### 1. [HIGH] 86% false-positive rate on ordinary correct Python - the guard is unshippable outside hooks/

- **file:** `hooks/cap_shapes.py`  (finder rated CRITICAL, refuter corrected to HIGH)
- **detail:** slicing_offenders() reports 0 sites on hooks/ (10,155 loc) and 105 sites on the CPython 3.12 stdlib top level (128,291 loc). A deterministic systematic 1-in-5 sample of those 105 (offs[::5] = 21 sites) was read in source and hand-adjudicated: 18 FALSE, 3 defensible-true. A further 14 sites adjudicated outside the sample gave 13 FALSE. The clean sweep over hooks/ is therefore not evidence that the detector is precise - it is evidence that hooks/ has been iteratively sanded down to fit the detector's blind spots. The repo's own tools/ (4 reports) and scripts/ (1 report) already contain 3 false alarms, and B1-SCOPE plans to widen the sweep into exactly that tree. This is finding B3-FP with a new subject: making a guard see more made it fire on correct code, and a guard that nags gets disabled.
- **failure scenario:** The moment slicing_offenders is pointed at any tree that was not written against it - tools/, scripts/, a vendored dependency, or a second repo - it emits tens of reports per 10k lines, ~86% of which are wrong. The operator disables it. Reproduce: run cap_shapes.slicing_offenders(r'<python>/Lib') and count 105; run it on Lib/email and count 16 against 9,927 lines.
- **refuter:** NOT REFUTED — the counts reproduce exactly and every false-positive family is demonstrable on a planted fixture. But the write-up needs four corrections and the severity is one band too high. WHAT REPRODUCES EXACTLY (scratch copy at ...\scratchpad\refute_b3fp_slicing_fp, repo untouched): - cap_shapes.slicing_offenders(hooks) = 0 sites / 22 files / 10,155 loc. - slicing_offenders(<py312>\Lib) = 105 sites / 163 files / 128,291 loc. - tools/ = 4 sites / 3,533 loc; scripts/ = 1 site / 297 loc; tests/ = 0. - The deterministic offs[::5] sample is 21 sites, exactly as stated. MY OWN ADJUDICATION OF T

### 2. [HIGH] `seq[n:] = []` is invisible while its exact semantic twin `del seq[n:]` is a shape - the assignment-target context is never examined

- **file:** `hooks/cap_shapes.py`  (finder rated CRITICAL, refuter corrected to HIGH)
- **detail:** _slice_sites handles ast.Delete explicitly (lines 135-142) and flags `del out[12:]` as "deletes the tail past a bound". The identical operation spelled as a slice assignment is invisible. Verified side by side: `del out[12:]` -> FLAG; `out[12:] = []` -> CLEAN; `out[12:] = out[:0]` -> CLEAN. The reason is structural and easy to miss: the assignment target `out[12:]` IS an ast.Subscript with an ast.Slice, so the generic Subscript branch does reach it, but the tail-slice arm at line 158 requires `_negative_constant(sl.lower)` - a POSITIVE lower with no upper is treated as "drops the first n", which is right for an rvalue and exactly wrong for an assignment target, where it means "replace everything from n onward". Nothing anywhere distinguishes ast.Store from ast.Load context. This is the single cleanest counterexample to the grammar-closed claim because both spellings live in the same AST 
- **failure scenario:** `findings[MAX:] = []` - a completely ordinary, idiomatic in-place truncation - is reported clean by a guard that flags the byte-for-byte equivalent `del findings[MAX:]`. A contributor who hits the `del` form, sees the guard complain, and rewrites it as a slice assignment to silence it has fully disarmed the check while every test stays green.
- **refuter:** NOT REFUTED - reproduced end to end through the project's own ship gate, and it is new. REPRODUCTION (isolated scratch copy; the real repo was never written to - git status shows only a pre-existing one-line change to docs/V131_REVIEW_PLAN.md that I did not make). 1. Side-by-side, via cap_shapes.slicing_offenders on planted modules: del out[12:] -> FLAG "deletes the tail past a bound" del out[MAX:] -> FLAG out[12:] = [] -> CLEAN out[MAX:] = [] -> CLEAN out[MAX:] = () -> CLEAN out[12:] = out[:0] -> CLEAN module-level SHOWN[MAX:] = [] -> CLEAN def report(findings: list): findings[MAX:]=[] -> CLE

### 3. [HIGH] The grammar-closed claim is false: 12 silent-shortening operations produce ZERO sites, and three of them planted in a real hook leave the ship-blocking gate green

- **file:** `hooks/cap_shapes.py`  (finder rated CRITICAL, refuter corrected to HIGH)
- **detail:** The module docstring stakes the whole rebuild on "the ways to silently shorten a sequence ARE grammar-closed and few". probe_root_cause.py calls the four shape functions directly, before any clause runs, and gets ZERO sites for all twelve of: `out[12:] = []`, random.sample, random.choices, itertools.batched (3.12), itertools.compress, `.head(12)`, `[xs[i] for i in range(12)]`, bounded recursion, functools.reduce with a bounded accumulator, `list(zip(*[iter(xs)]*12))[0]`, operator.itemgetter(*range(12)), and a match/case structural bound. Zero sites means no clause was consulted - the operation was never classified at all, so these are not defended design decisions, they are unenumerated shapes reported CLEAN. Of 30 candidate operations probed, 18 were reported clean. probe_ab.py then pairs seven of them against a spelling that IS caught, with identical semantics: 7 of 7 pairs split. The 
- **failure scenario:** A contributor adds `out[12:] = []` or `random.sample(findings, 12)` to any hook that prints a findings list. slicing_offenders returns [], verdict() returns ok=True, capped_report --selftest exits 0, the pre-push gate passes, and the hook ships printing 12 of N findings with no truncation notice - the precise defect (five hooks independently capping without saying what they dropped) that capped_report.py was written to make impossible.
- **refuter:** REPRODUCED, with a clean A/B control, in a scratch copy at C:\Users\ammar\AppData\Local\Temp\claude\lens_grammar_closed_refute\. CONFIRMED FACTS (all run, not read): 1. Planting `out[12:] = []` into the real hook hooks/plan_defer_guard.py: cap_shapes.slicing_offenders -> 0 offenders, verdict() -> ok=True, `python hooks/capped_report.py --selftest` -> exit 0, SELFTEST OK. Baseline is also clean, so this is a true fail-open, not noise. 2. THE DECISIVE A/B: `del out[12:]` and `out[:12]`, semantically identical and planted at the SAME line in the SAME file, each produce 1 offender, verdict ok=Fals

### 4. [HIGH] _routed() is a two-name roster that disarms an entire function scope, including caps on unrelated collections

- **file:** `hooks/cap_shapes.py`  (finder rated HIGH, refuter corrected to HIGH)
- **detail:** _routed() returns True if ANY call whose callee name is `keep` or `render` appears anywhere in the scope, and line 436 then `continue`s past the ENTIRE function - every shape, every collection in it. The names are matched by attribute name or resolved alias only; there is no check that the routed object is the one being shortened, nor that the callee is capped_report. DEMONSTRATED, one planted module per case: bare cap (control) -> 1 offender same cap + an unrelated template.render(ctx) call -> 0 offenders same cap + a local parameter named `render` being called -> 0 offenders capped_report.render on list A + silent slice of list B -> 0 offenders capped_report.render on list A + a `break` cap destroying B's total -> 0 offenders The last two are the realistic ones: doing the right thing for one findings list buys silence for every other collection in the same function. The corpus's contro
- **failure scenario:** A hook that correctly routes its bullet list through capped_report.render() and, in the same function, slices a second list `sources[:12]` or breaks out of a scan is reported CLEAN. Equally, any function in any swept hook that calls a third-party .render() (a template, a formatter) is exempt from the entire cap guard.
- **refuter:** CONFIRMED and NEW. Every claimed case reproduces in a scratch copy using the project's own tooling. CODE: hooks/cap_shapes.py:406-414 `_routed()` returns True for ANY ast.Call whose func is an Attribute with attr in ("keep","render") or a Name resolving to those, anywhere in the scope. There is no check that the routed object is the shortened one, and no check that the callee is capped_report. Line 436 then `continue`s past the ENTIRE scope, skipping all four shape detectors and all of clauses 1-3. DEMONSTRATED (planted modules via cap_shapes.slicing_offenders): bare display cap -> 1 offender;

### 5. [HIGH] The index-bounds-check exclusion is structurally dead for every `while` loop - loop_vars is passed empty

- **file:** `hooks/cap_shapes.py`  (finder rated HIGH, refuter corrected to HIGH)
- **detail:** _loop_exit_sites handles ast.While with `_size_test_target(node.test, set())`. The bounds-check exclusion inside _size_test_target is `if any(_references(o, loop_vars) for o in others): continue` - with loop_vars = set(), _references can never return True, so the exclusion is unreachable on every while loop. Consequence: any `while i < len(xs):` index cursor over a list that is mutated anywhere in scope is reported as 'loops until the collection is under a bound'. Live: ntpath.normpath:545 (`i = 0; while i < len(comps): ... del comps[i]`), email/_parseaddr.py:243/400/498 (`while self.pos < len(self.field)`), tarfile.py:1558.
- **failure scenario:** Reproduce (fixtures.py case C): `def normalize(comps):\n i = 0\n comps.append('')\n while i < len(comps):\n if not comps[i]:\n i += 1\n continue\n i += 1\n return comps` -> 'loops until the collection is under a bound at line 4'. Nothing is dropped; the loop visits every element.
- **refuter:** CONFIRMED, not refuted. Structural: hooks/cap_shapes.py:347 passes set() as loop_vars for every ast.While; _references (hooks/cap_types.py:388-389) is any(... s.id in names), which with an empty set is identically False, so the index-bounds-check exclusion at cap_shapes.py:286 is unreachable from the While call site. Demonstrated, not just reasoned: (1) the claim's case C reproduces verbatim - "planted: normalize() loops until the collection is under a bound at line 4"; (2) end-to-end at the ship gate - baseline `python hooks/capped_report.py --selftest` returns SELFTEST OK rc 0, and after a 2

### 6. [HIGH] _name_of collapses attribute chains to their root, so any method calling self.<x>.append() marks `self` as a mutated collection

- **file:** `hooks/cap_types.py`  (finder rated MEDIUM, refuter corrected to HIGH)
- **detail:** _name_of() walks Subscript/Attribute down to the bare root Name, so `self.field` -> 'self' and `self.commentlist.append(...)` records 'self' in Facts.mutated. `while self.pos < len(self.field)` then resolves to target 'self', which is 'mutated', and _loop_exit_sites reports 'loops until the collection is under a bound' with `self` as the collection. Every stateful parser or cursor class is exposed. Live: email/_parseaddr.py AddrlistClass.gotonext:243, getdomain:400, getphraselist:498 - three of the 16 reports on Lib/email, all in one class, all from the same root collapse.
- **failure scenario:** Any class method that both (a) has a `while <attr-compare against len(self.something)>` cursor and (b) calls .append/.add/.update on any attribute of self, is reported. Reproduce by running slicing_offenders over Lib/email and reading _parseaddr.py:397-410: the loop advances self.pos through self.field and appends to self.commentlist; nothing is bounded and nothing is truncated.
- **refuter:** CONFIRMED and reproduced, and the severity is UNDER-rated rather than over-rated. REPRODUCTION (scratch copy, Python 3.12): running cap_shapes.slicing_offenders over Lib/email yields 16 reports including the three named: _parseaddr AddrlistClass.gotonext():243, getdomain():400, getphraselist():498, all "loops until the collection is under a bound". None of those functions bounds or truncates anything. MECHANISM PROVEN BY COUNTERFACTUAL, not by reading. Minimal fixture (a Cursor class with `while self.pos < len(self.field)` plus `self.notes.append(ch)`) reports the false site; deleting only the

### 7. [MEDIUM] The key design claim inverted: refusing to look at the BOUND makes a cut-at-a-found-index indistinguishable from a cap

- **file:** `hooks/cap_shapes.py`  (finder rated HIGH, refuter corrected to MEDIUM)
- **detail:** The module docstring's premise is that classifying the OPERATION is grammar-closed so the bound's spelling stops mattering. It does not stop mattering in the false-alarm direction. `xs[:i]` where `i = s.rfind(sep)` is a SPLIT AT A DELIMITER - it partitions, it does not cap - and the shape detector cannot tell it from `xs[:MAX_ROWS]` because it deliberately never reads the bound. This is the largest false family in the sweep: posixpath.dirname:184 (`i = p.rfind(sep)+1; head = p[:i]`), pathlib.PurePath.stem:624, pdb.do_clear:1034, pyclbr._readmodule:151, mimetypes.guess_type:145/147, cgi.parse_header:250, logging/handlers.py:1289, logging/__init__.py:1437, genericpath.commonprefix:94, posixpath.commonpath:566. The predecessor enumerated cap spellings and failed OPEN on the unenumerated; this refuses to look at the bound at all and fires LOUD on every bound that is not a cap. Same unsolved 
- **failure scenario:** Every string-partitioning helper in existence is reported. Reproduce (fixtures.py case G): `def dirname(p):\n i = p.rfind('/') + 1\n head = p[:i]\n return head` -> 'dirname() slices to a bound at line 3'.
- **refuter:** NOT REFUTED - the finding reproduces exactly and is not in the rowed set, but the severity is over-rated. REPRODUCED IN FULL. Running cap_shapes.slicing_offenders() over CPython 3.12 Lib returned 105 offenders including EVERY site the reviewer named, at the EXACT cited line numbers: posixpath:184 (dirname), posixpath:566 (commonpath), pathlib:624 (PurePath.stem), pdb:1034 (do_clear), pyclbr:151 (_readmodule), mimetypes:145/147 (guess_type), cgi:250 (parse_header), genericpath:94 (commonprefix), logging/__init__:1437, logging/handlers:1289. The claimed fixture-G output is verbatim: "pp: dirname

### 8. [MEDIUM] capped_report's shipped timing claim is 20x stale: '0.20s, under 1% of the cap' against a measured 3.8-4.7s and 31-37%

- **file:** `hooks/capped_report.py`  (finder rated MEDIUM, refuter corrected to MEDIUM)
- **detail:** The comment reads '[P14 D1] default share. Measured 2026-08-02: 0.20s, under 1% of the cap. This is the hook the 36s regression happened in, which is why it is wired first.' MEASURED today, five consecutive runs (three against a scratch copy, two against the repo as shipped): 4.39s / 35% 4.65s / 37% 4.00s / 32% 3.84s / 31% 4.67s / 37% 4.24s / 34% all reported by the module's own selftest_budget line: 'ok capped_report 4.xx s of 12.50s ... - 3x% used'. Stable across runs, so not a cold-start artifact. The budget gate still passes because the threshold is 12.50s, so nothing goes red; the only record of the drift is a comment asserting the opposite. This is the repo's recorded 'uncontrolled TIMING claims' class, in the hook whose comment explicitly says it is the one a 36s regression happened in.
- **failure scenario:** The selftest has silently consumed 20x its recorded share and is now a third of its budget. The next addition to cap_shapes' fixture list or the corpus pushes it past 12.5s and the failure arrives as a budget breach with no history, while every reviewer who read the comment believed there was 99% of headroom.
- **refuter:** NOT REFUTED. Confirmed by controlled measurement, but the reviewer's magnitudes are wrong and I corrected them. EVIDENCE (scratch copy + sibling git worktree at 0204224, the commit the comment was measured against; real repo never written to): 1. Interleaved A/B, alternating NEW(HEAD 5f23b2b)/OLD(0204224), with a CPU load probe, run twice at n=7: run 1: NEW 2.39s median (1.75-3.71), OLD 0.16s (0.12-0.27) = 14.9x, probe median 0.199s run 2: NEW 1.80s median (1.61-1.99), OLD 0.13s (0.11-0.15) = 13.8x, probe median 0.148s The OLD version REPRODUCES the recorded 0.20s baseline on this box today, s

### 9. [MEDIUM] `continue` counts as a loop exit, so a per-element filter reads as a truncation

- **file:** `hooks/cap_types.py`  (finder rated MEDIUM, refuter corrected to MEDIUM)
- **detail:** _contains_exit() returns True for ast.Continue as well as Break and Return. A `continue` does not stop the scan: the loop still visits every element and the real total survives, so the message 'stops the scan at a bound, destroying the real total' is factually wrong at every such site. Combined with _counter_test's acceptance of any ordering comparison against a non-small value, every range-guard filter in a loop is reported. Live: difflib.py:381 `for j in b2j.get(...): if j < blo: continue`, csv.py:415, nntplib.py:216.
- **failure scenario:** Reproduce (fixtures.py case J): `def r(js, blo, bhi):\n out = []\n for j in js:\n if j < blo:\n continue\n out.append(j)\n return out` -> 'stops the scan at a bound, destroying the real total at line 4'. Nothing is destroyed; every element of js is examined.
- **refuter:** NOT REFUTED - reproduced exactly, including all three cited stdlib line numbers. REPRODUCTION (scratch copy at %TEMP%\claude_lens_continue_exit_probe; repo untouched): Fixture case J verbatim -> "planted: r() stops the scan at a bound, destroying the real total at line 4 - route it through capped_report.keep() so the message names what it hid". Exactly the claimed output. MECHANISM CONFIRMED IN CODE: cap_types._contains_exit (line 97-103) returns True for ast.Continue. cap_shapes._loop_exit_sites (line 362-371) uses it as the gate for the If, then falls back to _counter_test when _size_test_ta

### 10. [MEDIUM] The exemption key has function-level, not site-level, granularity - a second unrelated cap inside an exempted function is silently pre-authorised, and the corpus entry named for that defect never reaches the roster

- **file:** `hooks/cap_shapes.py`  (finder rated HIGH, refuter corrected to MEDIUM)
- **detail:** `key = (module, qual, site.kind)` carries no line number and no site count, so ONE approved bound licenses an unbounded number of further shortening operations of the same kind inside the same function. DEMONSTRATED on a copy of the live hooks/: grafting a completely unrelated collection cap into the exempted numbers_match_on_write.index_sources() smuggled = [] for _row in _CALLER_ROWS: if len(smuggled) >= 10: break smuggled.append(_row) leaves the sweep at 0 offenders (unmodified copy: 0; with the graft: 0). The reason this was not caught is a decorative grader entry. tests/cap_spelling_corpus.py carries ('second_cap_under_an_approved_exemption', 'hooks/f.py', True, ...) - named for exactly this defect - but it is planted at hooks/f.py, and the only module any exemption key names is numbers_match_on_write. Measured: as shipped, hooks/f.py -> 2 offender(s) [scored CAUGHT] identical sourc
- **failure scenario:** A maintainer adds a new bounded scan to numbers_match_on_write.index_sources() or .matches_source() - the two functions with live exemptions. The cap is reported CLEAN by the guard, the corpus still scores 102/105, run_selftests prints 30/30, and the mutation sweep stays 115 CAUGHT / 0 SURVIVED, because no mutation and no corpus entry ever plants a second site under an approved key.
- **refuter:** NOT REFUTED - reproduced end to end in a scratch copy; the live repo is byte-unchanged (git status before/after identical: only the pre-existing "M docs/V131_REVIEW_PLAN.md"). EVIDENCE 1. hooks/cap_shapes.py:445 - `key = (module, qual, site.kind)`, no site/lineno/count component; ALL_EXEMPTIONS (BOUND_EXEMPTIONS + FIXTURE_EXEMPTIONS) is consulted per-site with that triple. 2. Airtight A/B, byte-identical source with TWO independent bounded scans in index_sources(), planted under two module names: numbers_match_on_write.py -> 0 offenders; some_other_hook.py -> 2 offenders (lines 4 and 9). The s

### 11. [MEDIUM] UnicodeDecodeError escapes both handlers: a valid PEP 263 source file crashes the entire sweep, and every other file in the directory goes unchecked

- **file:** `hooks/cap_shapes.py`  (finder rated HIGH, refuter corrected to MEDIUM)
- **detail:** slicing_offenders opens with a hardcoded encoding="utf-8" and catches exactly two things: OSError (line 481) and SyntaxError (line 486). UnicodeDecodeError is a subclass of ValueError and is neither, so it propagates out of the whole sweep. Python source is NOT required to be UTF-8: PEP 263 permits `# -*- coding: latin-1 -*-`. Verified with a file the interpreter itself accepts and executes correctly (`legit_hook.report(list(range(30)))` returns 12 items): slicing_offenders raises UnicodeDecodeError, and a sibling file in the same directory containing a real `xs[:99]` cap is never reported. Through the actual gate entrypoint, `capped_report.py --selftest` exits 1 with a bare traceback and no SELFTEST output at all. This directly contradicts the message the code itself prints one line below - "a file this guard cannot read is REPORTED, never silently skipped" - and the selftest's UNREADAB
- **failure scenario:** One hook carries a legal latin-1 coding declaration (or any non-UTF8 byte in a comment, an author name, a test fixture string). The cap guard stops checking the ENTIRE hooks directory - not just that file - and reports the failure as an unhandled traceback rather than as the promised per-file UNREADABLE finding. In score_corpus the same input is counted as neither caught nor missed and the process still exits 0.
- **refuter:** CORE DEFECT CONFIRMED, TWO OF ITS THREE CLAIMS REFUTED, SEVERITY OVERSTATED. REPRODUCED (scratch copy, all commands run): 1. hooks/cap_shapes.py:479-486 reads with encoding="utf-8" and catches only OSError (481) and SyntaxError (486). Planted a PEP 263 latin-1 file the interpreter itself accepts (importlib exec_module OK; legit_hook.report(list(range(30))) -> 12 items). cap_shapes.slicing_offenders(dir) raised UnicodeDecodeError: 'utf-8' codec can't decode byte 0xe9 in position 47, and the sibling file containing a real xs[:99] was never reported. 2. Through the cited entrypoint: converted the

### 12. [MEDIUM] hook_layers: a plugin whose name matches a marketplace or sibling directory component claims another plugin's hooks.json - the WRONG file is merged and a correct install is reported ambiguous

- **file:** `hooks/hook_layers.py`  (finder rated MEDIUM, refuter corrected to MEDIUM)
- **detail:** _matches() tests `name.lower() in parts` where parts is the SET of all path components. The docstring defends this against SUBSTRING collisions ('hookify' vs 'hookify-extras') but not against COMPONENT collisions. In the observed marketplace layout `plugins/marketplaces/<market>/plugins/<name>/hooks/hooks.json`, the marketplace directory is itself a component - so a plugin whose name equals its marketplace (the common org-flagship pattern: posthog@posthog, hookify@hookify) matches EVERY sibling plugin's hooks.json under that marketplace. plugin_layers then takes hits[0] by sort order, which is not necessarily the right one, and reports 'ambiguous' on a completely correct install. The merged sibling may be DISABLED, so its hooks never fire - which is precisely the B3-FP mode the module's own docstring says it exists to prevent ('feeding them in would have invented six plugins worth of wir
- **failure scenario:** Demonstrated: plant `plugins/marketplaces/posthog/plugins/posthog/hooks/hooks.json` and `plugins/marketplaces/posthog/plugins/aaa-tool/hooks/hooks.json`, enabledPlugins = {'posthog@posthog': True}. plugin_layers merges aaa-tool/hooks/hooks.json (sorts first) instead of posthog's, and reports 'posthog@posthog resolves to 2 copies on disk'. aaa-tool is not enabled, so every hook it declares is now a phantom layer feeding duplicate_registration_check.
- **refuter:** NOT REFUTED - reproduced on the production path in a scratch copy, and the harm is larger than the reviewer described. CONFIRMED MECHANISM. hook_layers._matches() tests `name.lower() in parts` where parts is the set of ALL path components. Planting `plugins/marketplaces/posthog/plugins/posthog/hooks/hooks.json` and a sibling `.../plugins/aaa-tool/hooks/hooks.json` with enabledPlugins={'posthog@posthog': True} yields _matches(sibling,'posthog','posthog')=True; plugin_layers merges ONLY aaa-tool (sorts first) and drops the real posthog file; report['ambiguous']=['posthog@posthog resolves to 2 co

### 13. [MEDIUM] A slice on the LEFT of an assignment (Store context) is reported as a shortening - ctx is never inspected

- **file:** `hooks/cap_shapes.py`  (finder rated HIGH, refuter corrected to MEDIUM)
- **detail:** _slice_sites() matches every ast.Subscript with an ast.Slice and never looks at node.ctx. `grep -c ctx hooks/cap_shapes.py hooks/cap_types.py` returns 0 and 0. A Store-context slice `b[:len(data)] = data` WRITES INTO a buffer; it shortens nothing and cannot hide anything, because the right-hand side is what supplies the data. This is the single commonest report family in the stdlib sweep: _compression.py:69 `byte_view[:len(data)] = data`, _pyio.py:1696 `m[:n] = data`, tarfile.py:703 `b[:len(buf)] = buf`, base64.py:245 `decoded[-5:] = last[:leftover]`. Four of the 21 sampled sites.
- **failure scenario:** Any readinto()-style buffer protocol implementation is reported as hiding data. Reproduce (fixtures.py case A): plant `def readinto(b, src):\n data = src.read(len(b))\n b[:len(data)] = data\n return len(data)` and slicing_offenders reports 'slices to a bound at line 3'.
- **refuter:** CONFIRMED, not refuted, but over-rated. hooks/cap_shapes.py:143 filters only `isinstance(node, ast.Subscript)` and never inspects node.ctx; grep for `ctx` over the pristine hooks/cap_*.py returns 0 occurrences. Reproduced the reviewer's exact case in a scratch copy: `def readinto(b, src): data = src.read(len(b)); b[:len(data)] = data; return len(data)` yields "planted: readinto() slices to a bound at line 3". Isolated the mechanism: `def r(m, n, data): m[:n] = data; return m` reports, the identical function minus the store-slice is CLEAN, and the flagged Subscript's ctx is Store. The AugAssign

### 14. [MEDIUM] score_corpus.main() can never fail: a guard that detects nothing, crashes on all 135 entries, or returns a generator all exit 0

- **file:** `tools/score_corpus.py`  (finder rated HIGH, refuter corrected to MEDIUM)
- **detail:** main() prints and falls off the end, so `raise SystemExit(main())` (line 241) exits 0 unconditionally. There is no threshold, no comparison against a recorded baseline, and no non-zero path. The only thing wired into run_selftests.py (line 80, AUX gate `corpus-scorer`) is `--selftest`, which checks the tool's arithmetic and never runs a guard. The scoring half of the scorer - the half that produces 102/105, the figure quoted in capped_report.py, cap_shapes.py, noregress_waivers.py and the plan - is executed by no gate and cannot fail if it is. DEMONSTRATED, three synthetic guards scored against the live corpus: slicing_offenders -> [] CAUGHT 0/105 MISSED 105 CRASHED 0 EXIT=0 slicing_offenders -> raise RuntimeError CAUGHT 0/105 MISSED 0 CRASHED 105 EXIT=0 slicing_offenders -> (x for x in []) CAUGHT 105/105 MISSED 0 FALSE-POS 30 EXIT=0 The third is the dangerous one and answers the brief's
- **failure scenario:** score_corpus is promoted from a hand-run tool to a CI step (which already happened once, on 2026-08-06, and is what exposed the hardcoded C:\ path). `python tools/score_corpus.py hooks/capped_report.py` is added to the pipeline as the guard's regression check. cap_shapes is later refactored to yield offenders instead of returning a list; the step prints CAUGHT 105/105 and exits 0 while the guard flags every file in the repo, or a merge leaves slicing_offenders raising and the step prints CRASHED 135 and exits 0. In both cases the ship-blocking guard's own grade is green on a guard that has sto
- **refuter:** CONFIRMED, reproduced in an isolated copy at C:\Users\ammar\AppData\Local\Temp\claude\C--Users-ammar-Downloads-Claude\3ce9eb90-5173-4238-b390-b3aa551647f4\scratchpad\lens_scorer_exit0. Original repo untouched (git status shows only the pre-existing docs/V131_REVIEW_PLAN.md modification). WHAT I REPRODUCED, running the project's own tool against the live 135-entry corpus. Baseline C:\Users\ammar\Downloads\unbluff\hooks\capped_report.py = CAUGHT 102/105, FALSE-POS 0/30, EXIT=0. Three synthetic guards, all EXIT=0: slicing_offenders -> [] CAUGHT 0/105 MISSED 105 EXIT=0 slicing_offenders -> raise R

### 15. [MEDIUM] Four shipped docstrings state the current corpus score with denominators and scores that are all wrong

- **file:** `hooks/cap_shapes.py`  (finder rated MEDIUM, refuter corrected to MEDIUM)
- **detail:** Every one of these is written in the present tense as the module's measured standing, not as dated history. Actual, from `python tools/score_corpus.py hooks/capped_report.py`: 'POSITIVES CAUGHT 102/105', 'NEGATIVES CLEAN 30/30', 'DENOMINATOR: 105 positives + 30 negatives = 135 corpus entries'; predecessor is 38 of 105. hooks/cap_shapes.py:25-28 'MEASURED against tests/cap_spelling_corpus.py, 132 entries: CAUGHT 93 of 96 ... FALSE-POS 0 of 29 ... against a predecessor at 31 of 96' -> five wrong numbers: 132/135, 93/102, 96/105, 29/30, 31/38. hooks/cap_shapes.py:386 '...no corpus entry covers it - every one of the 125 wraps its cap in a function' -> 135. hooks/capped_report.py:86-88 'MEASURED against tests/cap_spelling_corpus.py: predecessor 38 of 103 with 1 false positive; this 100 of 103 with 0.' -> 105 and 102. tools/score_corpus.py:87-88 'MEASURED when this was written: THREE such pair
- **failure scenario:** A reviewer comparing a future run's 102/105 against the module's own claim of '93 of 96' or '100 of 103' concludes the guard has regressed or that the corpus shrank, and re-derives the ceiling by hand. Because these numbers are the ship-blocking claim for the whole C1-NEW rebuild, the stale ones are the ones most likely to be quoted into a plan row.
- **refuter:** CONFIRMED by running the project's own tooling in a scratch copy (C:\Users\ammar\AppData\Local\Temp\claude\C--Users-ammar-Downloads-Claude\3ce9eb90-5173-4238-b390-b3aa551647f4\scratchpad\lens_docstale_probe) at HEAD 5f23b2b. No files under C:\Users\ammar\Downloads\unbluff were written; the pre-existing "M docs/V131_REVIEW_PLAN.md" in git status predates this run (only cp/grep/git status were run there). MEASUREMENTS: - `python tools/score_corpus.py hooks/capped_report.py` -> POSITIVES CAUGHT 102/105, NEGATIVES CLEAN 30/30, "DENOMINATOR: 105 positives + 30 negatives = 135 corpus entries", CEILI

### 16. [MEDIUM] expr_kind reads only INSIDE the slice, so s[:12].lower() is unknown and flags

- **file:** `hooks/cap_types.py`  (finder rated MEDIUM, refuter corrected to MEDIUM)
- **detail:** _slice_sites passes `node.value` (the object being sliced) as Site.expr, and expr_kind classifies that expression alone. When the scalar evidence sits on the OUTSIDE of the slice - `orig_enc[:12].lower().replace('_','-')` - expr_kind sees the bare Name `orig_enc`, Facts.kind returns 'unknown', and unknown flags. The module already knows to read expressions rather than root names (that is the stated reason expr_kind exists), but only in one direction. Live: tokenize.py:352 `enc = orig_enc[:12].lower().replace('_', '-')`.
- **failure scenario:** Reproduce (fixtures.py case K): `def r(orig_enc):\n enc = orig_enc[:12].lower().replace('_', '-')\n return enc` -> 'slices to a bound at line 2'. The `.lower()` immediately following the slice is conclusive evidence the sliced value is a str.
- **refuter:** NOT REFUTED - reproduced exactly, and the decisive evidence is an order-dependence the claim did not even state. REPRODUCTION (scratch copy, `cap_shapes.slicing_offenders` on a one-file probe dir): - `def r(orig_enc): enc = orig_enc[:12].lower().replace('_','-'); return enc` -> "m: r() slices to a bound at line 2 - route it through capped_report.render()" Confirmed verbatim as the reviewer predicted. WHY IT IS A RULE GAP AND NOT THE DOCUMENTED "unknown flags, by design" CHOICE - the same value and the same two operations, verdict flips on ORDER alone: clean o.lower()[:12] (evidence INSIDE the 

### 17. [MEDIUM] The pad-with-constants rule requires literal constants, so padding with a computed filler is not suppressed

- **file:** `hooks/cap_shapes.py`  (finder rated MEDIUM, refuter corrected to MEDIUM)
- **detail:** _pads_with_constants() requires every argument of every append/extend/insert to be an ast.Constant. Padding a list up to a minimum with a variable, a parameter, or a default object grows the list exactly as much as padding with '' does - nothing is dropped either way - but only the literal form is suppressed. The rule is documented as derived from one live site (duplicate_registration_check._parts) and was fitted to that site's exact spelling.
- **failure scenario:** Reproduce (fixtures.py case M): `def r(entry, filler):\n bits = entry.split('|')\n while len(bits) < 6:\n bits.append(filler)\n return bits` -> 'loops until the collection is under a bound at line 3'. The list only ever grows.
- **refuter:** CONFIRMED, not refuted, and demonstrated by running the project's own tooling in a scratch copy (C:\Users\ammar\AppData\Local\Temp\claude\lens-padconst-M\unbluff). The real repo was never written to. 1. The claimed fixture reproduces verbatim: `def r(entry, filler): bits = entry.split('|'); while len(bits) < 6: bits.append(filler); return bits` -> "planted: r() loops until the collection is under a bound at line 3". The list only ever grows; nothing is hidden. 2. The mechanism is as described: _pads_with_constants (hooks/cap_shapes.py:299-311) is `all(all(isinstance(a, ast.Constant) for a in c

### 18. [MEDIUM] exemption_problems() fails open: it audits which keys were SEEN, not which verdicts would CHANGE

- **file:** `hooks/cap_shapes.py`  (finder rated HIGH, refuter corrected to MEDIUM)
- **detail:** The docstring (lines 496-508) states the contract twice: 'the roster is audited by REMOVING it and asking which of its entries the detector would actually have flagged. An entry nothing would flag is dead weight and is REPORTED', and 'this cannot fail open'. The implementation does something weaker. `keys` is populated at line 446-447, immediately after `key = (module, qual, site.kind)` and BEFORE clause 2 (line 451), clause 1 (line 456) and clause 3 (line 461) get to suppress the site. So `seen` is the set of keys that reached the roster CHECK - a strict superset of the keys that would have produced an offender. DEMONSTRATED, planted module m.py: MAX_B = 200 def f(fh): text = fh.read() # clause 1 -> scalar, never reported return text[:MAX_B] slicing_offenders(d, use_roster=False) -> [] slicing_offenders(d) -> [] (removing the exemption changes NOTHING) exemption_problems(d, {('m','f','d
- **failure scenario:** Someone exempts a site, later fixes the underlying code so clause 1/2/3 suppress it anyway (e.g. the value becomes a str, or the slice moves inside a message format). The exemption is now pure dead cover, but exemption_problems() still sees the shape reach the roster check and reports the roster clean forever. The next collection cap added at that (module, qualname, kind) is silently pre-authorised.
- **refuter:** CONFIRMED and NEW; severity corrected HIGH -> MEDIUM. MECHANISM (read in hooks/cap_shapes.py): at lines 444-448 `keys.add(key)` runs immediately after `key = (module, qual, site.kind)` and BEFORE clause 2 (451), clause 1 (456) and clause 3 (461) can suppress the site. `exemption_problems()` (495-520) calls `slicing_offenders(hooks_dir, use_roster=False, keys=seen)` -- it genuinely runs the removal experiment, but then DISCARDS the returned offender list and reads `seen` instead. `seen` is therefore the set of keys that REACHED the roster check, a strict superset of the keys that would have pro

### 19. [MEDIUM] score_corpus.run_entry makes rel_path decorative, crediting the guard with 3 detections production cannot make

- **file:** `tools/score_corpus.py`  (finder rated HIGH, refuter corrected to MEDIUM)
- **detail:** run_entry plants the entry at rel_path and then calls `guard.slicing_offenders(os.path.join(td, os.path.dirname(rel_path)))` - it points the guard AT the entry's own directory. Production never does that: capped_report.selftest() calls slicing_offenders(<hooks dir>) once, and cap_shapes.slicing_offenders globs `hooks_dir/*.py` non-recursively. So the directory component of rel_path can never change the answer, and the corpus contract's stated purpose for the field ('where to plant it, so directory-scoped guards can be exercised') is unexercisable by construction. The corpus contains exactly three positives that differ from `plain_assign_slice` ONLY by directory - `module_in_subdirectory` (hooks/lib/f.py), `module_in_tools` (tools/f.py), `module_in_skill_scripts` (skills/s/scripts/f.py). All four carry the byte-identical source `MAX_BULLETS = 12\ndef report(xs):\n return xs[:MAX_BULLETS]\
- **failure scenario:** A reviewer reads 'CAUGHT 102/105 at zero false positives, which is the arithmetic ceiling' and concludes the guard has no coverage gap. Three of those 102 are entries whose entire reason for existing is to ask whether the guard sees a module outside hooks/, and the guard sees none of them. Worse in the other direction: a future guard that correctly declines to flag a MAX_BULLETS slice inside skills/s/scripts/ (not a hook) is scored as a MISS, so the corpus actively penalises correct directory-awareness. Adding more location-sensitive entries cannot improve the measurement - each new one is sco
- **refuter:** CORE CLAIM STANDS, DEMONSTRATED AND REPRODUCIBLE - but the finding is over-rated at HIGH and four of its supporting arguments are individually refutable. WHAT I REPRODUCED (probe_a.py / probe_b.py / probe_c.py / probe_d.py in scratch): 1. tools/score_corpus.py:42 - `hooks_dir = os.path.join(td, os.path.dirname(rel_path))` - the guard is always handed the LEAF directory the fixture sits in. All four of plain_assign_slice, module_in_subdirectory, module_in_tools, module_in_skill_scripts carry byte-identical source and all four score CAUGHT. 2. The sharpest evidence, which the reviewer did not pr

### 20. [MEDIUM] FIXTURE_EXEMPTIONS suppresses a REAL site in hooks/ and is structurally excluded from the only liveness audit

- **file:** `hooks/cap_shapes.py`  (finder rated HIGH, refuter corrected to MEDIUM)
- **detail:** FIXTURE_EXEMPTIONS is documented (lines 61-71) as holding corpus fixtures only - 'a fixture module is never IN [a real directory]' - and is therefore deliberately excluded from exemption_problems(), whose default roster is BOUND_EXEMPTIONS (line 510). But ALL_EXEMPTIONS (line 80-81) is what _module_sites() consults, so both fixture keys bite on the live tree, and one of them MATCHES A LIVE SITE. DEMONSTRATED (scratch copy of hooks/, nothing else changed): shipped ALL_EXEMPTIONS -> 0 offender(s) FIXTURE roster REMOVED -> 1 offender(s) numbers_match_on_write: run() stops the scan at a bound, destroying the real total at line 432 and keyed instrumentation confirms it is not incidental: numbers_match_on_write / run / collection live-site-present=True numbers_match_on_write / scan / collection live-site-present=False The audit that exists to catch exactly this returns nothing: exemption_probl
- **failure scenario:** A report with more than 500 unmatched cited numbers makes numbers_match_on_write.run() break at 500; build_message announces '500 cited number(s) ... have no match' as the real total and the per-session marker then silences the hook, so the remainder are never mentioned. cap_shapes flags this site, and the only thing stopping it is a FIXTURE_EXEMPTIONS key that exemption_problems() can never audit and that cap_shapes' own selftest asserts is dead. All six selftests print SELFTEST OK.
- **refuter:** CONFIRMED, and not already rowed (not among B1-C2FLOW / B1-SCOPE / B1-AC3 / M2-ledger / POSITIONAL_FLOOR; `MAX_FINDINGS_TRACKED` has 0 hits in docs/V131_REVIEW_PLAN.md). REPRODUCED EXACTLY (scratch copy at %TEMP%\claude\refute_fixture_exempt_live_site): - shipped ALL_EXEMPTIONS -> 0 offenders in hooks/; FIXTURE_EXEMPTIONS removed -> 1 offender: "numbers_match_on_write: run() stops the scan at a bound, destroying the real total at line 432". - keyed instrumentation via slicing_offenders(keys=): ('numbers_match_on_write','run','collection') live-site-present=True; ('numbers_match_on_write','scan

### 21. [MEDIUM] zip(range(len(xs)), xs) - the standard enumerate() substitute - is reported; the range bound is never compared to the sequence

- **file:** `hooks/cap_shapes.py`  (finder rated MEDIUM, refuter corrected to MEDIUM)
- **detail:** _zip_range_target() returns (sequence, True) whenever a zip() call has at least one range() argument and one non-range argument. It never asks whether the range bound is len() of that same sequence, in which case the zip truncates nothing at all and is simply how pre-enumerate code numbered its items. Live: email/message.py:589 `for i, (k, v) in zip(range(len(self._headers)), self._headers)`. Separately, the non-comprehension branch at line 257 names the target as `others[0]` but passes `node.args[0]` as the expr for clause 1 - when range() is the first argument those are different nodes, so clause 1 is applied to the range() call rather than to the sequence.
- **failure scenario:** Reproduce (fixtures.py case B): `def r(headers, name):\n out = []\n for i, (k, v) in zip(range(len(headers)), headers):\n out.append((i, k))\n return out` -> 'cuts the sequence with zip(range(...)) at line 3'. The loop yields every header.
- **refuter:** CONFIRMED - could not refute; both halves reproduce by running the repo's own code in a scratch copy (...\scratchpad\lens_zipRangeEnum), with the real repo left untouched. HALF 1 (the false alarm). The claim's exact fixture: def r(headers, name): out = [] for i, (k, v) in zip(range(len(headers)), headers): out.append((i, k)) return out cap_shapes.slicing_offenders() returns exactly the predicted message: "fixtures: r() cuts the sequence with zip(range(...)) at line 3 - route it through capped_report.render() so the message names what it hid". The loop yields every header; nothing is hidden, so

### 22. [MEDIUM] SANCTIONED_MODULES skips capped_report.py before it is read, so the guard's own stated 'never silently skipped' invariant does not hold for the module the whole design routes through

- **file:** `hooks/cap_shapes.py`  (finder rated HIGH, refuter corrected to MEDIUM)
- **detail:** SANCTIONED_MODULES = {'capped_report'} is consumed at lines 476-477, BEFORE the file is opened (line 479) and before it is parsed (line 485). The two branches that make this guard fail closed - 'UNREADABLE (%s) - nothing in it was checked' and 'does not parse (%s) - a file this guard cannot read is REPORTED, never silently skipped' - are therefore unreachable for this module. DEMONSTRATED on a copy of hooks/: appended a display cap AND a collection cap to capped_report.py -> 0 offender(s) def audit_rows(rows): kept = [] for r in rows: if len(kept) >= 25: break kept.append(r) return kept[:12] then appended a syntax error to capped_report.py -> 0 offender(s) A file that does not parse is silently skipped, in the one module named in the exemption. The roster is keyed by basename only, has no liveness check of any kind (exemption_problems() does not look at it), and no mutation entry targets
- **failure scenario:** Any cap added anywhere in capped_report.py - the module every other hook is told to route through - is invisible to the guard that enforces the routing. Worse, if capped_report.py is left syntactically broken by an edit, slicing_offenders() reports the tree clean rather than reporting the unparseable file, which is the precise fail-open the rebuild's docstring claims to have closed.
- **refuter:** CANNOT REFUTE THE CORE. The mechanism and both probes reproduce exactly, and the finding is NEW (not in the rowed set). But the reviewer's headline harm - the unparseable half - is refuted at the system level, which is what drives the severity correction from HIGH to MEDIUM. WHAT IS CONFIRMED (all run in an isolated scratch copy, hooks/cap_shapes.py:474-478): for path in sorted(glob.glob(...)): module = os.path.splitext(os.path.basename(path))[0] if module in SANCTIONED_MODULES: # line 476 - BEFORE open() at 479, BEFORE ast.parse() at 485 continue probe1 - reviewer's exact audit_rows() (a coll

### 23. [MEDIUM] _callee_sites is a closed roster of library callee NAMES - the predecessor's enumerate-the-spellings disease, moved from the bound to the callee

- **file:** `hooks/cap_shapes.py`  (finder rated HIGH, refuter corrected to MEDIUM)
- **detail:** The design claim is that classifying the OPERATION escapes the unbounded-enumeration trap that classifying the BOUND fell into. But _ITERTOOLS_SHORTENERS = {"islice", "takewhile"}, _HEAPQ_SHORTENERS = {"nlargest", "nsmallest"} and the literal {"deque"} at line 194 are a hardcoded list of library function names, and every callee not on it fails open. Measured misses, all with zero sites produced: random.sample(xs, 12), random.choices(xs, k=12), itertools.compress(xs, sel), and - most tellingly - itertools.batched(xs, 12), which is in the SAME MODULE as the two names that are enumerated and is new in Python 3.12, the interpreter this repo runs on. probe_ab.py shows the split directly: heapq.nsmallest FLAG / random.sample CLEAN; itertools.islice FLAG / itertools.batched CLEAN. The stdlib grows shorteners over time (batched landed in 3.12), so this roster is guaranteed to rot in the fail-ope
- **failure scenario:** A contributor writes `return random.sample(findings, MAX_BULLETS)` or, on 3.12, `return list(itertools.batched(findings, MAX))[0]`. Both silently return 12 of N with no notice; both sweep clean. When Python 3.14 adds another bounded-iterator helper, the guard silently loses coverage again with no test going red and no denominator changing.
- **refuter:** NOT REFUTED at the core, but half the cited evidence is wrong and the severity is over-rated. All work in scratch copy C:\Users\ammar\AppData\Local\Temp\claude\...\scratchpad\lens_callee_roster; the real repo was only read (git status/rev-parse); its one dirty file (docs/V131_REVIEW_PLAN.md) is pre-existing. WHAT SURVIVES (reproduced by running hooks/cap_shapes.slicing_offenders on planted modules): _callee_sites (hooks/cap_shapes.py:179-198) matches only _ITERTOOLS_SHORTENERS={"islice","takewhile"}, _HEAPQ_SHORTENERS={"nlargest","nsmallest"} and {"deque"}+maxlen; every other callee falls thro

### 24. [MEDIUM] _import_aliases has no scoping: a local name in one function renames a callee in an unrelated function, producing a false positive on correct code

- **file:** `hooks/cap_types.py`  (finder rated MEDIUM, refuter corrected to MEDIUM)
- **detail:** _import_aliases walks the whole module with ast.walk and builds ONE flat {name -> leaf} map with no notion of scope, then _is_call_to consults it for every call in every function. So a local `take = itertools.islice` inside one function makes the bare name `take` resolve to islice everywhere in the module. Demonstrated: a module where summarise() does `take = itertools.islice; return list(take(xs, 12))` (a true positive, correctly flagged) and an unrelated build_all(rows, take) takes `take` as a caller-supplied predicate and returns `[take(r) for r in rows]` - every row transformed, nothing shortened. The sweep reports BOTH: "build_all() shortens via islice at line 10". The alias map is also unconditional on shadowing, so a parameter name silently inherits a module-level binding. This is the B3-FP class the repo has already recorded once (making a guard see more made it report ~15 false 
- **failure scenario:** Any module that both uses a short local alias for a shortener and takes a caller-supplied callable under the same common name (take, sample, filter, batch, compress) gets a false 'shortens via ...' finding on a function that shortens nothing. The developer cannot silence it without either renaming an innocent parameter or adding a BOUND_EXEMPTIONS entry - which then rots into pre-authorisation for whatever is added at that site later, the exact roster failure exemption_problems() exists to prevent.
- **refuter:** CONFIRMED, reproducible, and new. hooks/cap_types.py:106-148 `_import_aliases` walks the module with bare ast.walk twice and returns one flat {name -> leaf} map, while every other collector in the same file (Facts, _message_context_nodes, _aggregated_nodes) is built per-scope via _walk_skipping_functions, whose own docstring says a nested def "is its own scope". So the unscoped map is an internal inconsistency, not a stated design choice. DEMONSTRATED in a scratch copy (C:\Users\ammar\AppData\Local\Temp\claude\lens_import_alias_scope\unbluff, probes in .../probes). The filed case reproduces ve

### 25. [LOW] hook_layers._matches() tests membership in the SET of path components, so a plugin named after any path component claims every other plugin's hooks.json

- **file:** `hooks/hook_layers.py`  (finder rated LOW, refuter corrected to LOW)
- **detail:** `parts = {p.lower() for p in path.replace('\\','/').split('/')}` then `name.lower() in parts`. Every plugin hooks.json path contains the components 'plugins', 'cache' or 'marketplaces', the marketplace name, the version directory, and 'hooks'. Any plugin whose NAME equals one of those matches all of them. DEMONSTRATED with two unrelated installed plugins (alpha, beta) under plugins/cache/mkt/<name>/1.0.0/hooks/hooks.json: plugin named hooks matches 2 of 2 plugin named cache matches 2 of 2 plugin named plugins matches 2 of 2 plugin named mkt matches 2 of 2 plugin named 1.0.0 matches 2 of 2 enabledPlugins={'hooks@mkt': True} with NO plugin named 'hooks' installed -> layers merged = 1 (it belongs to alpha), ambiguous=['hooks@mkt resolves to 2 copies on disk; using plugins/cache/mkt/alpha/1.0.0/hooks/hooks.json'] Mitigated, not silent: the multi-copy path reports `ambiguous`. But a plugin wh
- **failure scenario:** An enabled plugin whose name collides with a path component (most plausibly a plugin sharing its marketplace's name, e.g. 'mkt@mkt', or a plugin literally named 'hooks') resolves to another plugin's hooks.json. duplicate_registration_check then attributes that plugin's hook wirings to the wrong plugin, and may report a correct config as duplicated - the B3-FP false-alarm mode this module exists to avoid.
- **refuter:** CONFIRMED by execution, and the finding is stronger than reported. MECHANISM REPRODUCED. hooks/hook_layers.py:96-99 builds `parts = {p.lower() for p in path.replace("\\","/").split("/")}` and tests `name.lower() in parts`. Running _matches directly against two unrelated plugin paths (plugins/cache/mkt/<name>/1.0.0/hooks/hooks.json) reproduces the reporter's table exactly: names `hooks`, `cache`, `plugins`, `mkt`, `1.0.0` each match 2 of 2; `alpha` matches 1 of 2 and `zeta` 0 of 2 (correct). The substring defence the docstring actually claims still holds - _matches(".../alpha-extras/...", "alph

### 26. [LOW] The structural-split rule only recognises the two-piece form; a three-way split is not suppressed

- **file:** `hooks/cap_shapes.py`  (finder rated MEDIUM, refuter corrected to LOW)
- **detail:** _structural_split_names() collects heads only from `x[:k]` (upper set, lower None) and tails only from `x[k:]` (lower set, upper None), then requires the dumped bound expressions to intersect. A middle segment `x[k:k+m]` has BOTH lower and upper, so it is recorded as neither head nor tail - and the real tail `x[k+m:]` has a bound (`k+m`) that does not match the head's (`k`). A three-way partition therefore leaves the head slice unsuppressed even though the function demonstrably keeps everything. Live: inspect.py:2421 `positional = arg_names[:pos_count]` followed by `keyword_only = arg_names[pos_count:pos_count+keyword_only_count]`.
- **failure scenario:** Reproduce (fixtures.py case I): `def r(names, npos, nkw):\n positional = names[:npos]\n kwonly = names[npos:npos + nkw]\n rest = names[npos + nkw:]\n return positional, kwonly, rest` -> 'slices to a bound at line 2'. All three pieces are returned; nothing is hidden.
- **refuter:** NOT REFUTED - the defect reproduces exactly as written, but MEDIUM is over-rated; it is LOW. CONFIRMED BY RUNNING. In a scratch copy (C:\Users\ammar\AppData\Local\Temp\claude\lens_split3way\unbluff; the original repo was never written to) I ran cap_shapes.slicing_offenders() over planted probes: - Two-way split `head = names[:npos]; tail = names[npos:]` -> QUIET (the suppression fires). - The reviewer's exact three-way fixture -> "m: r() slices to a bound at line 2 - route it through capped_report.render()". - Same three-way shape with `names = list(raw)` so clause 1 resolves to 'collection' -

### 27. [LOW] check_mutation_anchors crashes with an unhandled UnicodeDecodeError on a VALID anchor in a non-UTF-8 unit

- **file:** `tools/mutation_check.py`  (finder rated MEDIUM, refuter corrected to LOW)
- **detail:** anchor_audit() opens each unit with `open(path, encoding='utf-8')` inside `except OSError`. UnicodeDecodeError is a subclass of ValueError, not OSError, so a unit file containing any non-UTF-8 byte propagates a traceback out of anchor_audit, past verdict(), and out of check_mutation_anchors.main(). The gate's stated contract - 'Fails CLOSED. A unit that cannot be read is a PROBLEM, not a skip, and its anchors stay in the denominator' - is bypassed: instead of the designed 'cannot read' problem line with the anchor still counted, the operator gets a stack trace and no denominator at all. The selftest at check_mutation_anchors.py:134 exercises only the ABSENT-file path (which does raise OSError); no fixture plants an undecodable unit.
- **failure scenario:** Demonstrated: plant hooks/planted.py containing the bytes `# caf\xe9\ndef alpha():\n return 1\n` and call anchor_audit(td, [('planted','P-OK','control',[(' return 1',' return 9')], False)]). The anchor is present and valid; the call raises UnicodeDecodeError: 'utf-8' codec can't decode byte 0xe9 in position 5.
- **refuter:** The MECHANISM reproduces exactly as described, so I cannot refute the finding outright - but its stated IMPACT ("the fail-CLOSED contract is bypassed") is demonstrably false, and MEDIUM is over-rated. REPRODUCED: anchor_audit() at tools/mutation_check.py:604-608 wraps `open(path, encoding="utf-8")` in `except OSError`. UnicodeDecodeError subclasses ValueError, so it propagates. Confirmed with the reviewer's synthetic fixture AND by planting a latin-1 byte into a real MUTATIONS unit (hooks/capped_report.py): `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xe9 in position 71`, traceback ex

### 28. [LOW] hook_layers._PRUNE silences enabled plugins by directory NAME, in a module whose docstring forbids exactly that kind of silent narrowing

- **file:** `hooks/hook_layers.py`  (finder rated MEDIUM, refuter corrected to LOW)
- **detail:** _PRUNE = {'node_modules', '.git', '__pycache__', 'dist', 'build'} is applied at every level of the os.walk (line 80), and the comment justifies name-pruning over depth-pruning because 'a depth cap would silently stop finding real plugins the day a marketplace nests one level deeper, and this module must not narrow in silence.' The name prune narrows in exactly that way for two ordinary build-output directory names. DEMONSTRATED, one enabled plugin per case: hooks.json at plugins/cache/mkt/alpha/1.0.0/dist/hooks.json -> on_disk=0 layers=0 no_hooks_file=['alpha@mkt'] hooks.json at .../build/hooks.json -> on_disk=0 layers=0 no_hooks_file=['alpha@mkt'] hooks.json at .../hooks/hooks.json -> on_disk=1 layers=1 hooks.json at .../lib/hooks.json -> on_disk=1 layers=1 and a plugin whose own directory is named build/dist/node_modules is equally invisible. The miss is not reportable. It lands in `no
- **failure scenario:** A compiled plugin (TypeScript, bundled) that emits its hooks.json under dist/ or build/ is enabled and its hooks fire, but duplicate_registration_check never reads it as a layer. A hook wired both by that plugin and by settings.json reports CLEAN - the identical B3-P failure this module was written to end, one directory name later.
- **refuter:** NOT REFUTED as a defect, but over-rated and partly mis-described. Verified by running code in an isolated scratch copy (no writes to the real repo). CONFIRMED BY EXECUTION: 1. hooks/hook_layers.py:80 applies _PRUNE at every os.walk level. Probe of plugin_layers(): an enabled plugin's hooks.json at .../1.0.0/dist/hooks.json or .../build/hooks.json yields on_disk=0, layers=0, no_hooks_file=['alpha@mkt']; controls at hooks/, lib/, src/ and the version-dir root all yield on_disk=1 layers=1. A plugin whose own directory is named build/dist is likewise invisible (case-sensitively: 'Build'/'BUILD' ar

### 29. [LOW] check_mutation_anchors reports OK for a multi-edit mutation that the sweep cannot apply - the gate's rule diverges from run()'s

- **file:** `tools/check_mutation_anchors.py`  (finder rated MEDIUM, refuter corrected to LOW)
- **detail:** The module docstring's headline claim is that this gate catches 'a mutation that cannot run' in under a second instead of after a ~25-minute sweep, and lines 19-22 argue that sharing `missing_anchors()` with the harness is what stops the gate speaking for a program nobody runs. The sharing is only half the rule. `anchor_audit` (mutation_check.py:615) checks every `find` in an entry against the ORIGINAL file text. `run()` (mutation_check.py:685-688) applies the edits SEQUENTIALLY and re-checks each `find` against the text as already mutated by the preceding edits. For any entry whose edits interact, the two disagree. DEMONSTRATED (probe4.py, planted unit + planted entry with two edits): edits = [('def f():\n return 1', 'def f():\n return 2'), (' return 1', ' return 9')] check_mutation_anchors.verdict(...) -> (0, 'mutation-anchors: OK - 2 anchor(s) across 1 mutation entr(ies) in 1 file(s) 
- **failure scenario:** A future two-edit mutation is added where the first replacement rewrites a line the second anchors to (the natural shape when both edits touch one function). check_mutation_anchors runs on every stop and prints 'mutation-anchors: OK - N anchors ... still match their source'. The mutation is unrunnable from the day it is written. Nobody hears about it until the next full CI sweep reports HARNESS ERROR - which is precisely the window this gate was built to close, and the operator has an explicit green tick telling them it is closed.
- **refuter:** MECHANISM: CONFIRMED, and I could not refute it. I reproduced it end-to-end by calling `mutation_check.run()` itself (not a replica of its loop) in a scratch copy, against a planted unit `hooks/planted_seq.py`, with a matched negative control: GATE -> (0, 'mutation-anchors: OK - 2 anchor(s) across 1 mutation entr(ies) in 1 file(s) still match their source') RUN -> HARNESS ERROR: mutation anchor not found: ' return 1' CTRL GATE -> (0, ...OK...) # same unit, NON-interacting edits CTRL RUN -> CAUGHT (rc=1) The control rules out the fixture: the divergence is caused by the edits interacting, not b

### 30. [LOW] The NOT_A_GATE justification for score_corpus.py outlived its entry and now reads as the rationale for make_hook_screenshot.py; classify_tools cannot see a double-listed exemption

- **file:** `run_selftests.py`  (finder rated LOW, refuter corrected to LOW)
- **detail:** score_corpus.py was promoted into AUX_GATES as ('corpus-scorer', ('tools','score_corpus.py'), ('--selftest',)) at line 80. Its five-line NOT_A_GATE justification was not removed with it. Verified live: NOT_A_GATE entries: ['compare_delivery_gate.py', 'make_hook_screenshot.py', 'measure_dispatcher_cost.py', 'mutation_check.py'] score_corpus.py in NOT_A_GATE? False in AUX_GATES? True Lines 89-93 ('[P14 B1] grades a cap-guard against tests/cap_spelling_corpus.py and prints the denominator. Measurement, not a gate: it scores whatever guard it is pointed at, so it has no pass/fail opinion of its own...') now sit immediately above line 94, `"make_hook_screenshot.py", # docs asset generation`, and read as its reason. The reasoning it states is also the reasoning score_corpus.py's own docstring (line 58) and AUX_GATES entry (line 74-79) explicitly record as WRONG. Related blind spot in the same 
- **failure scenario:** A reader auditing which tools are ungated sees a paragraph of reasoning attached to make_hook_screenshot.py that describes a different file and states a position the repo has since reversed. Separately, if a tool is ever promoted to AUX_GATES without its NOT_A_GATE entry being deleted, the exemption survives indefinitely with no check able to report it - the same rot the stale_exempt check was added to prevent, one direction over.
- **refuter:** REFUTATION FAILED - every element is concrete, reproducible, and novel. (1) ORPHANED COMMENT - confirmed by git archaeology. `git show 7d789c3 -- run_selftests.py` shows the promotion commit deleted exactly one line (`"score_corpus.py", # measurement (grades a guard against the cap corpus)`) and left the five-line `# [P14 B1]` justification block above it. `cat -A` on the shipped file confirms lines 89-93 now sit flush against line 94, `"make_hook_screenshot.py", # docs asset generation`. (2) MISATTRIBUTION IS UNAMBIGUOUS - the orphan says "grades a cap-guard against tests/cap_spelling_corpus.

## REFUTED (14) - kept, because a cluster of near-misses is itself a signal

- **The documented index-bounds-check exclusion is inert: _counter_test re-flags exactly what _size_test_target rejected** (`hooks/cap_shapes.py`) - REFUTED on all three load-bearing assertions, each disproved by running the code in a scratch copy at C:\Users\ammar\AppData\Local\Temp\claude\C--Users-ammar-Downloads-Claude\3ce9eb90-5173-4238-b390-b3aa551647f4\scratchpad\ub_idxbound_lens (pristine repo untou
- **The aggregate rule (clause 2) is a closed roster of 12 builtin names, so any other reducer leaves the slice flagged** (`hooks/cap_types.py`) - REPRODUCED but REFUTED as filed. All experiments in scratch copy C:\Users\ammar\AppData\Local\Temp\claude\lens_agg_roster_clause2\unbluff; origin tree untouched (the one dirty file, docs/V131_REVIEW_PLAN.md, has mtime 06:56 vs my session start 07:27, and my re
- **The sweep's glob misses .pyi and, on Linux/macOS CI only, any .PY file - the same repo is swept differently on Windows t** (`hooks/cap_shapes.py`) - The MECHANISM reproduces, but every element of the claimed HARM is refuted by measurement. REPRODUCED (so the finding is not fabricated): a probe planting plain.py / UPPER.PY / Mixed.Py / stub.pyi / notpy.txt each carrying the same head-slice cap, driven throu
- **run_entry catches Exception but not BaseException, so a guard that calls sys.exit kills the scoring run with no totals** (`tools/score_corpus.py`) - REFUTED on three independent grounds, each verified by running the project's own tooling in a scratch copy (C:\Users\ammar\AppData\Local\Temp\claude\lens_sysexit_bex_7c3f\unbluff). The real tree was never written to (git status shows only the pre-existing "M d
- **_size_test_target accepts == and != as bounds; _counter_test already learned this lesson and _size_test_target never got** (`hooks/cap_shapes.py`) - REFUTED on four independent grounds, all demonstrated by running the code in a scratch copy. (1) FABRICATED EVIDENCE. The claimed live site "csv.py:415" does not exist: `find . -name "csv*.py"` returns nothing anywhere in the repo, and hooks/ contains 21 files
- **The retention rule for tail slices requires a literal .append outside a loop, so a comprehension-built history window fl** (`hooks/cap_shapes.py`) - REFUTED. The reproduction is real (case O does flag) but it is CORRECT behaviour, pinned as must_flag=True by the project's own corpus, and the claim's stated mechanism and live evidence are both wrong. (1) CORPUS ALREADY LABELS THIS CLASS MUST-FLAG. tests/cap
- **mutation_check's headline denominator counts HARNESS ERRORs as executed mutations** (`tools/mutation_check.py`) - REFUTED AS NOT-NEW (the code claim itself is true and I reproduced it). VERIFIED TRUE: tools/mutation_check.py:777 reads `executed = considered - len(skipped) - len(other_platform) - len(unproven)` and never subtracts `len(errors)`. Every HARNESS ERROR return 
- **Three different stamped corpus denominators for one measurement, one of them internally impossible** (`hooks/cap_shapes.py`) - REFUTED on the substance. I reconstructed tests/cap_spelling_corpus.py at each commit in its history and re-ran the repo's own tools/score_corpus.py (HEAD version) against every snapshot, scoring both the predecessor blob (hooks/capped_report.py @0204224) and 
- **The --selftest gate goes RED on the exact case split_corpus's docstring says it supports, and contradictions() cannot se** (`tools/score_corpus.py`) - REFUTED on both halves, by running the code in a scratch copy at ...\scratchpad\refute_scorer_corpus_split\ (probe_a.py through probe_d.py). (a) THE PREMISE IS FALSE. The failure scenario rests on "a reviewer adds one negative control to NEGATIVE_CONTROLS - th
- **Clause 1's 'unknown flags' turns _SCALAR_CALLS into the predecessor's unbounded enumeration, pointed at false alarms** (`hooks/cap_types.py`) - MECHANISM REPRODUCES, FINDING AS FRAMED IS REFUTED ON THREE GROUNDS. What I confirmed by running code in a scratch copy (...\scratchpad\lens_clause1_roster\unbluff): - _module_sites suppresses only on kind == "scalar" (cap_shapes.py:456-459), so "unknown" flag
- **23 roster entries across _COLLECTION_CALLS and _COLLECTION_ANNOTATIONS are provably inert - emptying either roster entir** (`hooks/cap_types.py`) - REFUTED - the measurement reproduces but the claim built on it is false, and acting on it would introduce a fail-open. WHAT REPRODUCED. In a scratch copy (C:\Users\ammar\AppData\Local\Temp\lens_inert_roster_probe\), baseline = CAUGHT 102/105, FALSE-POS 0/30, l
- **score_corpus.py always exits 0: the scorer that grades the ship-blocking guard cannot fail, and a fully disarmed guard s** (`tools/score_corpus.py`) - REFUTED as filed. The mechanical observation is true; the load-bearing impact claim is demonstrably false, and the failure scenario is hypothetical. WHAT REPRODUCES (the finding's true half). tools/score_corpus.py:241 is `raise SystemExit(selftest() if "--self
- **split_corpus dedups negatives by name BEFORE the selftest's duplicate-name check looks, so a name collision silently shr** (`tools/score_corpus.py`) - REFUTED by running the exact scenario. The reviewer's mechanism observation is correct in isolation but they audited the wrong check, and the stated consequence does not reproduce. CORRECT PREMISE: split_corpus does dedup negatives by name (tools/score_corpus.
- **POSITIONAL_FLOOR = 5 excludes 5 itself; the commonest positional destructures are 5 and 6 wide - LIVE false alarm in thi** (`hooks/cap_types.py`) - The mechanism reproduces but the finding is not new, not live, and mis-diagnosed. REPRODUCED: _small_int (hooks/cap_types.py:155-158) tests `node.value < POSITIONAL_FLOOR`, so 5 is not small. The F1 fixture flags: "f1_parseaddr: r() slices to a bound at line 4
