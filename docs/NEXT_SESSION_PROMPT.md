# Next session

**This file is a POINTER. The canonical order is [docs/PLAN.md](PLAN.md). Nothing else.**

It used to hold a second, competing order - 66 lines describing themselves as "the single
canonical recommended order", for **v1.0**, dated 2026-08-16, citing a 38/38 suite and a 209/211
sweep. All of that is long dead and none of it agreed with the plan.

`#10` says this must not exist. The close meta-review's CHECK 6 found it still did on 2026-08-24,
two days after `#10` was written. Worse: earlier in that same session I told the user this file
was ABSENT - I had checked the repository root, and it lives in `docs/`. A verification that looks
in the wrong place returns a comforting answer, which is the failure this repo exists to refuse.

Reduced to a pointer rather than deleted, so that anyone following a stale link lands here and is
sent to the plan, instead of concluding the plan does not exist.

Start every session by reading `docs/PLAN.md`: Phase 1 is the ordered gate list (gate 0 is the
current release blocker), Phase 2 is the post-release backlog, and the standing checks at the
bottom are the rules that have each already cost something.
